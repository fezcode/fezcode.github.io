<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
        <title><![CDATA[Fezcodex]]></title>
        <description><![CDATA[A personal blog by Ahmed Samil Bulbul]]></description>
        <link>https://fezcode.com</link>
        <image>
            <url>https://fezcode.com/logo512.png</url>
            <title>Fezcodex</title>
            <link>https://fezcode.com</link>
        </image>
        <generator>RSS for Node</generator>
        <lastBuildDate>Thu, 08 Jan 2026 15:50:54 GMT</lastBuildDate>
        <atom:link href="https://fezcode.com/rss.xml" rel="self" type="application/rss+xml"/>
        <pubDate>Thu, 08 Jan 2026 15:50:54 GMT</pubDate>
        <copyright><![CDATA[2026 Ahmed Samil Bulbul]]></copyright>
        <language><![CDATA[en]]></language>
        <managingEditor><![CDATA[samil.bulbul@gmail.com (Ahmed Samil Bulbul)]]></managingEditor>
        <webMaster><![CDATA[samil.bulbul@gmail.com (Ahmed Samil Bulbul)]]></webMaster>
        <ttl>60</ttl>
        <item>
            <title><![CDATA[Fixing gh-pages: Resolving spawn ENAMETOOLONG]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/gh-pages-enametoolong-fix</link>
            <guid isPermaLink="false">https://fezcode.com/blog/gh-pages-enametoolong-fix</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Thu, 08 Jan 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Resolving <code>spawn ENAMETOOLONG</code> in <code>gh-pages</code> Deployment</h1>
<p><a href="https://fezcode.com/blog/gh-pages-enametoolong-fix">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Aether: Cyberpunk Audio Interface]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/aether-music-player</link>
            <guid isPermaLink="false">https://fezcode.com/blog/aether-music-player</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 06 Jan 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Aether: The Cyberpunk Music Player</h1>
<p>I&#39;ve just deployed <strong>Aether</strong>, a new cloud-based music player for Fezcodex.</p>
<p><img src="/images/apps/aether.png" alt="Aether Music Player"></p>
<h2>Overview</h2>
<p>Aether isn&#39;t just a music player; it&#39;s an atmospheric audio interface designed to immerse you in the soundscape of the site. It features:</p>
<ul>
<li><strong>Cyberpunk Aesthetic:</strong> A high-contrast, terminal-inspired interface with CRT scanlines, glitch effects, and a generative art background that reacts to the music.</li>
<li><strong>Persistent Playback:</strong> A tiny, &quot;cyber deck&quot; style player docks to the bottom of your screen, allowing you to browse the site without interrupting your tunes.</li>
<li><strong>Generative Art:</strong> If a track lacks cover art, the system generates a unique visual signature based on the track&#39;s title.</li>
</ul>
<p>Check it out here: <a href="/apps/aether">Aether Music Player</a></p>
<p>Enjoy the vibes.</p>
<p><a href="https://fezcode.com/blog/aether-music-player">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[5 Ways to Pass Arguments in a URL (Beyond the Basic Query)]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/5-ways-to-pass-arguments-in-a-url</link>
            <guid isPermaLink="false">https://fezcode.com/blog/5-ways-to-pass-arguments-in-a-url</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 24 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>When building web applications or designing APIs, understanding how to transfer data is crucial. While <strong>Query Parameters</strong> (the bits after the <code>?</code>) are the most common method, there are four other fundamental ways to pass arguments to a server via a URL or its associated HTTP request.</p>
<p>Here is a quick reference guide to the five main argument passing mechanisms:</p>
<h2>1. Query Parameters</h2>
<ul>
<li><strong>Location:</strong> Appears in the URL after a <strong>?</strong> (question mark) and separated by <strong>&amp;</strong> (ampersand) symbols.</li>
<li><strong>Purpose:</strong> Used for <strong>optional</strong> parameters such as filtering, sorting, searching, or pagination controls.</li>
<li><strong>Characteristics:</strong> Data is highly visible (in the URL, server logs, and browser history). It is typically used with <strong>GET</strong> requests.</li>
<li><strong>Example:</strong> <code>https://example.com/products?**category=1&amp;sort=price**</code></li>
</ul>
<h2>2. Path Parameters</h2>
<ul>
<li><strong>Location:</strong> Directly integrated into the URL&#39;s path structure.</li>
<li><strong>Purpose:</strong> Used to uniquely <strong>identify a specific resource</strong> or define a hierarchical location.</li>
<li><strong>Characteristics:</strong> Essential for defining clear, clean, and meaningful URLs, especially in RESTful API design.</li>
<li><strong>Example:</strong> <code>https://example.com/users/**123**</code> or <code>https://example.com/books/**sci-fi**/dune</code></li>
</ul>
<h2>3. Header Parameters</h2>
<ul>
<li><strong>Location:</strong> Contained within the <strong>HTTP Request Header</strong>, invisible in the URL.</li>
<li><strong>Purpose:</strong> Used for <strong>metadata</strong> about the request, such as authentication (e.g., API keys, tokens), content type, and language preferences.</li>
<li><strong>Characteristics:</strong> Offers better security for sensitive, non-data payload information compared to Query Parameters, as it doesn&#39;t appear in the URL.</li>
<li><strong>Example:</strong> <code>Header: **Authorization: Bearer token**</code> or <code>Header: **Content-Type: application/json**</code></li>
</ul>
<h2>4. Fragment Identifier Arguments</h2>
<ul>
<li><strong>Location:</strong> Appears at the very end of the URL after a <strong>#</strong> (hash symbol).</li>
<li><strong>Purpose:</strong> Used for client-side functionality, like navigating to a specific section (anchor) on a page or managing application state in Single Page Applications (SPAs).</li>
<li><strong>Characteristics:</strong> The browser <strong>does NOT send this part to the server</strong>; it is client-side only. It can still be used to pass data to the front-end application.</li>
<li><strong>Example:</strong> <code>https://example.com/page**#section-name**</code></li>
</ul>
<h2>5. Request Body Arguments</h2>
<ul>
<li><strong>Location:</strong> Contained within the <strong>body</strong> (payload) of the HTTP request, invisible in the URL.</li>
<li><strong>Purpose:</strong> Used for sending large data payloads when creating or updating resources (e.g., submitting a complex form, uploading a file, or sending a JSON object).</li>
<li><strong>Characteristics:</strong> The primary method for data submission using <strong>POST, PUT, or PATCH</strong> HTTP methods. It is an HTTP request argument, not a true URL argument, and it is secure from URL exposure.</li>
<li><strong>Example:</strong> <em>(Data like a user object in JSON format is sent in the hidden body payload.)</em></li>
</ul>
<hr>
<h2>Conclusion</h2>
<p>By strategically selecting among Query, Path, Header, Fragment, or Body arguments, developers can ensure their data is transferred efficiently and securely, leading to a robust and scalable application architecture.</p>
<p><a href="https://fezcode.com/blog/5-ways-to-pass-arguments-in-a-url">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Quick Renaming .js to .jsx for React]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/renaming-js-to-jsx-gist</link>
            <guid isPermaLink="false">https://fezcode.com/blog/renaming-js-to-jsx-gist</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 23 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h2>The Command (PowerShell)</h2>
<p>I ran this specific script in your terminal. It uses &quot;piping&quot; (<code>|</code>) to pass the result of one command to the next, like a bucket brigade.</p>
<pre><code class="language-powershell">Get-ChildItem -Path src -Recurse -Filter *.js |
  Where-Object { $_.Name -notin @(&#39;index.js&#39;, &#39;reportWebVitals.js&#39;, &#39;setupTests.js&#39;) } |
  ForEach-Object {
    if (Select-String -Pattern &quot;&lt;[a-zA-Z]&quot; -Path $_.FullName -Quiet) {
      Write-Host &quot;Renaming $($_.Name) to .jsx&quot;;
      Rename-Item -LiteralPath $_.FullName -NewName ($_.Name -replace &#39;\.js$&#39;, &#39;.jsx&#39;)
    }
  }
</code></pre>
<h3>Deep Dive: Commands &amp; Arguments</h3>
<p>Here is exactly what every part of that spell does:</p>
<p><strong>1. Finding the files</strong>
<code>Get-ChildItem -Path src -Recurse -Filter *.js</code></p>
<ul>
<li><code>Get-ChildItem</code>: The standard command to list files (like <code>ls</code> or <code>dir</code>).</li>
<li><code>-Path src</code>: We only look inside the <code>src</code> folder.</li>
<li><code>-Recurse</code>: We dig deep into every subfolder, not just the top level.</li>
<li><code>-Filter *.js</code>: We ignore everything except files ending in <code>.js</code>.</li>
</ul>
<p><strong>2. Filtering the list</strong>
<code>Where-Object { $_.Name -notin @(...) }</code></p>
<ul>
<li><code>Where-Object</code>: Acts like a bouncer; only lets items through that match the condition.</li>
<li><code>$_</code>: Represents the &quot;current file&quot; being checked.</li>
<li><code>-notin</code>: The condition operator. We are saying &quot;The name must NOT be in this list&quot;.</li>
<li><code>@(&#39;index.js&#39;, ...)</code>: The list of system files we want to skip (leave as <code>.js</code>).</li>
</ul>
<p><strong>3. Processing each file</strong>
<code>ForEach-Object { ... }</code></p>
<ul>
<li><code>ForEach-Object</code>: Runs the code block inside <code>{ ... }</code> for every single file that made it past the filter.</li>
</ul>
<p><strong>4. Checking for React Code (JSX)</strong>
<code>if (Select-String -Pattern &quot;&lt;[a-zA-Z]&quot; -Path $_.FullName -Quiet)</code></p>
<ul>
<li><code>Select-String</code>: Searches for text inside a file (like <code>grep</code>).</li>
<li><code>-Pattern &quot;&lt;[a-zA-Z]&quot;</code>: A Regex pattern. It looks for a <code>&lt;</code> followed by a letter. This catches HTML tags like <code>&lt;div&gt;</code> or React components like <code>&lt;App&gt;</code>.</li>
<li><code>-Path $_.FullName</code>: The full path to the file we are currently reading.</li>
<li><code>-Quiet</code>: Important! This tells the command &quot;Don&#39;t print the matching text, just tell me True or False.&quot;</li>
</ul>
<p><strong>5. Renaming the file</strong>
<code>Rename-Item -LiteralPath $_.FullName -NewName (...)</code></p>
<ul>
<li><code>Rename-Item</code>: The command to change a file&#39;s name.</li>
<li><code>-LiteralPath $_.FullName</code>: We use the full path to ensure we target the exact right file.</li>
<li><code>-NewName ($_.Name -replace &#39;\.js$&#39;, &#39;.jsx&#39;)</code>: We calculate the new name by taking the old name and swapping the ending <code>.js</code> with <code>.jsx</code>.</li>
</ul>
<p><strong>The Result</strong></p>
<p>Now your code editor knows exactly which files contain UI components. You&#39;ll get better autocomplete, better color highlighting, and generally a much happier development experience.</p>
<p><a href="https://fezcode.com/blog/renaming-js-to-jsx-gist">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[The Corrupted Blood Incident: When a Glitch Taught Us About Pandemics]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/corrupted-blood-incident</link>
            <guid isPermaLink="false">https://fezcode.com/blog/corrupted-blood-incident</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sun, 21 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p><em>Okay, gather &#39;round</em>, fellow nerds and accidental epidemiologists.
I need to talk about something that happened in <em>World of Warcraft</em> back in 2005.
It&#39;s called the <strong>Corrupted Blood incident</strong>, and it&#39;s basically the coolest (and most terrifying) accidental science experiment in gaming history.</p>
<iframe width="560" height="315" src="https://www.youtube.com/embed/vj-mz5piYYY?si=RlcxH-t2Q7-2bYIJ" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

<h2>ELI5: What the Heck Happened?</h2>
<p>Imagine you&#39;re playing a game, right? You and your 19 closest friends decide to go punch a giant blood god named <strong>Hakkar the Soulflayer</strong> in the face. This raid boss has a nasty spell called &quot;Corrupted Blood.&quot;</p>
<p>Here&#39;s how it worked:</p>
<ol>
<li><strong>You catch it:</strong> It drains your health. Fast.</li>
<li><strong>It spreads:</strong> If you stand near anyone else, they catch it too. Like a super-flu.</li>
<li><strong>It&#39;s meant for the boss room:</strong> The disease was programmed to disappear when you died or left the dungeon.</li>
</ol>
<p><strong>BUT HERE&#39;S THE GLITCH.</strong></p>
<p>Hunter pets (animals that players control) could catch the disease. If a player dismissed their pet while it was sick, the game &quot;froze&quot; the pet&#39;s state.
When they summoned the pet back in a major city (like Ironforge or Orgrimmar), the pet came back... <em>still sick</em>.</p>
<p><em>Boom. Patient Zero.</em></p>
<h2>The Virtual Apocalypse</h2>
<p>Suddenly, high-level players&#39; pets were nuking entire cities. Low-level players (newbies) were dropping dead instantly just by walking past the auction house.
High-level players were scrambling to keep themselves alive, healing frantically.</p>
<p>It was chaos.</p>
<ul>
<li><strong>The Cities:</strong> Zones of death. Skeletons everywhere.</li>
<li><strong>The Players:</strong> Panic. Some fled to the wilderness (social distancing!). Some deliberately spread it (trolls/bioterrorists). Healers tried to set up triage centers.</li>
<li><strong>Blizzard (The Devs):</strong> They tried quarantines. Failed. They tried warnings. Failed. Eventually, they had to do a hard server reset to scrub the disease from existence.</li>
</ul>
<h2>Why Real Scientists Cared</h2>
<p>Here&#39;s the wild part. Real-world epidemiologists (the doctors who study diseases) looked at this and went, <em>&quot;Holy crap, this is better than our computer models.&quot;</em></p>
<p>Usually, scientific models assume people act rationally. &quot;If there is a plague, people will stay home.&quot; But in <em>WoW</em>, people did <strong>human</strong> things:</p>
<ul>
<li><strong>Curiosity:</strong> &quot;What&#39;s happening over there?&quot; -&gt; <em>Dies.</em></li>
<li><strong>Malice:</strong> &quot;Imma go infect the newbies lol.&quot; -&gt; <em>Spreads plague.</em></li>
<li><strong>Altruism:</strong> &quot;I&#39;ll heal you!&quot; -&gt; <em>Gets infected, spreads it further.</em></li>
</ul>
<p>This accidental glitch provided a perfect, unscripted look at human behavior during a crisis. It showed how fast things spread when people don&#39;t follow rules,
how asymptomatic carriers (pets/high-level players) can destroy vulnerable populations (low-level players), and how hard it is to contain stupidity.</p>
<h2>The GDC Legacy</h2>
<p>This wasn&#39;t just a &quot;remember when&quot; moment. It became a serious case study. At <strong>GDC (Game Developers Conference)</strong>,
this incident is often cited as a prime example of emergent gameplay and complex systems gone wrong (or right, depending on your view).</p>
<p>It taught developers that players will <em>always</em> find a way to break containment.
It taught scientists that &quot;Gamer Behavior&quot; might actually be a decent proxy for &quot;Human Panic.&quot;</p>
<h2>The Rant</h2>
<p>It drives me crazy that we had this perfect simulation in 2005, and when 2020 rolled around, we saw the <em>exact same behaviors</em> IRL.
The deniers, the spreaders, the people fleeing to the countryside. We didn&#39;t learn! We leveled up, but we didn&#39;t put any points into Wisdom!</p>
<p><strong>TL;DR:</strong> A coding bug in a fantasy game predicted modern pandemic behavior better than some government models.
Hakkar the Soulflayer is the ultimate teacher. Wash your hands, dismiss your pets responsibly, and for the love of Azeroth, stop standing in the fire.</p>
<p><a href="https://fezcode.com/blog/corrupted-blood-incident">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Building the Knowledge Graph: Visualizing Fezcodex in 3D]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/building-the-knowledge-graph</link>
            <guid isPermaLink="false">https://fezcode.com/blog/building-the-knowledge-graph</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sun, 21 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>The idea was simple: Fezcodex is growing. With hundreds of blog posts, apps, and project logs, a standard list or grid view just wasn&#39;t cutting it anymore. I wanted a way to visualize the <em>connections</em> between everything. To see the &quot;brain&quot; of the website.</p>
<p>Enter the <strong>Knowledge Graph Visualization Protocol</strong>.</p>
<h2>The Concept</h2>
<p>I wanted a 3D, interactive network graph where:</p>
<ul>
<li><strong>Nodes</strong> represent content (Blog Posts, Apps, Projects).</li>
<li><strong>Links</strong> represent relationships (Shared tags, Categories).</li>
<li><strong>Interaction</strong> allows users to fly through the data and navigate to content.</li>
</ul>
<p>It needed to feel like a &quot;cyberspace&quot; visualization from a sci-fi movie—immersive, dark, and slightly chaotic but organized.</p>
<h2>The Tech Stack</h2>
<ul>
<li><strong>React</strong>: The core framework.</li>
<li><strong>react-force-graph-3d</strong>: The heavy lifter. This library uses WebGL (via Three.js) to render force-directed graphs with great performance.</li>
<li><strong>PIML</strong>: My custom markup language for parsing project data.</li>
<li><strong>Tailwind CSS</strong>: For the overlay UI and brutalist styling.</li>
</ul>
<h2>Implementation Details</h2>
<h3>1. Data Extraction (<code>graphDataManager.js</code>)</h3>
<p>The first challenge was aggregating data from three different sources:</p>
<ul>
<li><code>posts.json</code>: A static JSON file containing blog metadata.</li>
<li><code>apps.json</code>: A structured list of all the mini-apps.</li>
<li><code>projects.piml</code>: A custom file format for my project portfolio.</li>
</ul>
<p>I created a utility function <code>fetchGraphData</code> that pulls all three.</p>
<pre><code class="language-javascript">export const fetchGraphData = async () =&gt; {
  const nodes = [];
  const links = [];
  const tagMap = new Map();

  // ... fetching logic ...
</code></pre>
<p>For each item, I created a <strong>primary node</strong>. Then, I looked at its <code>tags</code>, <code>category</code>, or <code>technologies</code>. For every tag found, I created a <strong>tag node</strong> (if it didn&#39;t exist) and created a <strong>link</strong> between the item and the tag.</p>
<p>This automatically creates clusters. If five posts are tagged &quot;React&quot;, they all link to the &quot;React&quot; tag node, pulling them together in the 3D space.</p>
<h3>2. The 3D Component (<code>KnowledgeGraphPage.js</code>)</h3>
<p>I used <code>&lt;ForceGraph3D&gt;</code> to render the data.</p>
<pre><code class="language-jsx">&lt;ForceGraph3D
    ref={fgRef}
    graphData={graphData}
    backgroundColor=&quot;#050505&quot;
    nodeLabel=&quot;name&quot;
    nodeColor=&quot;color&quot;
    onNodeClick={handleNodeClick}
    // ...
/&gt;
</code></pre>
<h3>3. Camera Controls</h3>
<p>The &quot;cool factor&quot; comes from the camera movement. When you click a node, I didn&#39;t want a hard jump. I wanted a smooth flight.</p>
<pre><code class="language-javascript">  const handleNodeClick = useCallback((node) =&gt; {
    // Calculate a position slightly &quot;outside&quot; the node
    const distance = 40;
    const distRatio = 1 + distance/Math.hypot(node.x, node.y, node.z);

    if (fgRef.current) {
        fgRef.current.cameraPosition(
            { x: node.x * distRatio, y: node.y * distRatio, z: node.z * distRatio }, // new pos
            node, // lookAt
            3000  // ms duration
        );
    }
  }, []);
</code></pre>
<p>This calculates a vector from the center (0,0,0) to the node, extends it by a fixed distance, and moves the camera there while focusing on the node.</p>
<h2>Challenges</h2>
<ul>
<li><strong>PIML Parsing</strong>: My custom language parser needed to be robust enough to handle the varying structures of the <code>projects.piml</code> file.</li>
<li><strong>Performance</strong>: Rendering hundreds of text labels in 3D can be heavy. I kept the UI minimal and only showing detailed info on hover.</li>
<li><strong>Theme</strong>: Matching the &quot;Brutalist/Cyberpunk&quot; aesthetic required careful tuning of node colors (Emerald for Apps, Red for Posts) and link opacity.</li>
</ul>
<h2>The Result</h2>
<p>The result is a living, breathing map of Fezcodex. It reveals patterns I didn&#39;t explicitly plan—like the massive cluster around &quot;React&quot; or the isolated islands of specific game experiments. It&#39;s not just a navigation tool; it&#39;s a piece of generative art powered by my own work.</p>
<p>Go check it out at <code>/graph</code> and fly through the system.</p>
<p><a href="https://fezcode.com/blog/building-the-knowledge-graph">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Routing Revolution: SSG, BrowserRouter, and the SEO Fix]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/routing-revolution-ssg-and-seo</link>
            <guid isPermaLink="false">https://fezcode.com/blog/routing-revolution-ssg-and-seo</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sun, 21 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Routing Revolution: SSG, BrowserRouter, and the SEO Fix</h1>
<p>For a long time, Fezcodex lived behind the &quot;Hash Gap.&quot; If you looked at your address bar, you’d see that familiar <code>/#/</code> slicing through every URL. While functional, this was the primary reason social media thumbnails were failing and search engines were only seeing the home page.</p>
<p>Today, I’ve completed a total migration to <strong><a href="/vocab/spa">BrowserRouter</a></strong> combined with <strong><a href="/vocab/ssg">SSG (Static Site Generation)</a></strong>. Here is the technical breakdown of why this was necessary and how it works.</p>
<h2>The Problem: The Hash Black Hole</h2>
<p>We originally used <code>HashRouter</code> because Fezcodex is hosted on GitHub Pages. Since GitHub Pages is a static file host, it doesn&#39;t know how to handle a request for <code>/apps/markdown-table-formatter</code>. It looks for a folder named <code>apps</code> and an <code>index.html</code> inside it. When it doesn&#39;t find them, it throws a 404.</p>
<p><code>HashRouter</code> solved this by putting everything after the <code>#</code>. The server ignores the hash, always serves the root <code>index.html</code>, and React handles the rest. </p>
<p><strong>The SEO Cost:</strong> Most <strong><a href="/vocab/crawler">crawlers</a></strong> (Twitter, Facebook, Discord) do not execute JavaScript and ignore the hash entirely. To them, every single link you shared looked like <code>fezcode.com/</code>—resulting in generic &quot;Fezcodex - Personal Blog&quot; thumbnails instead of page-specific content.</p>
<h2>The Solution Part 1: BrowserRouter</h2>
<p>I switched the core engine from <code>HashRouter</code> to <code>BrowserRouter</code>. This gives us &quot;clean&quot; URLs:</p>
<ul>
<li><strong>Old:</strong> <code>fezcode.com/#/blog/my-post</code></li>
<li><strong>New:</strong> <code>fezcode.com/blog/my-post</code></li>
</ul>
<p>But how do we make this work on a static host without a backend?</p>
<h2>The Solution Part 2: react-snap &amp; SSG</h2>
<p>Enter <strong><a href="/vocab/ssg">Static Site Generation</a></strong> via <code>react-snap</code>. </p>
<p>Instead of shipping a nearly empty <code>index.html</code> and letting the browser build the page (<strong><a href="/vocab/csr">Client-Side Rendering</a></strong>), we now build the pages during the deployment phase. </p>
<ol>
<li><strong>The Crawl:</strong> During <code>npm run build</code>, <code>react-snap</code> fires up a headless browser (Puppeteer).</li>
<li><strong>The Snapshot:</strong> It visits every route defined in our sitemap and apps list.</li>
<li><strong>The Export:</strong> It captures the fully rendered HTML (including meta tags, titles, and unique descriptions) and saves it as a physical <code>index.html</code> file in a matching folder structure.</li>
</ol>
<p>In our latest build, this generated <strong>281 unique HTML files</strong>. Now, when you share a link, the crawler sees a real, static HTML file with the correct <strong><a href="/vocab/open-graph">Open Graph</a></strong> tags immediately.</p>
<h2>The Solution Part 3: Hydration</h2>
<p>Once the browser loads the static HTML, we don&#39;t want to lose the interactivity of React. I updated <code>src/index.js</code> to use <code>ReactDOM.hydrateRoot</code>. </p>
<p>This process, known as <strong><a href="/vocab/hydration">Hydration</a></strong>, allows React to &quot;attach&quot; to the existing HTML already on the screen rather than re-rendering everything from scratch. It preserves the fast initial load of a static site with the power of a modern web app.</p>
<h2>Global Content Cleanup</h2>
<p>Switching the router was only half the battle. Thousands of internal links within our <code>.piml</code> logs and <code>.txt</code> blog posts still pointed to the old <code>/#/</code> structure.</p>
<p>I executed a global recursive replacement across the <code>public/</code> directory:</p>
<pre><code class="language-powershell">Get-ChildItem -Path public -Include *.json, *.txt, *.piml, *.md -Recurse | 
ForEach-Object { (Get-Content $_.FullName) -replace &#39;/#/&#39;, &#39;/&#39; | Set-Content $_.FullName }
</code></pre>
<p>This ensured that the entire ecosystem—from the timeline to the project descriptions—is now synchronized with the new routing architecture.</p>
<h2>Conclusion</h2>
<p>Fezcodex is no longer just a Single Page Application; it is a high-performance, SEO-optimized static engine. Clean URLs, unique thumbnails, and faster perceived load times are now the standard.</p>
<p><a href="https://fezcode.com/blog/routing-revolution-ssg-and-seo">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[The Art of the Algorithm: Generative Visuals in Fezcodex]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/art-generation-in-fezcodex</link>
            <guid isPermaLink="false">https://fezcode.com/blog/art-generation-in-fezcodex</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 20 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Art in the digital age isn&#39;t just about pixels and brushes; it&#39;s about rules, logic, and mathematics. In Fezcodex, I&#39;ve integrated several &quot;visual experiments&quot; that turn code into aesthetics. From the subtle backgrounds of cards to full-blown design laboratories, let&#39;s explore how these algorithms work.</p>
<h2>The Heart: GenerativeArt</h2>
<p>You might have noticed unique geometric patterns appearing behind various elements in the site. These are powered by the <code>GenerativeArt</code> component.</p>
<h3>Seeded Randomness</h3>
<p>Imagine you have a robot that can draw. If you tell the robot &quot;draw something random,&quot; it might draw a circle today and a square tomorrow. But what if you want the robot to draw the <strong>same</strong> &quot;random&quot; thing every time you say the word &quot;Apple&quot;?</p>
<p>That is what a <strong>Seed</strong> does. The algorithm takes a word (like a project name or a date), turns it into a number, and uses that number to make every &quot;random&quot; choice. Because the starting number is the same, the result is always the same. This is why &quot;Project A&quot; always has its own unique, permanent visual identity.</p>
<h3>How it generates symbols</h3>
<p>The component uses a &quot;Bauhaus Grid&quot; logic to create symbols. Here is the step-by-step:</p>
<ol>
<li><strong>The Grid:</strong> It divides a square into a 5x5 grid.</li>
<li><strong>The Coin Flip:</strong> For each square in the grid, it flips a digital coin to decide if it should draw something there.</li>
<li><strong>The Shape:</strong> If it decides to draw, it picks one of four shapes: a square, a circle, a quarter-circle, or a triangle.</li>
<li><strong>The Twist:</strong> It rotates the shape by 0, 90, 180, or 270 degrees.</li>
<li><strong>The Color:</strong> It picks colors from a palette generated based on the same seed.</li>
</ol>
<p>By combining these simple rules, the algorithm creates complex, balanced symbols that look like modern art but are just math in disguise.</p>
<h2>The Laboratory: BlendLab</h2>
<p>While <code>GenerativeArt</code> is about sharp geometry, <strong>BlendLab</strong> is about atmosphere and &quot;vibe.&quot; It uses a coordinate-based system to create abstract color fields.</p>
<p>In BlendLab, you position different &quot;entities&quot; (points of color) on a digital canvas. The algorithm then applies heavy Gaussian blurs and noise filters. This blends the distinct points into a smooth, flowing gradient. When combined with high-impact typography, it creates a style often seen in modern &quot;Brutalist&quot; design.</p>
<h2>The Creative Suite</h2>
<p>Beyond these two, Fezcodex houses several other specialized art generators:</p>
<ul>
<li><strong>Topographic Maps:</strong> Uses &quot;Perlin Noise&quot; (a type of smooth, natural-looking randomness) to create height maps. By drawing lines at specific height levels, it creates the look of a physical map.</li>
<li><strong>Abstract Waves:</strong> Uses Trigonometry (Sine and Cosine waves). By layering multiple waves with slight offsets and adding a &quot;noise&quot; distortion, it generates 3D-looking landscapes reminiscent of retro album covers.</li>
<li><strong>Fractal Flora:</strong> Uses &quot;Recursion&quot;—a function that calls itself. To draw a tree, the code draws a trunk, then tells itself to &quot;draw two smaller branches at the end,&quot; and repeats this until a full, organic-looking tree is formed.</li>
<li><strong>Spirograph:</strong> Uses the classic math of hypotrochoids and epitrochoids. It tracks the path of a point on a circle rolling inside or outside another circle.</li>
</ul>
<h2>Conclusion</h2>
<p>Code is often seen as cold and rigid, but when we introduce randomness and recursion, it becomes a brush. Fezcodex is a sandbox for these experiments, proving that the pursuit of code can indeed be an art form.</p>
<p><a href="https://fezcode.com/blog/art-generation-in-fezcodex">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Brutalist Fezcodex: The Big Cleanup]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/brutalist-refactor</link>
            <guid isPermaLink="false">https://fezcode.com/blog/brutalist-refactor</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 19 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Today was a huge day for Fezcodex. We did a &quot;Major Refactor.&quot;</p>
<h3>What did we change?</h3>
<ol>
<li><strong>Brutalist Style</strong>: We made the site look bold and strong, like a high-tech terminal. Big letters, sharp edges, and high contrast.</li>
<li><strong>Generative Art</strong>: We added &quot;math art&quot; that draws itself in the background. It&#39;s unique every time you look at it!</li>
<li><strong>Timeline &amp; Games</strong>: We updated the Timeline and the Memory Game to fit this cool new look.</li>
<li><strong>The &quot;Under the Hood&quot; Stuff</strong>: We cleaned up the code (the &quot;linter&quot; stuff). We removed unused pieces and fixed tiny mistakes that make the computer happy.</li>
</ol>
<p>The garden is now cleaner, faster, and much more &quot;Brutalist.&quot;</p>
<h2>Enjoy the new vibe!</h2>
<p><em>Log Entry: 2025-12-19</em></p>
<p><a href="https://fezcode.com/blog/brutalist-refactor">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Introducing New Reading Modes: Dossier and Terminal!]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/introducing-reading-experience</link>
            <guid isPermaLink="false">https://fezcode.com/blog/introducing-reading-experience</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 17 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Excited to unveil two brand new ways to experience content on Fezcodex Blogposts: <strong>Dossier Mode</strong> and <strong>Terminal Mode!</strong>
These unique reading modes are designed to offer a fresh and engaging perspective, allowing you to tailor your browsing experience to your personal style.</p>
<h2>What&#39;s New?</h2>
<h3>1. Dossier Mode</h3>
<p>Step into the role of an investigator with <strong>Dossier Mode</strong>. This mode transforms the blogpost&#39;s interface into a sleek,
document-style layout, reminiscent of classified files and confidential reports. It&#39;s perfect for those who appreciate a clean,
minimalist aesthetic and want to immerse themselves in content without distractions, feeling like they&#39;re poring over important case files.</p>
<h3>2. Terminal Mode</h3>
<p>For the tech enthusiasts and command-line aficionados, we present <strong>Terminal Mode</strong>. This mode re-skins blogposts with a retro,
monospaced font, glowing green text, and a classic command-line interface feel. It&#39;s an homage to the early days of computing,
offering a nostalgic and functional environment that&#39;s ideal for developers, hackers, or anyone who enjoys a vintage digital vibe while consuming content.</p>
<blockquote>
<p>Inspired by Fallout: New Vegas colors</p>
</blockquote>
<h2>Why build this?</h2>
<p>The goal is to continually innovate and provide diverse ways for our users to interact with our content.
I believe that offering distinct visual experiences like Dossier and Terminal modes enhances user engagement
and allows for a more personalized journey through Fezcodex.</p>
<p>Head over to the <a href="/settings">Settings page</a> (accessible from the Sidebar). Scroll down to the new <strong>Reading Experience</strong> section and set you mode...</p>
<h1>Oh, One More Thing. <em><strong>Sidebar Colors</strong></em></h1>
<p>Also sidebar now supports multiple background colors. Some of your favorite even.</p>
<ul>
<li><span style="background-color: rgba(250, 128, 114, 0.5)">  Salmon Light  </span></li>
<li><span style="background-color: rgba(250, 128, 114, 0.75)"> Salmon Medium </span></li>
<li><span style="background-color: rgba(100, 149, 237, 0.5)">  Blue          </span></li>
<li><span style="background-color: rgba(60, 179, 113, 0.5)">   Green         </span></li>
<li><span style="background-color: rgba(147, 112, 219, 0.5)">  Purple        </span></li>
<li><span style="background-color: rgba(0, 255, 255, 0.5)">    Cyan          </span></li>
</ul>
<p>Head over to the <a href="/settings">Settings page</a>, again. Scroll down to the new <strong>Interface &amp; Layout</strong> then under <strong>Sidebar Color</strong> section set your sidebar color.</p>
<p>Hope you enjoy exploring these new immersive reading modes. Happy reading!</p>
<p><a href="https://fezcode.com/blog/introducing-reading-experience">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Typeface vs. Font: The Music Analogy]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/typeface-vs-font</link>
            <guid isPermaLink="false">https://fezcode.com/blog/typeface-vs-font</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 17 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>The easiest way to understand the difference is to think about music:</p>
<ul>
<li><strong>The Typeface</strong> is the <strong>song</strong> itself (the melody, the lyrics, the creative idea).</li>
<li><strong>The Font</strong> is the <strong>MP3 file</strong> (the actual digital file you use to play the music).</li>
</ul>
<p>In design terms:</p>
<ul>
<li><strong>Typeface</strong>: The specific design or look of the letters (what you see).</li>
<li><strong>Font</strong>: The computer file or mechanism that contains the letters (what you use).</li>
</ul>
<h2>In Practice</h2>
<p>You choose a <strong>Typeface</strong>, but you install a <strong>Font</strong>.</p>
<h2>Examples</h2>
<h4>1. Helvetica</h4>
<ul>
<li>Typeface: &quot;Helvetica&quot; (The entire family of letters).</li>
<li>Font: <code>Helvetica-Bold.otf</code> (The specific file for the bold version).</li>
</ul>
<h4>2. Times New Roman</h4>
<ul>
<li>Typeface: &quot;Times New Roman&quot; (The creative design).</li>
<li>Font: <code>Times New Roman, Italic, 12 point</code> (The specific variation you are using on the page).</li>
</ul>
<h2>Summary</h2>
<p>If you are talking to a designer about the look, you are talking about a <strong>Typeface</strong>.
If you are talking to a developer about the file or the bold setting, you are talking about a <strong>Font</strong>.</p>
<p><a href="https://fezcode.com/blog/typeface-vs-font">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Why Your Brain Hates Lyrics While You Work: The Irrelevant Speech Effect]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/the-irrelevant-speech-effect</link>
            <guid isPermaLink="false">https://fezcode.com/blog/the-irrelevant-speech-effect</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 17 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Have you ever tried to read a book while someone next to you is having a loud conversation on the phone?
You probably found yourself reading the same sentence three times without understanding a word.</p>
<p>This isn&#39;t because you aren&#39;t focused; it is because of a psychological glitch called the <strong>Irrelevant Speech Effect (ISE)</strong>.</p>
<p>Here is the simple breakdown of why this happens and why your favorite playlist might be killing your productivity.</p>
<h2>What is the &quot;Irrelevant Speech Effect&quot;?</h2>
<p>Imagine your brain’s working memory is like a <strong>single-lane bridge</strong>. This bridge is responsible for processing language—whether that&#39;s reading an email, writing code, or studying for an exam.</p>
<p>When you are working, you are sending &quot;cars&quot; (words and thoughts) over this bridge.</p>
<ul>
<li>Silence: The cars move smoothy.</li>
<li>Instrumental Music: A little scenery on the side of the road, but the traffic flows.</li>
<li>Speech (or Lyrics): This is like a massive truck trying to force its way onto that same single-lane bridge from the opposite direction.</li>
</ul>
<p>Even if you <em>try</em> to ignore the speech, you can&#39;t. Your brain is hardwired to prioritize human voices.
It involuntarily tries to process the words it hears, causing a traffic jam on the bridge. This crash is the Irrelevant Speech Effect.
Why You Shouldn&#39;t Listen to Lyrical Music While Working</p>
<p>You might think, <strong>&quot;I&#39;m not listening to the lyrics, I&#39;m just vibing.&quot;</strong> Unfortunately, your subconscious disagrees.</p>
<p>If your task involves words (reading, writing, coding, planning), your brain uses a system called the <strong>Phonological Loop.</strong>
This is the inner voice you hear when you read silently.</p>
<p>When you play music with lyrics:</p>
<ul>
<li>Conflict: Your inner voice (reading/thinking) starts fighting with the singer&#39;s voice.</li>
<li>Processing Power: Your brain wastes energy trying to filter out the singer&#39;s words to focus on your own thoughts.</li>
<li>Result: Your IQ temporarily drops, you make more mistakes, and you get tired faster.</li>
</ul>
<h3>Real-Life Examples</h3>
<ul>
<li>The Coffee Shop Dilemma: You can work fine in a coffee shop with the hum of a machine or clinking cups (white noise). But the moment the couple at the next table starts arguing about their relationship, your focus shatters.-</li>
<li>The Open Office: You are trying to write an important email, but a colleague two desks away is explaining a recipe. Suddenly, you find yourself typing &quot;pasta&quot; into your professional report.-</li>
<li>TV in the Background: You think having the news on helps you relax while studying, but you realize you’ve been staring at the same page for 20 minutes because your brain is tracking the reporter&#39;s voice.</li>
</ul>
<h3>The Solution?</h3>
<p>If you are doing manual labor (like washing <strong>dishes</strong>), lyrical music is great! <strong>It keeps you energized.</strong></p>
<p>But for deep mental work:</p>
<ul>
<li>Stick to Lo-Fi beats, Classical, or Video Game Soundtracks.</li>
<li>These genres have no words, so they occupy the &quot;emotional&quot; part of your brain without crashing into the &quot;language&quot; bridge.</li>
</ul>
<p><a href="https://fezcode.com/blog/the-irrelevant-speech-effect">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Hooks Showdown: useMemo vs useCallback vs useState vs useEffect]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-hooks-comparison</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-hooks-comparison</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Mon, 15 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h2>1. useState: The Memory</h2>
<p><strong>What it does:</strong> Allows a functional component to &quot;remember&quot; information between renders.</p>
<p><strong>When to use:</strong> Whenever you have data that changes over time and needs to trigger a re-render to update the UI (e.g., form inputs, toggle states, counters).</p>
<pre><code class="language-javascript">const [count, setCount] = useState(0);

// Update state
setCount(count + 1);
</code></pre>
<h2>2. useEffect: The Side Effect</h2>
<p><strong>What it does:</strong> Performs side effects in functional components. &quot;Side effects&quot; are things like data fetching, subscriptions, or manually changing the DOM.</p>
<p><strong>When to use:</strong> When you need to do something <em>after</em> the component renders or when a specific value changes.</p>
<pre><code class="language-javascript">useEffect(() =&gt; {
  // This runs after every render
  document.title = `You clicked ${count} times`;

  // Optional cleanup mechanism
  return () =&gt; {
    // Clean up code here
  };
}, [count]); // Only re-run if &#39;count&#39; changes
</code></pre>
<h2>3. useMemo: The Calculator</h2>
<p><strong>What it does:</strong> Memoizes (caches) the <em>result</em> of a calculation. It only re-calculates the value when one of its dependencies changes.</p>
<p><strong>When to use:</strong> Optimization. Use it to avoid expensive calculations on every render.</p>
<pre><code class="language-javascript">const expensiveValue = useMemo(() =&gt; {
  return computeExpensiveValue(a, b);
}, [a, b]); // Only re-compute if &#39;a&#39; or &#39;b&#39; changes
</code></pre>
<p><em>Note: Don&#39;t overuse this. Memoization has its own cost.</em></p>
<h2>4. useCallback: The Function Saver</h2>
<p><strong>What it does:</strong> Memoizes a <em>function definition</em>. It returns the same function instance between renders unless its dependencies change.</p>
<p><strong>When to use:</strong> Optimization. Primarily useful when passing callbacks to optimized child components (like those wrapped in <code>React.memo</code>) to prevent unnecessary re-renders of the child.</p>
<pre><code class="language-javascript">const handleClick = useCallback(() =&gt; {
  doSomething(a, b);
}, [a, b]); // Function identity remains stable unless &#39;a&#39; or &#39;b&#39; changes
</code></pre>
<h2>Summary Table</h2>
<table>
<thead>
<tr>
<th align="left">Hook</th>
<th align="left">Returns</th>
<th align="left">Purpose</th>
<th align="left">Re-runs when...</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>useState</strong></td>
<td align="left"><code>[state, setter]</code></td>
<td align="left">Manage state</td>
<td align="left">Setter is called</td>
</tr>
<tr>
<td align="left"><strong>useEffect</strong></td>
<td align="left"><code>undefined</code></td>
<td align="left">Side effects</td>
<td align="left">Dependencies change</td>
</tr>
<tr>
<td align="left"><strong>useMemo</strong></td>
<td align="left">Calculated Value</td>
<td align="left">Cache expensive calculation</td>
<td align="left">Dependencies change</td>
</tr>
<tr>
<td align="left"><strong>useCallback</strong></td>
<td align="left">Memoized Function</td>
<td align="left">Stable function identity</td>
<td align="left">Dependencies change</td>
</tr>
</tbody></table>
<h2>Key Difference: useMemo vs useCallback</h2>
<ul>
<li><code>useMemo</code> caches the <strong>result</strong> of a function call.</li>
<li><code>useCallback</code> caches the <strong>function itself</strong>.</li>
</ul>
<blockquote>
<p><code>useCallback(fn, deps)</code> is equivalent to <code>useMemo(() =&gt; fn, deps)</code>.</p>
</blockquote>
<p><a href="https://fezcode.com/blog/react-hooks-comparison">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Magic: Rendering Components from Markdown Links]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-magic-markdown-components</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-magic-markdown-components</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 12 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Static text is boring. In a modern React application, your content should be alive.</p>
<p>Today I want to share a fun pattern I implemented in <strong>Fezcodex</strong>: triggering dynamic UI interactions directly from standard Markdown links. Specifically, clicking a link in a blog post to open a side panel with a live React component, rather than navigating to a new page.</p>
<h2>The Idea</h2>
<p>I wanted to explain technical terms like <strong><a href="/vocab/prop-drilling">Prop Drilling</a></strong> without forcing the reader to leave the article. A tooltip is too small; a new tab is too distracting. The solution? My global <strong>Side Panel</strong>.</p>
<p>But how do you tell a static Markdown file to &quot;render a React component in the side panel&quot;?</p>
<h2>The Solution</h2>
<p>The secret sauce lies in <code>react-markdown</code>&#39;s ability to customize how HTML elements are rendered. We can intercept every <code>&lt;a&gt;</code> tag and check if it&#39;s a &quot;special&quot; link.</p>
<h3>1. The Interceptor (<code>MarkdownLink</code>)</h3>
<p>I created a custom component that replaces standard HTML anchors. It checks the <code>href</code> for a specific pattern (in my case, <code>/vocab/</code>).</p>
<pre><code class="language-javascript">const MarkdownLink = ({ href, children }) =&gt; {
  const { openSidePanel } = useSidePanel();

  // Check if this is a &quot;vocabulary&quot; link
  const isVocab = href &amp;&amp; href.includes(&#39;/vocab/&#39;);

  if (isVocab) {
    // 1. Extract the term ID (e.g., &quot;prop-drilling&quot;)
    const term = href.split(&#39;/vocab/&#39;)[1];

    // 2. Look up the definition/component
    const definition = vocabulary[term];

    return (
      &lt;a
        href={href}
        onClick={(e) =&gt; {
          e.preventDefault(); // Stop navigation!
          if (definition) {
            // 3. Trigger the global UI
            openSidePanel(definition.title, definition.content);
          }
        }}
        className=&quot;text-pink-400 dashed-underline cursor-help&quot;
      &gt;
        {children}
      &lt;/a&gt;
    );
  }

  // Fallback for normal links
  return &lt;a href={href}&gt;{children}&lt;/a&gt;;
};
</code></pre>
<h3>2. The Data (<code>vocabulary.js</code>)</h3>
<p>I store the actual content in a simple lookup object. The beauty is that <code>content</code> can be <em>anything</em>--text, images, or fully interactive React components.</p>
<pre><code class="language-javascript">export const vocabulary = {
  &#39;prop-drilling&#39;: {
    title: &#39;Prop Drilling&#39;,
    content: &lt;PropDrillingDiagram /&gt; // A real component!
  },
  // ...
};
</code></pre>
<h3>3. Handling &quot;Deep Links&quot;</h3>
<p>What if someone actually copies the URL <code>https://fezcodex.com/vocab/prop-drilling</code> and sends it to a friend? The <code>onClick</code> handler won&#39;t fire because they aren&#39;t clicking a link—they are loading the app.</p>
<p>To handle this, I added a &quot;phantom&quot; route in my Router:</p>
<pre><code class="language-javascript">// VocabRouteHandler.js
const VocabRouteHandler = () =&gt; {
  const { term } = useParams();
  const navigate = useNavigate();
  const { openSidePanel } = useSidePanel();

  useEffect(() =&gt; {
    // 1. Open the panel immediately
    if (vocabulary[term]) {
      openSidePanel(vocabulary[term].title, vocabulary[term].content);
    }
    // 2. Redirect to home (so the background isn&#39;t blank)
    navigate(&#39;/&#39;, { replace: true });
  }, [term]);

  return null;
};
</code></pre>
<h2>Why this rocks</h2>
<p>This pattern effectively turns your static Markdown content into a control surface for your application. You can write:</p>
<blockquote>
<p>&quot;Check out this <code>[interactive demo](/demos/sorting-algo)</code>...&quot;</p>
</blockquote>
<p>And have it launch a full-screen visualization, a game, or a configuration wizard, all without leaving the flow of your writing. It bridges the gap between &quot;content&quot; and &quot;app&quot;.</p>
<p><a href="https://fezcode.com/blog/react-magic-markdown-components">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Implementing a Resizable Global Sliding Side Panel in React]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/implementing-a-sliding-side-panel</link>
            <guid isPermaLink="false">https://fezcode.com/blog/implementing-a-sliding-side-panel</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Thu, 11 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Sometimes, a modal is just too intrusive. You want to show detailed context—like a complex rating system or metadata—without forcing the user to lose their place on the page or blocking the entire UI with a backdrop that demands immediate attention. Enter the <strong>Sliding Side Panel</strong>.</p>
<p>In this post, I&#39;ll walk through how I implemented a global side panel system for <strong>Fezcodex</strong>, allowing any component in the app to trigger a content-rich overlay that slides in smoothly from the right. Even better? I made it <strong>resizable</strong>, so users can drag to expand the view if they need more space.</p>
<h2>The Goal</h2>
<p>The immediate need was simple: I wanted to explain my G4-inspired 5-star rating system on the Logs page. A simple tooltip wasn&#39;t enough, and a full modal felt heavy-handed. I wanted a panel that felt like an extension of the UI, sliding in to offer &quot;more details&quot; on demand.</p>
<h2>The Architecture</h2>
<p>To make this truly reusable, I avoided prop drilling by using the <strong>Context API</strong>.</p>
<h3>Why Context? Avoiding Prop Drilling</h3>
<p>Without a global context, implementing a feature like this would require <strong><a href="/vocab/prop-drilling">prop drilling</a></strong>. This is a common pattern (or <strong><a href="/vocab/anti-pattern">anti-pattern</a></strong>) in React where you pass data or functions down through multiple layers of components just to get them to where they are needed.</p>
<p>Imagine we managed the side panel state in <code>App.js</code>. We would have to pass the <code>openSidePanel</code> function like this:</p>
<p><code>App</code> → <code>Layout</code> → <code>MainContent</code> → <code>LogsPage</code> → <code>LogCard</code> → <code>InfoButton</code></p>
<p>Every intermediate component would need to accept and pass along a prop it doesn&#39;t even use. This makes refactoring a nightmare and clutters your component signatures. By using the <strong>Context API</strong>, we can bypass the middle layers entirely. Any component, no matter how deep in the tree, can simply reach out and grab the <code>openSidePanel</code> function directly.</p>
<h3>1. The Context (<code>SidePanelContext</code>)</h3>
<p>We need a way to tell the app: &quot;Open the panel with <em>this</em> title, <em>this</em> content, and start at <em>this</em> width.&quot;</p>
<pre><code class="language-javascript">// src/context/SidePanelContext.js
import React, { createContext, useContext, useState } from &#39;react&#39;;

const SidePanelContext = createContext();

export const useSidePanel = () =&gt; useContext(SidePanelContext);

export const SidePanelProvider = ({ children }) =&gt; {
  const [isOpen, setIsOpen] = useState(false);
  const [panelContent, setPanelContent] = useState(null);
  const [panelTitle, setPanelTitle] = useState(&#39;&#39;);
  const [panelWidth, setPanelWidth] = useState(450); // Default width

  // openSidePanel now accepts an optional initial width
  const openSidePanel = (title, content, width = 450) =&gt; {
    setPanelTitle(title);
    setPanelContent(content);
    setPanelWidth(width);
    setIsOpen(true);
  };

  const closeSidePanel = () =&gt; setIsOpen(false);

  return (
    &lt;SidePanelContext.Provider
      value={{
        isOpen,
        panelTitle,
        panelContent,
        panelWidth,
        setPanelWidth,
        openSidePanel,
        closeSidePanel
      }}
    &gt;
      {children}
    &lt;/SidePanelContext.Provider&gt;
  );
};
</code></pre>
<p>This allows any component to call <code>openSidePanel(&#39;My Title&#39;, &lt;MyComponent /&gt;, 600)</code> to trigger the UI with a custom starting width.</p>
<h3>2. The Component (<code>SidePanel</code>)</h3>
<p>The visual component uses <strong>Framer Motion</strong> for silky smooth entrance and exit animations, and vanilla JS event listeners for the resize logic.</p>
<pre><code class="language-javascript">// src/components/SidePanel.js
import { motion, AnimatePresence } from &#39;framer-motion&#39;;
import { useState, useEffect } from &#39;react&#39;;
import { useSidePanel } from &#39;../context/SidePanelContext&#39;;

const SidePanel = () =&gt; {
  const { isOpen, closeSidePanel, panelTitle, panelContent, panelWidth, setPanelWidth } = useSidePanel();
  const [isResizing, setIsResizing] = useState(false);

  // Resize Logic
  useEffect(() =&gt; {
    const handleMouseMove = (e) =&gt; {
      if (!isResizing) return;
      const newWidth = window.innerWidth - e.clientX;
      // Constrain width: min 300px, max 90% of screen
      if (newWidth &gt; 300 &amp;&amp; newWidth &lt; window.innerWidth * 0.9) {
        setPanelWidth(newWidth);
      }
    };

    const handleMouseUp = () =&gt; setIsResizing(false);

    if (isResizing) {
      window.addEventListener(&#39;mousemove&#39;, handleMouseMove);
      window.addEventListener(&#39;mouseup&#39;, handleMouseUp);
      document.body.style.cursor = &#39;ew-resize&#39;;
      document.body.style.userSelect = &#39;none&#39;; // Prevent text selection while dragging
    }

    return () =&gt; {
      window.removeEventListener(&#39;mousemove&#39;, handleMouseMove);
      window.removeEventListener(&#39;mouseup&#39;, handleMouseUp);
      document.body.style.cursor = &#39;default&#39;;
      document.body.style.userSelect = &#39;auto&#39;;
    };
  }, [isResizing, setPanelWidth]);

  return (
    &lt;AnimatePresence&gt;
      {isOpen &amp;&amp; (
        &lt;&gt;
          &lt;motion.div onClick={closeSidePanel} className=&quot;fixed inset-0 bg-black/50 z-[60]&quot; /&gt;

          &lt;motion.div
            initial={{ x: &#39;100%&#39; }}
            animate={{ x: 0 }}
            exit={{ x: &#39;100%&#39; }}
            transition={{ type: &#39;spring&#39;, damping: 25, stiffness: 200 }}
            style={{ width: panelWidth }} // Dynamic width
            className=&quot;fixed top-0 right-0 h-full bg-gray-900 border-l border-gray-700 z-[70] flex flex-col&quot;
          &gt;
            {/* Resize Handle */}
            &lt;div
              onMouseDown={(e) =&gt; { setIsResizing(true); e.preventDefault(); }}
              className=&quot;absolute left-0 top-0 bottom-0 w-1.5 cursor-ew-resize hover:bg-primary-500/50 transition-colors z-50&quot;
            /&gt;

             {/* Header &amp; Content */}
          &lt;/motion.div&gt;
        &lt;/&gt;
      )}
    &lt;/AnimatePresence&gt;
  );
};
</code></pre>
<h3>3. Integration</h3>
<p>I wrapped the entire application in the <code>SidePanelProvider</code> in <code>App.js</code> and placed the <code>&lt;SidePanel /&gt;</code> component in <code>Layout.js</code>. This ensures the panel is always available and renders on top of everything else.</p>
<h2>Inspiration</h2>
<p>The first use case for this panel was to detail the <strong>Rating System</strong> for my logs. I wanted to pay homage to the classic <strong>X-Play (G4TV)</strong> scale, emphasizing that a <strong>3 out of 5</strong> is a solid, good score—not a failure.</p>
<p>The side panel proved perfect for this: users can check the rating criteria without leaving the logs list, keeping their browsing flow uninterrupted.</p>
<h2>Conclusion</h2>
<p>Global UI elements controlled via Context are a powerful pattern in React. By adding a simple resize handle and managing width in the global state, we&#39;ve transformed a static overlay into a flexible, user-friendly tool that adapts to the user&#39;s needs.</p>
<p><a href="https://fezcode.com/blog/implementing-a-sliding-side-panel">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Building a Digital Rotary Phone]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/building-a-digital-rotary-phone</link>
            <guid isPermaLink="false">https://fezcode.com/blog/building-a-digital-rotary-phone</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 02 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>In a world of touchscreens and haptic feedback, there&#39;s something deeply satisfying about the mechanical click-whirrr of a rotary phone.
I recently built a digital version of this retro interface for Fezcodex, and I want to take you through the engineering journey—from the
trigonometry of the dial to the state management of the call logic.</p>
<h2>The Challenge</h2>
<p>Building a rotary phone for the web isn&#39;t just about displaying an image. It&#39;s about capturing the <em>feel</em> of the interaction. You need to:</p>
<ol>
<li>Draw a dial with holes.</li>
<li>Detect user input (mouse or touch).</li>
<li>Calculate the rotation based on the pointer&#39;s position.</li>
<li>&quot;Drag&quot; the dial realistically.</li>
<li>Snap back when released.</li>
<li>Register the dialed number only if the user drags far enough.</li>
</ol>
<h2>Anatomy of the Dial</h2>
<p>I broke the <code>RotaryDial</code> component into a few key layers, stacked using CSS absolute positioning:</p>
<ol>
<li><strong>The Backplate</strong>: This is static. It sits at the bottom and holds the numbers (1, 2, 3... 0) in their correct positions.</li>
<li><strong>The Rotating Disk</strong>: This sits on top of the backplate. It rotates based on user interaction. It contains the &quot;holes&quot;.</li>
<li><strong>The Finger Stop</strong>: A static hook at the bottom right (approx 4 o&#39;clock position) that physically stops the dial on a real phone.</li>
</ol>
<h3>The Trigonometry of Angles</h3>
<p>The core of this component is converting a mouse position (x, y) into an angle (theta).</p>
<pre><code class="language-javascript">const getAngle = (event, center) =&gt; {
  const clientX = event.touches ? event.touches[0].clientX : event.clientX;
  const clientY = event.touches ? event.touches[0].clientY : event.clientY;

  const dx = clientX - center.x;
  const dy = clientY - center.y;
  // atan2 returns angle in radians, convert to degrees
  let theta = Math.atan2(dy, dx) * (180 / Math.PI);
  return theta;
};
</code></pre>
<p><code>Math.atan2(dy, dx)</code> is perfect here because it handles all quadrants correctly, returning values from -PI to +PI (-180 to +180 degrees).</p>
<h3>Why <code>Math.atan2</code>?</h3>
<p>You might remember SOH CAH TOA from school. To find an angle given x and y, we typically use the tangent function: <code>tan(θ) = y / x</code>, so <code>θ = atan(y / x)</code>.</p>
<p>However, <code>Math.atan()</code> has a fatal flaw for UI interaction: it can&#39;t distinguish between quadrants.</p>
<ul>
<li>Quadrant 1: x=1, y=1 -&gt; <code>atan(1/1)</code> = 45°</li>
<li>Quadrant 3: x=-1, y=-1 -&gt; <code>atan(-1/-1)</code> = <code>atan(1)</code> = 45°</li>
</ul>
<p>If we used <code>atan</code>, dragging in the bottom-left would behave exactly like dragging in the top-right!</p>
<p><code>Math.atan2(y, x)</code> solves this by taking both coordinates separately. It checks the signs of x and y to place the angle in the correct full-circle context (-π to +π radians).</p>
<p>We then convert this radian value to degrees:
<code>Degrees = Radians * (180 / π)</code></p>
<p>This gives us a continuous value we can use to map the mouse position directly to the dial&#39;s rotation.</p>
<h2>The Drag Logic</h2>
<p>When a user clicks a specific number&#39;s hole, we don&#39;t just start rotating from 0. We need to know <em>which</em> hole they grabbed.</p>
<p>Each digit has a &quot;Resting Angle&quot;. If the Finger Stop is at 60 degrees, and the holes are spaced 30 degrees apart:</p>
<ul>
<li>Digit 1 is at <code>60 - 30 = 30</code> degrees.</li>
<li>Digit 2 is at <code>60 - 60 = 0</code> degrees.</li>
<li>...and so on.</li>
</ul>
<p>When the user starts dragging, we track the mouse&#39;s current angle relative to the center of the dial. The rotation of the dial is then calculated as:</p>
<p><code>Rotation = CurrentMouseAngle - InitialHoleAngle</code></p>
<h3>Handling the &quot;Wrap Around&quot;</h3>
<p>One of the trickiest parts was handling the boundary where angles jump from 180 to -180. For numbers like 9 and 0, the rotation requires dragging past this boundary.</p>
<p>If you just subtract the angles, you might get a jump like <code>179 -&gt; -179</code>, which looks like a massive reverse rotation. I solved this with a normalization function:</p>
<pre><code class="language-javascript">const normalizeDiff = (diff) =&gt; {
  while (diff &lt;= -180) diff += 360;
  while (diff &gt; 180) diff -= 360;
  return diff;
};
</code></pre>
<p>However, simply normalizing isn&#39;t enough for the long throws (like dragging &#39;0&#39; all the way around). A normalized angle might look like <code>-60</code> degrees, but we actually mean <code>300</code> degrees of positive rotation.</p>
<p>I added logic to detect this &quot;underflow&quot;:</p>
<pre><code class="language-javascript">// If rotation is negative but adding 360 keeps it within valid range
if (newRotation &lt; 0 &amp;&amp; (newRotation + 360) &lt;= maxRot + 30) {
  newRotation += 360;
}
</code></pre>
<p>This ensures that dragging &#39;0&#39; feels continuous, even as it passes the 6 o&#39;clock mark.</p>
<h2>State Management vs. Animation</h2>
<p>Initially, I used standard React state (<code>useState</code>) for the rotation. This worked, but <code>setState</code> is asynchronous and can feel slightly laggy for high-frequency drag events (60fps).</p>
<p>I switched to <strong>Framer Motion&#39;s <code>useMotionValue</code></strong>. This allows us to update the rotation value directly without triggering a full React re-render on every pixel of movement. It&#39;s buttery smooth.</p>
<pre><code class="language-javascript">const rotation = useMotionValue(0);
// ...
rotation.set(newRotation);
</code></pre>
<p>When the user releases the dial (<code>handleEnd</code>), we need it to spring back to zero. Framer Motion makes this trivial:</p>
<pre><code class="language-javascript">animate(rotation, 0, {
  type: &quot;spring&quot;,
  stiffness: 200,
  damping: 20
});
</code></pre>
<h2>The &quot;Call&quot; Logic</h2>
<p>The drag logic only handles the visual rotation. To actually dial a number, we check the final rotation when the user releases the mouse.</p>
<p>If <code>abs(CurrentRotation - MaxRotation) &lt; Threshold</code>, we count it as a successful dial.</p>
<p>I connected this to a higher-level <code>RotaryPhonePage</code> component that maintains the string of dialed numbers.</p>
<h3>Easter Eggs</h3>
<p>Of course, no app is complete without secrets. I hooked up a <code>handleCall</code> function that checks specific number patterns:</p>
<ul>
<li><strong>911</strong>: Triggers a red &quot;Connected&quot; state and unlocks &quot;The Emergency&quot; achievement.</li>
<li><strong>0</strong>: Connects to the Operator.</li>
<li><strong>Others</strong>: Just simulates a call.</li>
</ul>
<h2>Visuals</h2>
<p>The dial uses Tailwind CSS for styling. The numbers and holes are positioned using <code>transform: rotate(...) translate(...)</code>.</p>
<ul>
<li><code>rotate(angle)</code> points the element in the right direction.</li>
<li><code>translate(radius)</code> pushes it out from the center.</li>
<li><code>rotate(-angle)</code> (on the inner text) keeps the numbers upright!</li>
</ul>
<p>The result is a responsive, interactive, and nostalgic component that was a joy to build. Give it a spin in the <strong>Apps</strong> section!</p>
<p><a href="https://fezcode.com/blog/building-a-digital-rotary-phone">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Nocturnote: A Sleek and Modern Text Editor]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/nocturnote</link>
            <guid isPermaLink="false">https://fezcode.com/blog/nocturnote</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Mon, 01 Dec 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h2>Nocturnote: The Text Editor I Always Wanted</h2>
<p>Have you ever felt like your text editor is either doing too much or too little? That&#39;s exactly how I felt before I started building <strong>Nocturnote</strong>.</p>
<p><img src="/images/projects/nocturnote.png" alt="Nocturnote Notepad Mode"></p>
<blockquote>
<p>Notepad Mode in Nocturnote</p>
</blockquote>
<p><strong>Nocturnote</strong> is my take on a modern, distraction-free writing environment. It&#39;s a sleek, cross-platform desktop application designed for those who want to just <em>write</em>, but with the comfort of modern tools.</p>
<h2>Why Nocturnote?</h2>
<p>I wanted something that looked good, felt fast, and offered just the right amount of customization without being overwhelming.</p>
<h3>Key Features</h3>
<ul>
<li><strong>Distraction-Free Interface</strong>: Clean lines, subtle colors, and a focus on your text.</li>
<li><strong>Rain Mode</strong>: This is one of my favorites. Toggle it on for a soothing visual effect that adds a cozy atmosphere to your writing sessions.</li>
<li><strong>Notepad Mode</strong>: Sometimes you just want that classic, stripped-back aesthetic. Nocturnote has you covered.</li>
<li><strong>Full Customization</strong>: Change fonts, sizes, line heights, and more. Make it yours.</li>
</ul>
<h2>Under the Hood</h2>
<p>For the tech-savvy, Nocturnote is built using a robust modern stack:</p>
<ul>
<li><strong>Electron</strong>: Ensuring it runs smoothly on Windows, macOS, and Linux.</li>
<li><strong>Svelte 5</strong>: For a blazing fast and reactive user interface.</li>
<li><strong>TypeScript</strong>: Because type safety is non-negotiable.</li>
<li><strong>Tailwind CSS</strong>: For rapid and beautiful styling.</li>
<li><strong>Electron-Vite</strong>: For a lightning-fast development experience.</li>
</ul>
<h2>Get It</h2>
<p>Nocturnote is open source! You can check out the code, contribute, or download it from the repository.</p>
<p><a href="https://github.com/fezcode/nocturnote">Check out Nocturnote on GitHub</a></p>
<p>Whether you&#39;re coding, journaling, or taking quick notes, I hope Nocturnote provides the calm, productive space you need.</p>
<p><a href="https://fezcode.com/blog/nocturnote">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[The Art of Recursive Botany: How Fractal Flora Works]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/how-fractal-flora-works</link>
            <guid isPermaLink="false">https://fezcode.com/blog/how-fractal-flora-works</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 28 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Have you ever wondered how nature creates such intricate and beautiful patterns,
from the branching of trees to the delicate veins of a leaf? Much of this complexity can be explained by surprisingly simple rules,
often involving <strong>fractals</strong> and <strong>recursion</strong>.
Our new &quot;Fractal Flora&quot; app lets you explore these principles by growing your own digital trees with a few sliders.</p>
<blockquote>
<p><a href="/apps::flora">Try apps::flora here</a></p>
</blockquote>
<p>This post will peel back the layers and explain the core mechanics behind the app.</p>
<p><img src="/images/projects/fractal-flora.png" alt="/images/projects/fractal-flora.png"></p>
<h2>What is a Fractal Tree?</h2>
<p>At its heart, a fractal tree is a structure where a basic branching pattern repeats itself at smaller scales. Each branch can be thought of as a miniature version of the entire tree. This self-similarity is a hallmark of fractals.</p>
<p>In programming, this concept is perfectly suited for <strong>recursion</strong>, where a function calls itself to solve smaller instances of the same problem.</p>
<h2>The Recursive Algorithm: <code>drawBranch</code></h2>
<p>The entire tree is generated by a single, powerful recursive function, let&#39;s call it <code>branch()</code>. It takes a starting point, a length, an angle, and its current depth in the tree.</p>
<p>Here&#39;s a simplified look at its logic:</p>
<ol>
<li><strong>Draw the Current Branch:</strong> It draws a line segment from its starting point, at its given length and angle.</li>
<li><strong>Base Case (Stop Condition):</strong> If the <code>depth</code> (how many times it has branched) reaches zero, it stops. This prevents infinite recursion.</li>
<li><strong>Branch Out:</strong> Otherwise, it calculates the endpoint of the current branch. From this endpoint, it calls itself <em>twice</em> (or more), creating two new &quot;sub-branches.&quot; Each sub-branch is drawn with a shorter length, a new angle (offset from the parent branch), and a reduced depth.</li>
</ol>
<pre><code class="language-javascript">const branch = (x, y, len, ang, d) =&gt; {
  // 1. Calculate end point of current branch
  const endX = x + len * Math.cos((ang * Math.PI) / 180);
  const endY = y + len * Math.sin((ang * Math.PI) / 180);

  // 2. Draw the branch (context.drawLine(x,y,endX,endY))

  // 3. If not at max depth, recurse
  if (d &gt; 0) {
    const nextLen = len * lengthMultiplier; // e.g., 0.7
    // Right branch
    branch(endX, endY, nextLen, ang + branchAngle, d - 1);
    // Left branch
    branch(endX, endY, nextLen, ang - branchAngle, d - 1);
  }
};

// Initial call (e.g., from bottom center of canvas)
// branch(canvas.width / 2, canvas.height, initialLength, -90, maxDepth);
</code></pre>
<p><em>(Note: The actual implementation in <code>FractalFloraPage.js</code> is slightly more complex, handling canvas transformations, line widths, and randomized elements.)</em></p>
<h2>The &quot;DNA&quot; of Your Digital Tree</h2>
<p>The beauty of Fractal Flora lies in how these simple parameters (the tree&#39;s &quot;DNA&quot;) dramatically change its appearance:</p>
<ul>
<li><strong>Recursion Depth (<code>depth</code>):</strong> This controls how many times the <code>branch()</code> function calls itself. A higher depth creates a denser, more complex tree, but also requires more computation.</li>
<li><strong>Branch Angle (<code>angle</code>):</strong> This is the angle at which new branches diverge from the parent branch. Small angles create tall, narrow trees, while larger angles create wider, more sprawling structures.</li>
<li><strong>Length Multiplier (<code>lengthMultiplier</code>):</strong> This determines how much shorter each successive branch becomes. A value of <code>0.7</code> means a new branch is 70% the length of its parent.</li>
<li><strong>Trunk Base Size (<code>lengthBase</code>):</strong> The initial length of the very first (main) trunk segment.</li>
<li><strong>Wind / Asymmetry (<code>asymmetry</code>):</strong> This parameter adds a bias to the branching angle, making one side of the tree grow more dominantly, simulating the effect of wind or environmental factors.</li>
<li><strong>Organic Randomness (<code>randomness</code>):</strong> This introduces slight, random variations to the length and angle of each branch, breaking the perfect symmetry of mathematical fractals and making the tree appear more organic and natural.</li>
</ul>
<h3>Seasons and Color Palettes</h3>
<p>The app also cycles through different &quot;seasons.&quot; These aren&#39;t complex simulations, but rather pre-defined color palettes for the trunk, branches, and leaves, instantly changing the mood and appearance of your flora.</p>
<h2>From Math to Art</h2>
<p>What&#39;s fascinating is how a few lines of code, driven by recursive mathematical principles, can generate forms that closely mimic those found in nature. Fractals are not just abstract mathematical concepts; they are the language of growth, efficiency, and beauty in the natural world.</p>
<p>Now that you understand the &quot;how,&quot; dive back into the Fractal Flora app and become a digital botanist, experimenting with its DNA to create your own unique, algorithmic arboretum!</p>
<p><a href="https://fezcode.com/blog/how-fractal-flora-works">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Unlocking Your Journey: Introducing the Fezcodex Achievement System!]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/the-fezcodex-achievement-system</link>
            <guid isPermaLink="false">https://fezcode.com/blog/the-fezcodex-achievement-system</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 28 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Here at Fezcodex, we believe exploration should be rewarded. That&#39;s why we&#39;re thrilled to unveil the brand-new <strong>Achievement System</strong> – a fun and engaging way to discover all the hidden corners and cool features of the site!</p>
<h2>What is it?</h2>
<p>The Achievement System gamifies your experience on Fezcodex. As you navigate, interact with our apps, explore visual modes, or simply read through our content, you&#39;ll be secretly unlocking various badges and trophies. Think of it as a personalized quest log for your journey through the digital world of Fezcodex!</p>
<h2>Why Achievements?</h2>
<p>We wanted to make exploring the site more interactive and rewarding. With achievements, you can:</p>
<ul>
<li><strong>Discover Hidden Gems:</strong> Uncover features you might not have found otherwise.</li>
<li><strong>Track Your Progress:</strong> See how much of Fezcodex you&#39;ve truly experienced.</li>
<li><strong>Engage More:</strong> Turn casual browsing into a rewarding adventure.</li>
</ul>
<h2>How it Works</h2>
<p>The system operates quietly in the background, tracking specific actions:</p>
<ol>
<li><strong>Triggers:</strong> Certain interactions, like opening the Command Palette, enabling a unique visual mode, or visiting a specific page, act as triggers.</li>
<li><strong>Local Storage:</strong> Your progress is saved securely and anonymously in your browser&#39;s local storage. No data is sent to any server – your achievements are yours alone!</li>
<li><strong>Toast Notifications:</strong> When you unlock a new achievement, a subtle (but celebratory!) toast notification will appear to let you know.</li>
</ol>
<h2>A Glimpse at Some Achievements</h2>
<p>Here are just a few examples of the achievements you can strive for:</p>
<ul>
<li><strong>Hello World:</strong> The very first step on your Fezcodex journey.</li>
<li><strong>The Hacker:</strong> For those who master the Command Palette.</li>
<li><strong>Curious Soul:</strong> For taking the time to learn more about the creator.</li>
<li><strong>The Architect:</strong> For appreciating the structural beauty of certain visual modes.</li>
<li><strong>Retro Futurist:</strong> For embracing the aesthetics of a bygone era.</li>
<li><strong>Novice Reader, Avid Reader, Bookworm:</strong> For delving into our blog posts and expanding your knowledge!</li>
</ul>
<h2>Visit the Trophy Room!</h2>
<p>Want to see your collection? Head over to the new <a href="/achievements">Trophy Room page</a> (accessible via the sidebar) to view all the achievements you&#39;ve unlocked and see what challenges still await!</p>
<p>We hope this new feature adds an extra layer of fun and discovery to your Fezcodex experience. Happy hunting!</p>
<p><a href="https://fezcode.com/blog/the-fezcodex-achievement-system">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Unlocking the Multiverse: New Visual Modes in Fezcodex]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/visual-modes-easter-eggs</link>
            <guid isPermaLink="false">https://fezcode.com/blog/visual-modes-easter-eggs</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Thu, 27 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Websites should be <strong>fun</strong>. While optimizing performance and fixing bugs is satisfying, sometimes you just want to flip the table -or in this case-, the entire viewport.</p>
<p>Today, I&#39;m excited to introduce a suite of new <strong>Visual Modes</strong> to Fezcodex. These are persistent, purely aesthetic toggles that let you experience the site in a completely different light (or lack thereof).</p>
<h2>The New Modes</h2>
<h3>1. Invert Colors (The Upside Down)</h3>
<p>Ever wondered what the site looks like in negative? This mode inverts all colors but cleverly rotates the hue by 180 degrees. This prevents photos from looking like scary X-rays and instead creates a cool, alternative color palette.</p>
<h3>2. Retro Mode (Cyberpunk 2077)</h3>
<p>Feeling nostalgic? Enable Retro Mode to overlay a CRT scanline effect and chromatic aberration (that red/blue text split). It gives the entire UI a gritty, 80s sci-fi terminal vibe.</p>
<h3>3. Party Mode (RGB Everywhere)</h3>
<p>Boots and cats and boots and cats. This mode continuously cycles the screen&#39;s hue through the entire rainbow. Warning: It&#39;s colorful. Very colorful.</p>
<h3>4. Mirror Mode (Through the Looking Glass)</h3>
<p>For those who want a challenge. This flips the entire website horizontally. Text is backwards, layouts are reversed, and your mouse muscle memory will be thoroughly confused. Good luck navigating!</p>
<h3>5. Noir Mode (Dramatic Effect)</h3>
<p>It was a dark and stormy night... This mode applies a high-contrast grayscale filter, turning the site into a scene from a classic detective film.</p>
<h3>6. Terminal Mode (The Hacker)</h3>
<p>Jack in. This mode transforms the entire UI into a monochrome green CRT monitor aesthetic. Perfect for feeling like you&#39;re browsing the web from a bunker in 1999.</p>
<h3>7. Blueprint Mode (The Architect)</h3>
<p>For those who appreciate structure. This applies a deep blue, inverted schematic look, making the site resemble an architectural blueprint.</p>
<h3>8. Sepia Mode (The Time Traveler)</h3>
<p>Dust off the archives. This gives everything a warm, aged parchment tone, perfect for reading through the D&amp;D logs or imagining the site as an ancient manuscript.</p>
<h2>How to Access Them</h2>
<p>You can unlock these modes in two ways:</p>
<h3>1. The Command Palette (For the Power User)</h3>
<p>Press <code>Alt + K</code> (or click the &quot;Commands&quot; button in the sidebar) to open the Command Palette. Then, simply type:</p>
<ul>
<li><code>Toggle Invert Colors</code></li>
<li><code>Toggle Retro Mode</code></li>
<li><code>Party Mode</code></li>
<li><code>Toggle Mirror Mode</code></li>
<li><code>Toggle Noir Mode</code></li>
<li><code>Toggle Terminal Mode</code></li>
<li><code>Toggle Blueprint Mode</code></li>
<li><code>Toggle Sepia Mode</code></li>
<li>...or try <code>Do a Barrel Roll</code> for a quick spin!</li>
</ul>
<h3>2. The Settings Page (For the Clicker)</h3>
<p>Head over to the <a href="/settings">Settings page</a> (accessible from the Sidebar). Scroll down to the new <strong>Visual Effects</strong> section, where you&#39;ll find toggle switches for all persistent modes.</p>
<h2>Under the Hood</h2>
<p>Implementing these was a fun exercise in CSS filters and React context.</p>
<ul>
<li><strong>Persistence:</strong> We use a custom <code>usePersistentState</code> hook (wrapper around <code>localStorage</code>) to remember your choices, so your Retro Mode stays on even after you refresh.</li>
<li><strong>CSS Magic:</strong> Most effects use <code>backdrop-filter</code> on a fixed pseudo-element (<code>body::after</code>). This was crucial to ensure that <code>position: fixed</code> elements (like the Sidebar) didn&#39;t break or scroll away when the filters were applied.</li>
<li><strong>Global Context:</strong> A new <code>VisualSettingsContext</code> manages the state application-wide, ensuring that the Settings page and Command Palette stay in sync.</li>
</ul>
<p>Go ahead, break the UI. It&#39;s a feature, not a bug.</p>
<p><a href="https://fezcode.com/blog/visual-modes-easter-eggs">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Reducing React App Bundle Size: A Practical Guide]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/reducing-react-app-size</link>
            <guid isPermaLink="false">https://fezcode.com/blog/reducing-react-app-size</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Thu, 27 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Web performance is crucial for user experience. A slow-loading website can drive visitors away before they even see your content. Recently, I noticed that <strong>Fezcodex</strong> was taking a bit too long to load, so I decided to investigate and optimize the production build.</p>
<p>Here&#39;s how I managed to reduce the main bundle size by over <strong>70%</strong>, shrinking <code>main.js</code> by approximately <strong>590 kB</strong>.</p>
<h2>The Diagnosis</h2>
<p>When I ran the build command, I noticed the generated <code>main.js</code> file was quite large. In a standard Create React App (CRA) setup, the entire application is often bundled into a single JavaScript file. This means a user has to download <em>every</em> page and component just to see the homepage.</p>
<h2>Strategy 1: Code Splitting with <code>React.lazy</code> and <code>Suspense</code></h2>
<p>The most effective way to reduce the initial bundle size is <strong>Code Splitting</strong>. Instead of loading the entire app at once, we split the code into smaller chunks that are loaded on demand.</p>
<p>React provides built-in support for this via <code>React.lazy</code> and <code>Suspense</code>.</p>
<h3>Before:</h3>
<p>All pages were imported statically at the top of the routing file:</p>
<pre><code class="language-javascript">import HomePage from &#39;../pages/HomePage&#39;;
import BlogPage from &#39;../pages/BlogPage&#39;;
import ProjectsPage from &#39;../pages/ProjectsPage&#39;;
// ... diverse imports
</code></pre>
<h3>After:</h3>
<p>I refactored the imports to be lazy loaded:</p>
<pre><code class="language-javascript">import React, { lazy, Suspense } from &#39;react&#39;;
import Loading from &#39;./Loading&#39;; // A simple spinner component

// Lazy Imports
const HomePage = lazy(() =&gt; import(&#39;../pages/HomePage&#39;));
const BlogPage = lazy(() =&gt; import(&#39;../pages/BlogPage&#39;));
const ProjectsPage = lazy(() =&gt; import(&#39;../pages/ProjectsPage&#39;));
// ...
</code></pre>
<p>And wrapped the routes in <code>Suspense</code>:</p>
<pre><code class="language-javascript">function AnimatedRoutes() {
  return (
    &lt;Suspense fallback={&lt;Loading /&gt;}&gt;
       {/* Routes ... */}
    &lt;/Suspense&gt;
  );
}
</code></pre>
<p>This change ensures that the code for <code>BlogPage</code> is only downloaded when the user actually navigates to <code>/blog</code>.</p>
<h3>How Does the Builder Know?</h3>
<p>You might wonder: <em>How does the build tool (Webpack, in this case) know to separate these files?</em></p>
<p>It all comes down to the <strong>dynamic <code>import()</code> syntax</strong>.</p>
<ol>
<li><strong>The Trigger:</strong> Standard imports (e.g., <code>import X from &#39;Y&#39;</code>) are static; Webpack bundles them immediately. When Webpack encounters <code>import(&#39;...&#39;)</code>, it recognizes a <strong>split point</strong>.</li>
<li><strong>Chunk Generation:</strong> Webpack cuts that specific module (and its unique dependencies) out of the main bundle and creates a separate file, known as a <strong>chunk</strong>.</li>
<li><strong>The Glue:</strong> The main bundle retains a tiny instruction. It effectively says, &quot;When the application needs this component, send a network request to fetch this specific chunk file.&quot;</li>
</ol>
<p><code>React.lazy</code> and <code>Suspense</code> simply manage the UI state (like showing the loading spinner) while that asynchronous network request is happening.</p>
<h2>Strategy 2: Disabling Source Maps in Production</h2>
<p>Source maps are incredibly useful for debugging, as they map the minified production code back to your original source code. However, they are also very large.</p>
<p>By default, Create React App generates source maps for production builds. While the browser only downloads them if you open the developer tools, they still occupy space on the server and can slow down deployment pipelines.</p>
<p>I disabled them in my <code>craco.config.js</code> (since I&#39;m using CRACO to override CRA settings):</p>
<pre><code class="language-javascript">webpack: {
  configure: (webpackConfig, { env }) =&gt; {
    // Disable sourcemaps for production
    if (env === &#39;production&#39;) {
      webpackConfig.devtool = false;
    }
    return webpackConfig;
  },
},
</code></pre>
<h2>The Results</h2>
<p>The impact was immediate and significant.</p>
<ul>
<li><strong>Before:</strong> <code>main.js</code> was heavy, containing the entire application logic.</li>
<li><strong>After:</strong> <code>main.js</code> reduced by <strong>~590 kB</strong>.</li>
</ul>
<p>Now, the initial load is snappy, and users only download what they need. If you&#39;re building a React app with many routes, I highly recommend implementing code splitting early on!</p>
<p><a href="https://fezcode.com/blog/reducing-react-app-size">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Mastering Tailwind CSS: The "Absolute Centering" Trick]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/mastering-tailwind-centering</link>
            <guid isPermaLink="false">https://fezcode.com/blog/mastering-tailwind-centering</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 26 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Have you ever tried to center a title in a header, but also wanted a &quot;Back&quot; button or a breadcrumb on the far left?</p>
<p>If you just use <code>flex justify-between</code>, the title gets pushed off-center if the left and right items aren&#39;t exactly the same width. It looks messy.</p>
<p>Today, I&#39;m going to show you the &quot;Magic&quot; behind perfectly centering an element while keeping a side item positioned absolutely, using <strong>Tailwind CSS</strong>.</p>
<h2>The Challenge</h2>
<p>The goal is to have the <strong>Title</strong> perfectly centered in the container, regardless of how long the <strong>Breadcrumb</strong> text on the left is.</p>
<p><strong>The Solution: Absolute Positioning within a Relative Container.</strong></p>
<pre><code class="language-jsx">&lt;div className=&quot;relative flex flex-col items-center justify-center mb-4&quot;&gt;
  {/* Breadcrumb (Absolute on Desktop) */}
  &lt;span className=&quot;md:absolute md:left-0 md:top-1/2 md:-translate-y-1/2 ...&quot;&gt;
    fc::apps::tcg
  &lt;/span&gt;

  {/* Title (Flow Content) */}
  &lt;h1 className=&quot;...&quot;&gt;Techno TCG Maker&lt;/h1&gt;
&lt;/div&gt;
</code></pre>
<h2>Step-by-Step Breakdown</h2>
<h3>1. The Parent (<code>relative</code>)</h3>
<pre><code class="language-jsx">&lt;div className=&quot;relative flex flex-col items-center justify-center&quot;&gt;
</code></pre>
<ul>
<li><code>relative</code>: This defines the &quot;sandbox&quot;. Any child with <code>absolute</code> positioning will position itself relative to <em>this</em> box, not the whole page.</li>
<li><code>flex flex-col items-center</code>: By default (mobile), this is just a vertical stack. The breadcrumb sits on top of the title.</li>
</ul>
<h3>2. The Breadcrumb (<code>absolute</code>)</h3>
<pre><code class="language-jsx">&lt;span className=&quot;md:absolute md:left-0 md:top-1/2 md:-translate-y-1/2&quot;&gt;
</code></pre>
<ul>
<li><code>md:absolute</code>: On medium screens (desktop) and up, we rip this element out of the document flow. It no longer takes up space, so the Title (which is still in the flow) naturally snaps to the exact center of the parent.</li>
<li><code>md:left-0</code>: &quot;Go to the far left edge.&quot;</li>
<li><code>md:top-1/2</code>: &quot;Move your top edge to 50% of the container&#39;s height.&quot; (This alone actually makes it look too low).</li>
<li><code>md:-translate-y-1/2</code>: &quot;Slide yourself UP by 50% of your own height.&quot; <strong>This is the golden rule</strong> for vertically centering absolute items.</li>
</ul>
<h2>Bonus: Coding Tailwind Like a Pro</h2>
<p>To write &quot;clean&quot; Tailwind that produces complex layouts like this, follow these mental models:</p>
<h3>A. Think Mobile-First</h3>
<p>Notice how I wrote <code>flex-col</code> first, and then <code>md:absolute</code>?</p>
<ul>
<li><strong>Bad:</strong> Write for desktop, then try to fix it for mobile.</li>
<li><strong>Good:</strong> Write for a narrow phone screen. Once that looks good, add <code>md:</code> prefix to change the layout for tablets/laptops.</li>
</ul>
<h3>B. Master the &quot;Invisible Box&quot; (Flexbox)</h3>
<p>90% of layout is just Flexbox.</p>
<ul>
<li><code>flex justify-between</code>: Items push to edges (Left ... Right).</li>
<li><code>flex justify-center</code>: Items bunch in the middle.</li>
<li><code>gap-4</code>: The best way to space items. Never use <code>margin-right</code> on children if you can use <code>gap</code> on the parent.</li>
</ul>
<h3>C. The &quot;Text Gradient&quot; Trick</h3>
<p>To get that shiny, futuristic text effect:</p>
<ol>
<li><code>bg-gradient-to-r</code>: Define the gradient direction.</li>
<li><code>from-X to-Y</code>: Define the colors.</li>
<li><code>bg-clip-text text-transparent</code>: The specific magic that clips the colored background to the shape of the letters and makes the text fill invisible so the background shows through.</li>
</ol>
<h3>D. Memorize the Spacing Scale</h3>
<p>Tailwind&#39;s scale is usually multiples of 4px (0.25rem).</p>
<ul>
<li><code>1</code> = 4px</li>
<li><code>4</code> = 16px (Standard padding/margin)</li>
<li><code>8</code> = 32px</li>
<li><code>16</code> = 64px</li>
</ul>
<p>Sticking to this rhythm makes your UI feel consistent and &quot;professional&quot; without you really trying.</p>
<p><a href="https://fezcode.com/blog/mastering-tailwind-centering">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[LeetCode 62: Unique Paths - A Dynamic Programming Approach]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/leetcode-62-unique-paths</link>
            <guid isPermaLink="false">https://fezcode.com/blog/leetcode-62-unique-paths</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Mon, 24 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>LeetCode 62, &quot;Unique Paths,&quot; is a classic problem that often serves as an excellent introduction to dynamic programming. It challenges us to find the number of unique paths a robot can take to reach the bottom-right corner of a <code>m x n</code> grid, starting from the top-left corner. The robot can only move either down or right at any point in time.</p>
<h2>Problem Description</h2>
<p>Imagine a robot positioned at the top-left cell (0,0) of a grid with <code>m</code> rows and <code>n</code> columns. The robot&#39;s goal is to reach the bottom-right cell (<code>m-1</code>, <code>n-1</code>). The only allowed moves are one step down or one step right. We need to calculate the total number of distinct paths the robot can take to reach its destination.</p>
<p>Let&#39;s visualize a simple <code>3 x 7</code> grid:</p>
<pre><code>S . . . . . .
. . . . . . .
. . . . . . F
</code></pre>
<p>Where <code>S</code> is the start and <code>F</code> is the finish.</p>
<h2>Dynamic Programming Approach</h2>
<p>This problem has optimal substructure and overlapping subproblems, making it a perfect candidate for dynamic programming.</p>
<p>Consider a cell <code>(i, j)</code> in the grid. To reach this cell, the robot must have come either from the cell directly above it (<code>i-1, j</code>) by moving down, or from the cell directly to its left (<code>i, j-1</code>) by moving right.</p>
<p>Therefore, the number of unique paths to reach <code>(i, j)</code> is the sum of unique paths to reach <code>(i-1, j)</code> and unique paths to reach <code>(i, j-1)</code>.</p>
<p>Let <code>dp[i][j]</code> represent the number of unique paths to reach cell <code>(i, j)</code>.
The recurrence relation is:
<code>dp[i][j] = dp[i-1][j] + dp[i][j-1]</code></p>
<p><strong>Base Cases:</strong></p>
<ul>
<li>For any cell in the first row (<code>i=0</code>), there&#39;s only one way to reach it: by moving right repeatedly from <code>(0,0)</code>. So, <code>dp[0][j] = 1</code>.</li>
<li>For any cell in the first column (<code>j=0</code>), there&#39;s only one way to reach it: by moving down repeatedly from <code>(0,0)</code>. So, <code>dp[i][0] = 1</code>.</li>
<li>The starting cell <code>(0,0)</code> has <code>dp[0][0] = 1</code> path (it&#39;s already there).</li>
</ul>
<p>We can build a 2D array (or even optimize space to a 1D array) to store these path counts.</p>
<h2>Go Solution</h2>
<p>Here&#39;s an implementation of the dynamic programming approach in Go:</p>
<pre><code class="language-go">func uniquePaths(m int, n int) int {
    // Create a 2D DP array initialized with 1s for the base cases
    dp := make([][]int, m)
    for i := range dp {
        dp[i] = make([]int, n)
    }

    // Initialize the first row and first column with 1s
    // since there&#39;s only one way to reach any cell in the first row/column
    // (by only moving right or only moving down respectively).
    for i := 0; i &lt; m; i++ {
        dp[i][0] = 1
    }
    for j := 0; j &lt; n; j++ {
        dp[0][j] = 1
    }

    // Fill the DP table
    for i := 1; i &lt; m; i++ {
        for j := 1; j &lt; n; j++ {
            dp[i][j] = dp[i-1][j] + dp[i][j-1]
        }
    }

    // The result is the value at the bottom-right corner
    return dp[m-1][n-1]
}
</code></pre>
<h3>Combinatorial Approach</h3>
<p>Alternatively, this problem can be solved using a combinatorial approach. To reach the bottom-right corner of an <code>m x n</code> grid, the robot must make exactly <code>m-1</code> &#39;down&#39; moves and <code>n-1</code> &#39;right&#39; moves. The total number of moves will be <code>(m-1) + (n-1)</code>.</p>
<p>The problem then reduces to finding the number of ways to arrange these <code>m-1</code> down moves and <code>n-1</code> right moves. This is a classic combinatorial problem: choosing <code>m-1</code> positions for the &#39;down&#39; moves (or <code>n-1</code> positions for the &#39;right&#39; moves) out of a total of <code>(m-1) + (n-1)</code> moves.</p>
<p>The formula for combinations is C(N, K) = N! / (K! * (N-K)!), where N is the total number of steps and K is the number of &#39;down&#39; (or &#39;right&#39;) moves.</p>
<h3>Go Solution (Combinatorial)</h3>
<pre><code class="language-go">func uniquePathsCombinatorial(m int, n int) int {
    downMoves := m - 1
    rightMoves := n - 1
    totalSteps := downMoves + rightMoves

    // Choose the smaller of downMoves or rightMoves for k to minimize calculations
    k := downMoves
    if rightMoves &lt; downMoves {
        k = rightMoves
    }

    var comb float64 = 1.0
    // Formula: C(N, K) = (N/1) * ((N-1)/2) * ... * ((N-k+1)/k)
    // This avoids large factorial calculations by performing multiplications and divisions iteratively.
    for i := 1; i &lt;= k; i++ {
        comb = comb * float64(totalSteps - i + 1) / float64(i)
    }

    return int(comb)
}
</code></pre>
<h2>Conclusion</h2>
<p>The &quot;Unique Paths&quot; problem demonstrates the power of dynamic programming in breaking down a complex problem into simpler, overlapping subproblems. By carefully defining our state and recurrence relation, we can build up the solution efficiently. This particular problem also has a combinatorial solution using binomial coefficients, but the dynamic programming approach is often more intuitive for beginners to DP.</p>
<p><a href="https://fezcode.com/blog/leetcode-62-unique-paths">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Gaussian Elimination: The Swiss Army Knife of Linear Systems in Computer Engineering]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/gaussian-elimination</link>
            <guid isPermaLink="false">https://fezcode.com/blog/gaussian-elimination</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sun, 23 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>When you hear &quot;linear algebra,&quot; your mind might jump to complex math, but at its heart lies a powerful tool called Gaussian Elimination. Far from being just a theoretical concept, this method is a workhorse in various fields of computer engineering, helping us solve systems of linear equations efficiently. In simple terms, it&#39;s a systematic way to solve multiple equations with multiple unknowns.</p>
<h2>What is Gaussian Elimination? (The Simple Explanation)</h2>
<p>Imagine you have a few simple equations:
Equation 1: <code>x + y = 5</code>
Equation 2: <code>x - y = 1</code></p>
<p>You can probably solve this in your head or by simple substitution. Gaussian elimination provides a step-by-step, mechanical way to solve this, even when you have hundreds or thousands of equations and variables.</p>
<p>The core idea is to transform a system of equations into an &quot;echelon form&quot; using three basic operations:</p>
<ol>
<li><strong>Swapping rows</strong>: Change the order of equations.</li>
<li><strong>Multiplying a row by a non-zero number</strong>: Scale an equation.</li>
<li><strong>Adding a multiple of one row to another row</strong>: Combine equations.</li>
</ol>
<p>These operations don&#39;t change the solution of the system. By applying them strategically, you eliminate variables one by one until you have a very simple system that can be solved by &quot;back-substitution&quot; (solving the last equation first, then plugging its answer into the second-to-last, and so on).</p>
<h2>How it Works (A Quick Visual)</h2>
<p>Let&#39;s represent our equations in a matrix format (an &quot;augmented matrix&quot;):</p>
<pre><code>[ 1  1 | 5 ]
[ 1 -1 | 1 ]
</code></pre>
<p><strong>Step 1: Get a leading 1 in the first row, first column.</strong> (Already done here!)</p>
<p><strong>Step 2: Make all entries below the leading 1 in the first column zero.</strong>
Subtract Row 1 from Row 2: <code>R2 = R2 - R1</code></p>
<pre><code>[ 1  1 | 5 ]
[ 0 -2 | -4 ]
</code></pre>
<p><strong>Step 3: Get a leading 1 in the second row, second column.</strong>
Divide Row 2 by -2: <code>R2 = R2 / -2</code></p>
<pre><code>[ 1  1 | 5 ]
[ 0  1 | 2 ]
</code></pre>
<p>Now the matrix is in row echelon form! We can translate it back to equations:
Equation 1: <code>x + y = 5</code>
Equation 2: <code>y = 2</code></p>
<p><strong>Step 4: Back-substitution.</strong>
From Equation 2, we know <code>y = 2</code>. Substitute <code>y=2</code> into Equation 1:
<code>x + 2 = 5</code>
<code>x = 3</code></p>
<p>So, <code>x = 3</code> and <code>y = 2</code>. This systematic process is what makes Gaussian Elimination so powerful for computers.</p>
<h2>Usages in Computer Engineering</h2>
<p>Gaussian Elimination might seem like abstract math, but its ability to efficiently solve linear systems is fundamental to many computer engineering applications:</p>
<h3>1. Computer Graphics</h3>
<ul>
<li><strong>3D Transformations</strong>: When you move, rotate, or scale objects in 3D space, you&#39;re performing linear transformations. Combining these transformations, especially finding inverse transformations, often boils down to solving linear systems.</li>
<li><strong>Ray Tracing</strong>: Determining intersections between rays and complex 3D objects (like planes or curved surfaces) can involve solving systems of equations.</li>
<li><strong>Lighting and Shading</strong>: Calculating how light interacts with surfaces (e.g., diffuse, specular components) can also lead to linear systems.</li>
</ul>
<h3>2. Machine Learning and Data Science</h3>
<ul>
<li><strong>Linear Regression</strong>: Finding the &quot;best fit&quot; line or plane for data points is a classic problem that can be solved by setting up and solving a system of linear equations (normal equations).</li>
<li><strong>Solving Optimization Problems</strong>: Many optimization algorithms (e.g., in deep learning) involve finding solutions to systems of equations to minimize error functions.</li>
</ul>
<h3>3. Robotics and Control Systems</h3>
<ul>
<li><strong>Kinematics</strong>: Determining the position and orientation of robot parts based on joint angles (forward kinematics) or finding joint angles to reach a desired position (inverse kinematics) frequently involves solving linear systems.</li>
<li><strong>Path Planning</strong>: Calculating trajectories for robots to move from one point to another while avoiding obstacles can be formulated using linear equations.</li>
</ul>
<h3>4. Circuit Analysis</h3>
<ul>
<li><strong>Kirchhoff&#39;s Laws</strong>: In electrical engineering, applying Kirchhoff&#39;s voltage and current laws to a circuit often results in a system of linear equations that need to be solved to find unknown currents or voltages.</li>
</ul>
<h3>5. Network Flow Problems</h3>
<ul>
<li><strong>Routing Algorithms</strong>: In computer networks, optimizing data flow, finding shortest paths, or allocating bandwidth can be modeled as systems of linear equations or inequalities, which are then solved using techniques related to Gaussian elimination.</li>
</ul>
<h2>Conclusion</h2>
<p>Gaussian Elimination provides a robust and algorithmic approach to a problem that appears everywhere in computing: solving linear systems. From rendering realistic 3D graphics to teaching machines to learn, and from controlling robots to analyzing complex electrical circuits, this mathematical workhorse underpins a vast array of technologies we use every day. Its beauty lies in its simplicity and its profound impact on making complex computational problems tractable.</p>
<p><a href="https://fezcode.com/blog/gaussian-elimination">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Fixing GRUB Syntax Errors Caused by Grub Customizer]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/fixing-grub</link>
            <guid isPermaLink="false">https://fezcode.com/blog/fixing-grub</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 22 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Fixing GRUB Syntax Errors Caused by Grub Customizer</h1>
<p>You&#39;ve updated your system, and suddenly you&#39;re greeted with a cryptic GRUB error message:</p>
<pre><code>error: syntax error.
error: Incorrect command.
error: syntax error.
Syntax error at line 221
Syntax errors are detected in generated GRUB config file.
Ensure that there are no errors in /etc/default/grub
and /etc/grub.d/* files or please file a bug report with
/boot/grub/grub.cfg.new file attached.
</code></pre>
<p>This error can be frustrating, especially when you haven&#39;t manually edited any GRUB configuration files. This blog post will guide you through identifying the source of this problem and how to fix it.</p>
<h2>The Source of the Problem: Grub Customizer</h2>
<p>In many cases, the culprit behind these GRUB syntax errors is a tool called <strong>Grub Customizer</strong>. While it offers a graphical interface to manage your GRUB bootloader, it can sometimes cause problems, especially after system updates.</p>
<p>Grub Customizer works by replacing the standard GRUB configuration scripts in <code>/etc/grub.d/</code> with its own &quot;proxy&quot; scripts. These proxy scripts then call a binary named <code>grubcfg_proxy</code> to apply the customizations. This can lead to a fragile configuration that breaks when other parts of the system are updated.</p>
<h2>How to Detect the Source of the Problem</h2>
<p>You can confirm if Grub Customizer is the cause of your issues by inspecting the <code>/etc/grub.d/</code> directory. Open a terminal and run:</p>
<pre><code class="language-bash">ls -l /etc/grub.d/
</code></pre>
<p>If you see files with <code>_proxy</code> in their names (e.g., <code>10_linux_proxy</code>, <code>30_os-prober_proxy</code>) and directories like <code>backup</code>, <code>bin</code>, and <code>proxifiedScripts</code>, it&#39;s a strong indication that Grub Customizer has modified your GRUB configuration.</p>
<p>You might also find a script like this in <code>/etc/grub.d/10_linux_proxy</code>:</p>
<pre><code class="language-sh">#!/bin/sh
#THIS IS A GRUB PROXY SCRIPT
&#39;/etc/grub.d/proxifiedScripts/linux&#39; | /etc/grub.d/bin/grubcfg_proxy &quot;-&#39;SUBMENU&#39; as &#39;Advanced options for Ubuntu&#39;{-&#39;Advanced options for Ubuntu&#39;/*, -&#39;Advanced options for Ubuntu&#39;/&#39;Ubuntu, with Linux 6.17.0-6-generic&#39;~09ff0eeb66e30428b876bfc87b466e5d~, -&#39;Advanced options for Ubuntu&#39;/&#39;Ubuntu, with Linux 6.17.0-6-generic (recovery mode)&#39;~235ee17b753aaaca5703a4e27ecda63b~}
+*
+#text
-&#39;Ubuntu&#39;~5eca380a341c422accf5af1ff1704fc7~
&quot;%
</code></pre>
<p>This non-standard script is a clear sign of Grub Customizer&#39;s intervention.</p>
<h2>The Approach and Solution</h2>
<p>The most reliable way to fix this issue is to completely remove Grub Customizer and restore your GRUB configuration to its default state. This will remove any customizations you&#39;ve made with the tool, but it will give you a stable and working bootloader.</p>
<p>Here are the steps to follow:</p>
<h3>1. Purge Grub Customizer</h3>
<p>First, you need to completely remove the <code>grub-customizer</code> package and its configuration files. Run the following command:</p>
<pre><code class="language-bash">sudo apt-get purge grub-customizer
</code></pre>
<h3>2. Reinstall GRUB</h3>
<p>Next, reinstall the GRUB package to ensure all the original scripts are restored in <code>/etc/grub.d/</code>.</p>
<pre><code class="language-bash">sudo apt-get install --reinstall grub-pc
</code></pre>
<p><strong>Note:</strong> This command is for systems using a traditional BIOS or CSM. If you are using UEFI, you might need to install <code>grub-efi-amd64</code> or a similar package depending on your architecture.</p>
<h3>3. Update GRUB</h3>
<p>Finally, regenerate the <code>grub.cfg</code> file with the restored, standard scripts. This command will also run <code>os-prober</code> to detect other operating systems like Windows and add them to the boot menu.</p>
<pre><code class="language-bash">sudo update-grub
</code></pre>
<p>After running these commands, your GRUB configuration should be back to a clean, working state, and the syntax errors should be gone.</p>
<h2>Conclusion</h2>
<p>Grub Customizer can be a convenient tool, but it can also lead to unexpected issues. If you encounter GRUB errors after using it, the best solution is often to remove it and revert to the standard GRUB configuration. By following the steps in this guide, you can quickly resolve these errors and get your system booting correctly again.</p>
<p><a href="https://fezcode.com/blog/fixing-grub">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[4 Equals For Complete Equalness]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/floating-point-precision-in-javascript</link>
            <guid isPermaLink="false">https://fezcode.com/blog/floating-point-precision-in-javascript</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 21 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Behold, A New Operator <code>====</code></h1>
<h2>About</h2>
<p>When <code>0.1 + 0.2</code> in JavaScript yields <code>0.30000000000000004</code>, it highlights a <strong>common aspect of computer arithmetic</strong>,
not a bug. This occurs because JavaScript, like most languages, uses the <strong>IEEE 754 standard</strong> for floating-point numbers,
which relies on <strong>binary (base-2) representation</strong>.</p>
<p><strong>Decimal fractions</strong> like 0.1 and 0.2 <strong>cannot be perfectly represented as finite binary fractions</strong>; they become infinitely repeating.
When these are stored in a <strong>finite number of bits</strong>, a <strong>tiny truncation error</strong> is introduced. This <strong>slight imprecision</strong>
in each number <strong>accumulates during addition</strong>, resulting in a sum that&#39;s marginally off from the <strong>exact mathematical total</strong>.</p>
<h3>Solutions</h3>
<p>For scenarios requiring <strong>precise decimal arithmetic</strong> (e.g., financial applications), direct floating-point calculations
can be problematic. Consider these approaches:</p>
<ol>
<li><strong>Rounding:</strong> Use <code>toFixed()</code> to round results to a desired decimal precision. Remember to convert the string output
back to a number if needed.<pre><code class="language-javascript">parseFloat((0.1 + 0.2).toFixed(1)); // 0.3
</code></pre>
</li>
<li><strong>Integer Arithmetic:</strong> <strong>Scale numbers to integers</strong> before calculations and then scale the final result back down.<pre><code class="language-javascript">(0.1 * 10 + 0.2 * 10) / 10; // 0.3
</code></pre>
</li>
<li><strong>Specialized Libraries:</strong> For <strong>advanced precision</strong>, utilize libraries like <code>Big.js</code> or <code>Decimal.js</code>.</li>
</ol>
<p>This behavior is a <strong>fundamental consequence of binary representation in computing</strong>, not a flaw in JavaScript,
and <strong>understanding it is key</strong> to handling numerical precision effectively.</p>
<h2>Introducing the <code>====</code> Operator: For When <code>===</code> Just Isn&#39;t Enough</h2>
<p>Sometimes, <strong>strict equality</strong> (<code>===</code>) feels like it&#39;s trying <em>too hard</em> to be precise, yet still falls short of our
deepest desires for perfect, unyielding truth. For those moments, when you need to compare not just value and type,
but also the very <strong>essence</strong> of existence, I propose the <strong>Quadruple Equals Operator (<code>====</code>)</strong>!</p>
<p>What does <code>====</code> do? Well, it&#39;s simple:</p>
<ul>
<li><code>0.1 + 0.2 ==== 0.3</code> would <em>(theoretically)</em> return <code>true</code>. Because in a world where <code>====</code> exists, numbers just <em>know</em> what they&#39;re supposed to be.</li>
<li><code>&quot;hello&quot; ==== &quot;hello&quot;</code> would, naturally, be <code>true</code>.</li>
<li><code>[] ==== []</code> might still be <code>false</code>, because even <code>====</code> respects the <strong>existential uniqueness</strong> of array instances. But I am working on it. ¯\_(ツ)_/¯</li>
<li>The <code>====</code> operator is so powerful, it can detect <strong>deep existential equality</strong>, ensuring that not only values and types match,
but also their <strong>historical context</strong>, their <strong>developer&#39;s intent</strong>, and their <strong>cosmic vibrational frequency</strong>.</li>
</ul>
<p>Alas, <code>====</code> is a mere dream, a <strong>mythical beast</strong> in the JavaScript ecosystem, born from the frustration of floating-point arithmetic.
For now, we&#39;ll have to stick to our practical solutions. But one can dream of a world where <code>0.1 + 0.2 ==== 0.3</code> just <em>makes sense</em>.</p>
<p><a href="https://fezcode.com/blog/floating-point-precision-in-javascript">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Kaprekar's Routine: A Curious Number Game]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/kaprekars-routine</link>
            <guid isPermaLink="false">https://fezcode.com/blog/kaprekars-routine</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 18 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Have you ever played with numbers and found a surprising pattern? One such fascinating pattern is hidden within <strong>Kaprekar&#39;s Routine</strong>, named after the Indian mathematician D.R. Kaprekar. It&#39;s a simple game that, for most four-digit numbers, always leads to the same result: <strong>6174</strong>.</p>
<p>Let&#39;s dive in and see how it works!</p>
<h2>The Rules of the Game</h2>
<ol>
<li><strong>Pick a four-digit number</strong> with at least two different digits. (Numbers like 1111, 2222, etc., won&#39;t work).</li>
<li><strong>Arrange the digits</strong> to form the largest possible number.</li>
<li><strong>Arrange the same digits</strong> to form the smallest possible number.</li>
<li><strong>Subtract</strong> the smallest number from the largest number.</li>
<li><strong>Repeat</strong> steps 2-4 with the new number you get.</li>
</ol>
<p>You&#39;ll be amazed at what happens!</p>
<h2>Example 1: Starting with 3524</h2>
<p>Let&#39;s try with the number <strong>3524</strong>:</p>
<ul>
<li><strong>Step 1:</strong> Our number is 3524. It has different digits.</li>
<li><strong>Step 2:</strong> Largest number: 5432</li>
<li><strong>Step 3:</strong> Smallest number: 2345</li>
<li><strong>Step 4:</strong> Subtract: 5432 - 2345 = <strong>3087</strong></li>
</ul>
<p>Now, we repeat the process with <strong>3087</strong>:</p>
<ul>
<li><strong>Step 2:</strong> Largest number: 8730</li>
<li><strong>Step 3:</strong> Smallest number: 0378 (remember to include leading zeros to keep it a four-digit number)</li>
<li><strong>Step 4:</strong> Subtract: 8730 - 0378 = <strong>8352</strong></li>
</ul>
<p>Repeat with <strong>8352</strong>:</p>
<ul>
<li><strong>Step 2:</strong> Largest number: 8532</li>
<li><strong>Step 3:</strong> Smallest number: 2358</li>
<li><strong>Step 4:</strong> Subtract: 8532 - 2358 = <strong>6174</strong></li>
</ul>
<p>And there it is! We reached 6174.</p>
<h2>Example 2: Starting with 1987</h2>
<p>Let&#39;s try another one with <strong>1987</strong>:</p>
<ul>
<li><strong>Step 1:</strong> Our number is 1987.</li>
<li><strong>Step 2:</strong> Largest number: 9871</li>
<li><strong>Step 3:</strong> Smallest number: 1789</li>
<li><strong>Step 4:</strong> Subtract: 9871 - 1789 = <strong>8082</strong></li>
</ul>
<p>Repeat with <strong>8082</strong>:</p>
<ul>
<li><strong>Step 2:</strong> Largest number: 8820</li>
<li><strong>Step 3:</strong> Smallest number: 0288</li>
<li><strong>Step 4:</strong> Subtract: 8820 - 0288 = <strong>8532</strong></li>
</ul>
<p>Repeat with <strong>8532</strong>:</p>
<ul>
<li><strong>Step 2:</strong> Largest number: 8532</li>
<li><strong>Step 3:</strong> Smallest number: 2358</li>
<li><strong>Step 4:</strong> Subtract: 8532 - 2358 = <strong>6174</strong></li>
</ul>
<p>Again, we arrived at 6174!</p>
<h2>The Magic of 6174</h2>
<p>This number, 6174, is known as <strong>Kaprekar&#39;s Constant</strong>. For almost any four-digit number (with at least two different digits), if you keep applying Kaprekar&#39;s routine, you will eventually reach 6174. Once you reach 6174, the next step will always be:</p>
<ul>
<li>Largest: 7641</li>
<li>Smallest: 1467</li>
<li>Subtract: 7641 - 1467 = <strong>6174</strong></li>
</ul>
<p>It&#39;s a loop!</p>
<p>Kaprekar&#39;s routine is a wonderful example of how simple arithmetic operations can lead to unexpected and beautiful mathematical constants. Try it with your own four-digit numbers and see the magic unfold!</p>
<p><a href="https://fezcode.com/blog/kaprekars-routine">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[The Inevitable Dance of Entropy: A Rant on Chaos]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/chaos-theory-philosophical-rant</link>
            <guid isPermaLink="false">https://fezcode.com/blog/chaos-theory-philosophical-rant</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 18 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>Oh, to be human! To crave <strong>order</strong>, to meticulously <strong>plan</strong>, to believe that if we just gather enough data, analyze enough variables, we can <strong>predict the future</strong>. What a <em>glorious, self-deceiving delusion</em>. Because lurking beneath our carefully constructed narratives of cause and effect, there&#39;s a mischievous, undeniable truth: <strong>Chaos Theory</strong>.</p>
<p>And no, I&#39;m not talking about some dry, academic treatise on differential equations. I&#39;m talking about the <em><strong>philosophy</strong></em> of chaos, the <em>infuriating, liberating realization</em> that the universe, and our lives within it, are fundamentally, gloriously, and terrifyingly <strong>unpredictable</strong>.</p>
<p>We cling to the idea that every grand outcome must have an equally grand progenitor. A monumental decision leads to a monumental consequence. But Chaos Theory, in its most poetic form, whispers (or rather, <strong>shouts</strong>) about the &quot;<em><strong>butterfly effect</strong></em>.&quot; It&#39;s the notion, famously articulated by meteorologist Edward Lorenz, that a butterfly flapping its wings in Brazil could, <em>theoretically</em>, set off a tornado in Texas. Think about that for a second. A <em>tiny</em>, almost imperceptible flutter, a mere breath of air, cascading through an infinitely complex system to <strong>reshape continents</strong>.</p>
<p>How many times have you looked back at a <strong>pivotal moment</strong> in your life and traced its origin not to a grand choice, but to a <em>forgotten email</em>, a <em>chance encounter</em>, a <em>delayed train</em>, or a <em>spilled cup of coffee</em>? That job you landed? Maybe it wasn&#39;t your stellar resume, but the fact that the hiring manager had a particularly good morning because their cat <em>didn&#39;t</em> wake them up at 4 AM for once. That relationship that changed everything? Perhaps it began because you took a different route home, avoiding a puddle that would have otherwise sent you down a <em>completely different path</em>.</p>
<p>We build our models, our algorithms, our five-year plans, convinced that if we just perfect the inputs, the outputs will be ours to command. But <strong>chaos laughs</strong>. It reminds us that even the <em>most minute, unmeasurable perturbation</em> can send the entire system veering off into an entirely new, <strong>unforeseen trajectory</strong>. It&#39;s why weather forecasts beyond a few days are <em>notoriously unreliable</em>, despite supercomputers churning through quadrillions of calculations. It&#39;s why economies crash when a seemingly minor market fluctuation triggers a <strong>cascade of panic</strong>.</p>
<p>And this, my friends, is where the &quot;<em><strong>rant</strong></em>&quot; truly begins. Because while our rational minds scream for <strong>control</strong>, for <strong>certainty</strong>, for a predictable narrative, chaos offers <em>none</em>. It offers a <strong>beautiful, maddening dance</strong> where every step influences the next in ways we can never fully grasp. It&#39;s the <em>ultimate cosmic prank</em>, reminding us of our infinitesimal place in a universe that cares not for our spreadsheets or our anxieties.</p>
<p>So, what&#39;s the point? To despair? To throw our hands up and surrender to the whims of the universe? Perhaps. Or perhaps, it&#39;s to find a <em>strange, unsettling peace</em> in the surrender. To embrace the fact that life is less a meticulously crafted blueprint and more a <strong>jazz improvisation</strong> – full of <em>unexpected notes</em>, <em>beautiful accidents</em>, and moments of pure, unadulterated, <strong>glorious chaos</strong>.</p>
<p>Stop trying to control the wind; <strong>learn to sail</strong>. Stop trying to predict the butterfly; just <strong>marvel at its flight</strong>. Because in the heart of that unpredictability lies the very essence of life&#39;s adventure. And maybe, just maybe, that&#39;s a <strong>rant worth having</strong>.</p>
<p><a href="https://fezcode.com/blog/chaos-theory-philosophical-rant">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Minimum Number of Steps to Make Two Strings Anagram]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/minimum-number-of-steps-to-make-two-strings-anagram</link>
            <guid isPermaLink="false">https://fezcode.com/blog/minimum-number-of-steps-to-make-two-strings-anagram</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Mon, 17 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>LeetCode 1347: Minimum Number of Steps to Make Two Strings Anagram</h1>
<h2>Problem Description</h2>
<p>Given two strings <code>s</code> and <code>t</code> of the same length, you want to change <code>t</code> in the minimum number of steps such that it becomes an anagram of <code>s</code>. A step consists of replacing one character in <code>t</code> with another character.</p>
<p>An Anagram is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once. For example, &quot;anagram&quot; and &quot;nagaram&quot; are anagrams.</p>
<p>Both strings consist of lowercase English letters.</p>
<p><strong>Example 1:</strong>
Input: s = &quot;bab&quot;, t = &quot;aba&quot;
Output: 1
Explanation: Replace the first &#39;a&#39; in t with b, t = &quot;bba&quot; which is an anagram of s.</p>
<p><strong>Example 2:</strong>
Input: s = &quot;leetcode&quot;, t = &quot;practice&quot;
Output: 5
Explanation: Replace &#39;p&#39;, &#39;r&#39;, &#39;a&#39;, &#39;i&#39;, &#39;c&#39; in t with &#39;l&#39;, &#39;e&#39;, &#39;e&#39;, &#39;t&#39;, &#39;d&#39; to form an anagram of s.</p>
<p><strong>Example 3:</strong>
Input: s = &quot;anagram&quot;, t = &quot;mangaar&quot;
Output: 0
Explanation: &quot;anagram&quot; is already an anagram of &quot;mangaar&quot;.</p>
<h2>Solution in Go</h2>
<p>The core idea to solve this problem is to count the frequency of each character in both strings <code>s</code> and <code>t</code>. Since we want to transform <code>t</code> into an anagram of <code>s</code> by replacing characters in <code>t</code>, we need to identify characters in <code>t</code> that are &quot;excess&quot; compared to what <code>s</code> needs.</p>
<p>For each character from &#39;a&#39; to &#39;z&#39;:</p>
<ol>
<li>Count its occurrences in <code>s</code>.</li>
<li>Count its occurrences in <code>t</code>.</li>
<li>If the count of a character in <code>t</code> is greater than its count in <code>s</code>, it means <code>t</code> has <code>t_count - s_count</code> extra occurrences of this character. These extra occurrences must be replaced to match the character distribution of <code>s</code>.</li>
<li>The sum of these differences for all characters will give us the minimum number of steps.</li>
</ol>
<p>This approach works because we only care about the characters that are <em>overrepresented</em> in <code>t</code>. Any characters that are <em>underrepresented</em> in <code>t</code> (i.e., <code>t_count &lt; s_count</code>) will be formed by replacing the overrepresented characters. The total number of replacements needed is exactly the sum of the excesses.</p>
<pre><code class="language-go">package main

import &quot;fmt&quot;

func minSteps(s string, t string) int {
	sFreq := make([]int, 26) // Frequency array for string s
	tFreq := make([]int, 26) // Frequency array for string t

	// Populate frequency array for string s
	for _, char := range s {
		sFreq[char-&#39;a&#39;]++
	}

	// Populate frequency array for string t
	for _, char := range t {
		tFreq[char-&#39;a&#39;]++
	}

	steps := 0
	// Compare frequencies and calculate steps
	for i := 0; i &lt; 26; i++ {
		// If character &#39;i&#39; appears more times in t than in s,
		// these are the characters that need to be changed.
		if tFreq[i] &gt; sFreq[i] {
			steps += tFreq[i] - sFreq[i]
		}
	}

	return steps
}

func main() {
	// Test cases
	fmt.Println(minSteps(&quot;bab&quot;, &quot;aba&quot;))    // Expected: 1
	fmt.Println(minSteps(&quot;leetcode&quot;, &quot;practice&quot;)) // Expected: 5
	fmt.Println(minSteps(&quot;anagram&quot;, &quot;mangaar&quot;)) // Expected: 0
	fmt.Println(minSteps(&quot;xxyyzz&quot;, &quot;xxyyzz&quot;)) // Expected: 0
	fmt.Println(minSteps(&quot;friend&quot;, &quot;family&quot;)) // Expected: 4
}
</code></pre>
<h2>Hashmap Solution</h2>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
)

func minSteps(s string, t string) int {
	m := map[string]int{}
	for i := 0; i &lt; len(s); i++ {
		m[string(s[i])]++
	}
	for i := 0; i &lt; len(t); i++ {
		m[string(t[i])]--
	}
	steps := 0
	for _, v := range m {
		steps += abs(v)
	}
	return steps / 2

}

func abs(x int) int {
	if x &lt; 0 {
		return -x
	}
	return x
}

func main() {
	fmt.Println(minSteps(&quot;bab&quot;, &quot;aba&quot;))
	fmt.Println(minSteps(&quot;leetcode&quot;, &quot;practice&quot;))
	fmt.Println(minSteps(&quot;anagram&quot;, &quot;mangaar&quot;))
	fmt.Println(minSteps(&quot;xxyyzz&quot;, &quot;xxyyzz&quot;))
	fmt.Println(minSteps(&quot;friend&quot;, &quot;family&quot;))
}
</code></pre>
<p><a href="https://fezcode.com/blog/minimum-number-of-steps-to-make-two-strings-anagram">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Decoding the Digital Alphabet: A Comprehensive Guide to BaseXX Encodings]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/decoding-the-digital-alphabet-base-xx-encodings</link>
            <guid isPermaLink="false">https://fezcode.com/blog/decoding-the-digital-alphabet-base-xx-encodings</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sun, 16 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h2>Introduction</h2>
<p>In the digital realm, data often needs to be transformed for various purposes, such as safe transmission over different mediums, storage, or simply to make it more human-readable. This is where &quot;BaseXX&quot; encodings come into play. These methods convert binary data into a textual representation using a specific set of characters, known as an alphabet. While Base64 is perhaps the most widely known, a diverse family of BaseXX encodings exists, each with its unique characteristics and ideal use cases. This post will explore Base32, Base58, Base62, Base64, and Base85, comparing their features and shedding light on why you might choose one over another.</p>
<h2>Understanding the Basics: How BaseXX Encodings Work</h2>
<p>At its core, BaseXX encoding involves representing binary data (sequences of bits) as a string of characters from a predefined alphabet. The &quot;XX&quot; in BaseXX refers to the size of this alphabet. For example, Base64 uses an alphabet of 64 characters. The larger the alphabet, the more efficiently data can be represented (i.e., fewer characters are needed to encode the same amount of binary data), but it might come at the cost of readability or URL-safety.</p>
<h2>The BaseXX Family: A Closer Look</h2>
<h3>Base32: The Human-Friendly Choice</h3>
<ul>
<li><strong>Character Set:</strong> Uses 32 characters, typically uppercase letters A-Z and digits 2-7. Digits 0, 1, and 8 are often excluded to avoid confusion with letters O, I, and B, enhancing human readability and reducing transcription errors.</li>
<li><strong>Encoding Scheme:</strong> Encodes 5 bytes of binary data (40 bits) into 8 printable characters, with each character representing 5 bits.</li>
<li><strong>Efficiency:</strong> Less efficient than Base64 or Base85, increasing data size by approximately 60%.</li>
<li><strong>Human Readability:</strong> Designed for case-insensitivity and manual entry, making it suitable for environments where human interaction with the encoded string is common.</li>
<li><strong>Use Cases:</strong> Product activation codes, DNSSEC, QR codes, and situations requiring case-insensitive identifiers.</li>
</ul>
<h3>Base58: Cryptocurrency&#39;s Foundation</h3>
<ul>
<li><strong>Character Set:</strong> A 58-character alphanumeric alphabet that intentionally excludes visually ambiguous characters like 0 (zero), O (uppercase O), I (uppercase I), and l (lowercase L). It also omits &#39;+&#39; and &#39;/&#39; found in Base64.</li>
<li><strong>Encoding Scheme:</strong> Works by treating binary data as a large integer and converting it to a base-58 representation.</li>
<li><strong>Efficiency:</strong> Less compact than Base64 or Base85, requiring about 25% more characters than Base64 for the same data.</li>
<li><strong>Human Readability:</strong> Highly optimized for human readability and transcription, significantly minimizing errors due to similar-looking characters.</li>
<li><strong>Use Cases:</strong> Widely used for cryptocurrency addresses (e.g., Bitcoin, Ethereum) and other identifiers in decentralized systems where error-resistant, human-friendly representation is crucial.</li>
</ul>
<h3>Base62: The Compact URL-Safe Option</h3>
<ul>
<li><strong>Character Set:</strong> Uses 62 alphanumeric characters (a-z, A-Z, 0-9).</li>
<li><strong>Encoding Scheme:</strong> Similar to other BaseXX methods, it converts binary data into a string using its 62-character alphabet.</li>
<li><strong>Efficiency:</strong> More compact than Base32 and Base58.</li>
<li><strong>Human Readability:</strong> Generally good, as it only uses alphanumeric characters.</li>
<li><strong>Use Cases:</strong> Ideal for short URLs, unique ID generation, and any scenario where a compact, URL-safe, and human-readable string is desired without padding.</li>
</ul>
<h3>Base64: The Ubiquitous Standard</h3>
<ul>
<li><strong>Character Set:</strong> Uses 64 characters: uppercase letters (A-Z), lowercase letters (a-z), digits (0-9), and two symbols, typically &#39;+&#39; and &#39;/&#39;. An optional padding character &#39;=&#39; is used to ensure encoded output is a multiple of 4 characters.</li>
<li><strong>Encoding Scheme:</strong> Encodes 3 bytes of binary data (24 bits) into 4 characters, with each character representing 6 bits.</li>
<li><strong>Efficiency:</strong> More efficient than Base32 and Base58, increasing data size by approximately 33%.</li>
<li><strong>Human Readability:</strong> Less human-readable than Base32, Base58, or Base62 due to the inclusion of symbols and padding.</li>
<li><strong>Use Cases:</strong> Encoding binary data in text-based formats like email (MIME), web APIs (JSON, XML), and embedding images directly into HTML or CSS. URL-safe variants (e.g., replacing &#39;+&#39; with &#39;-&#39; and &#39;/&#39; with &#39;_&#39;) are often used for web applications.</li>
</ul>
<h3>Base85 (Ascii85): The Efficiency Champion</h3>
<ul>
<li><strong>Character Set:</strong> Employs 85 printable ASCII characters, often ranging from &#39;!&#39; to &#39;u&#39;.</li>
<li><strong>Encoding Scheme:</strong> Encodes groups of 4 bytes of binary data (32 bits) into 5 ASCII characters. A special shortcut &#39;z&#39; can represent four null bytes.</li>
<li><strong>Efficiency:</strong> The most efficient of these encodings, offering superior data density. It increases data size by only 25% (5 characters for 4 bytes).</li>
<li><strong>Human Readability:</strong> The least human-readable due to its wider range of punctuation characters, which can be problematic in some contexts.</li>
<li><strong>Use Cases:</strong> Commonly found in Adobe&#39;s PostScript and PDF file formats, and used by Git for encoding binary patches, where compactness is prioritized over human readability.</li>
</ul>
<h2>Comparison Summary</h2>
<table>
<thead>
<tr>
<th align="left">Feature</th>
<th align="left">Base32</th>
<th align="left">Base58</th>
<th align="left">Base62</th>
<th align="left">Base64</th>
<th align="left">Base85 (Ascii85)</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>Character Set</strong></td>
<td align="left">32 (A-Z, 2-7)</td>
<td align="left">58 (alphanumeric, excludes 0, O, I, l)</td>
<td align="left">62 (a-z, A-Z, 0-9)</td>
<td align="left">64 (A-Z, a-z, 0-9, +, /)</td>
<td align="left">85 (printable ASCII &#39;!&#39; to &#39;u&#39;)</td>
</tr>
<tr>
<td align="left"><strong>Encoding Ratio</strong></td>
<td align="left">5 bytes to 8 chars</td>
<td align="left">Variable</td>
<td align="left">Variable</td>
<td align="left">3 bytes to 4 chars</td>
<td align="left">4 bytes to 5 chars</td>
</tr>
<tr>
<td align="left"><strong>Efficiency</strong></td>
<td align="left">~60% overhead</td>
<td align="left">~25% more than Base64</td>
<td align="left">Good</td>
<td align="left">~33% overhead</td>
<td align="left">~25% overhead (most efficient)</td>
</tr>
<tr>
<td align="left"><strong>Human Readability</strong></td>
<td align="left">Good (case-insensitive, limited set)</td>
<td align="left">Excellent (avoids ambiguous chars)</td>
<td align="left">Good (alphanumeric only)</td>
<td align="left">Moderate (includes symbols, padding)</td>
<td align="left">Poor (many punctuation chars)</td>
</tr>
<tr>
<td align="left"><strong>URL-Safe</strong></td>
<td align="left">Yes</td>
<td align="left">Yes</td>
<td align="left">Yes</td>
<td align="left">No (requires variants for web)</td>
<td align="left">No</td>
</tr>
<tr>
<td align="left"><strong>Padding</strong></td>
<td align="left">Yes (typically &#39;=&#39;)</td>
<td align="left">No</td>
<td align="left">No</td>
<td align="left">Yes (typically &#39;=&#39;)</td>
<td align="left">No (can use &#39;z&#39; for null bytes)</td>
</tr>
<tr>
<td align="left"><strong>Key Use Cases</strong></td>
<td align="left">DNSSEC, QR codes, human-typed keys</td>
<td align="left">Cryptocurrency addresses, short URLs</td>
<td align="left">Short URLs, unique IDs</td>
<td align="left">Email (MIME), web APIs, embedding data</td>
<td align="left">PDF, PostScript, Git binary patches</td>
</tr>
</tbody></table>
<h2>Conclusion</h2>
<p>The choice of BaseXX encoding depends heavily on the specific requirements of your application. If human readability and error reduction during manual transcription are paramount, Base32 or Base58 might be your best bet. For compact, URL-safe identifiers, Base62 offers a compelling solution. Base64 remains the workhorse for general binary-to-text encoding in web and email contexts, while Base85 shines when maximum data density is the primary concern, even at the expense of human readability. Understanding these distinctions allows developers to select the most appropriate encoding method for their particular needs, optimizing for efficiency, safety, and usability.</p>
<p><a href="https://fezcode.com/blog/decoding-the-digital-alphabet-base-xx-encodings">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Fezcodex Stories with `git subtree`]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/fezcodex-stories-with-git-subtrees</link>
            <guid isPermaLink="false">https://fezcode.com/blog/fezcodex-stories-with-git-subtrees</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 14 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Fezcodex Stories with <code>git subtree</code></h1>
<p>Let&#39;s cover how we integrate <code>fezcodex.stories</code> repo to store and show our stories (dnd) section.</p>
<h2>Integrating External Content Seamlessly with Git Subtree: A Practical Guide</h2>
<p>In modern web development, it&#39;s common to need to incorporate content or even entire sub-projects from external Git repositories into your main project. Whether it&#39;s a shared library, documentation, or, as in our case, a collection of stories or blog posts, managing this external content efficiently is key. Git offers a couple of powerful tools for this: <code>git submodule</code> and <code>git subtree</code>.</p>
<p>While <code>git submodule</code> is excellent for managing distinct project dependencies, <code>git subtree</code> often shines when you want to integrate external content directly into your repository as if it were always part of it, especially when you need to easily pull updates. Let&#39;s dive into how <code>git subtree</code> can help you manage external content like your <code>fezcodex.stories</code> within your <code>public/stories</code> directory.</p>
<h3>Why Choose Git Subtree?</h3>
<p>When deciding between <code>git submodule</code> and <code>git subtree</code>, consider these advantages of <code>git subtree</code> for content integration:</p>
<ul>
<li><strong>Integrated History:</strong> The content of the external repository becomes a part of your main repository&#39;s history. This means anyone cloning your main repository gets all the content directly, without needing extra steps.</li>
<li><strong>Simpler Cloning:</strong> A regular <code>git clone</code> of your main repository will fetch all the subtree content. No special commands like <code>git submodule update --init --recursive</code> are required for collaborators.</li>
<li><strong>Easy Updates:</strong> Keeping your integrated content up-to-date with the original source is straightforward with a single <code>git subtree pull</code> command.</li>
<li><strong>No <code>.gitmodules</code>:</strong> <code>git subtree</code> doesn&#39;t introduce additional configuration files like <code>.gitmodules</code>, keeping your repository root cleaner.</li>
<li><strong>Works with Existing Tools:</strong> Since the content is fully integrated, all your existing Git tools and workflows (like <code>git grep</code>, <code>git log</code>) work seamlessly across your entire project, including the subtree content.</li>
</ul>
<h3>Setting Up Your Git Subtree: Step-by-Step</h3>
<p>Let&#39;s walk through the process of adding the <code>fezcodex.stories</code> repository into your <code>public/stories</code> directory.</p>
<h4>Prerequisites:</h4>
<p>Before you begin, ensure your working directory is clean. Git commands like <code>git subtree add</code> prefer a state where there are no uncommitted changes to prevent conflicts.</p>
<ul>
<li><strong>Check your status:</strong> Run <code>git status</code> to see if you have any pending changes.</li>
<li><strong>Commit or Stash:</strong> If you have modifications, either commit them (<code>git add . &amp;&amp; git commit -m &quot;WIP: Prepare for subtree addition&quot;</code>) or temporarily stash them (<code>git stash</code>).</li>
</ul>
<h4>Step 1: Add the External Repository as a Remote</h4>
<p>First, we&#39;ll add the external repository as a remote to your current Git project. This gives it a short, memorable name that you can use to reference it later.</p>
<p><strong>Explanation:</strong> This command tells your local Git repository about the existence of the <code>fezcodex.stories</code> repository and associates it with the name <code>fezcodex-stories</code>. This makes it easier to fetch from or push to this external repository without typing out the full URL every time.</p>
<p><strong>Command:</strong></p>
<pre><code class="language-bash">git remote add fezcodex-stories https://github.com/fezcode/fezcodex.stories
</code></pre>
<h4>Step 2: Add the Remote as a Subtree</h4>
<p>Now, we&#39;ll integrate the content from the <code>fezcodex-stories</code> remote into a specific directory within your project (<code>public/stories</code>).</p>
<p><strong>Explanation:</strong></p>
<ul>
<li><code>git subtree add</code>: This is the core command to add a subtree.</li>
<li><code>--prefix public/stories</code>: This specifies the local directory within your main project where the content from the external repository will reside. Git will create this directory if it doesn&#39;t exist.</li>
<li><code>fezcodex-stories</code>: This is the name of the remote you defined in Step 1.</li>
<li><code>main</code>: This indicates the branch from the <code>fezcodex-stories</code> remote that you want to pull. <strong>Important:</strong> Double-check the default branch name of the external repository (it might be <code>master</code> instead of <code>main</code>).</li>
<li><code>--squash</code>: This option is highly recommended. It squashes all the commits from the external repository&#39;s history into a single commit when adding it to your main repository. This keeps your main project&#39;s commit history cleaner, preventing it from being flooded with potentially hundreds of commits from the external source.</li>
</ul>
<p><strong>Command:</strong></p>
<pre><code class="language-bash">git subtree add --prefix public/stories fezcodex-stories main --squash
</code></pre>
<h3>Managing Your Git Subtree</h3>
<p>Once your subtree is set up, here&#39;s how you&#39;ll typically interact with it.</p>
<h4>Pulling Updates from the Subtree Source</h4>
<p>The primary reason for using <code>git subtree</code> for content is to easily keep it updated. When the original <code>fezcodex.stories</code> repository has new content, you can pull those changes into your project.</p>
<p><strong>Explanation:</strong> This command is very similar to the <code>add</code> command, but <code>pull</code> fetches the latest changes from the specified remote and branch, and then merges them into your local subtree directory. The <code>--squash</code> option again helps to keep your history tidy by squashing the incoming changes into a single merge commit.</p>
<p><strong>Command:</strong></p>
<pre><code class="language-bash">git subtree pull --prefix public/stories fezcodex-stories main --squash
</code></pre>
<h4>Making Changes within the Subtree and Pushing Back (Optional)</h4>
<p>Sometimes, you might make modifications to the files within your <code>public/stories</code> directory (the subtree content) and wish to contribute those changes back to the original <code>fezcodex.stories</code> repository.</p>
<p><strong>Explanation:</strong></p>
<ul>
<li>First, commit your changes in your main repository as you normally would.</li>
<li>Then, use <code>git subtree push</code>. This command takes the commits related to your <code>public/stories</code> directory and pushes them to the <code>main</code> branch of the <code>fezcodex-stories</code> remote.</li>
<li><strong>Important:</strong> You must have push access to the original <code>https://github.com/fezcode/fezcodex.stories</code> repository for this to work. If you don&#39;t, you&#39;d typically fork the original repository, push to your fork, and then open a pull request.</li>
</ul>
<p><strong>Command:</strong></p>
<pre><code class="language-bash">git subtree push --prefix public/stories fezcodex-stories main
</code></pre>
<h4>Removing a Git Subtree (If Needed)</h4>
<p>If you ever need to remove the subtree, it&#39;s a multi-step process:</p>
<p><strong>Explanation:</strong></p>
<ol>
<li><code>git rm -r public/stories</code>: This removes the directory and its contents from your working tree and stages the deletion.</li>
<li><code>git commit -m &quot;Remove subtree public/stories&quot;</code>: Commits the removal.</li>
<li><code>git remote rm fezcodex-stories</code>: Removes the remote reference you added earlier.</li>
<li>You might also want to clean up any leftover Git configuration related to the subtree, though <code>git remote rm</code> handles the main part.</li>
</ol>
<p><strong>Commands:</strong></p>
<pre><code class="language-bash">git rm -r public/stories
git commit -m &quot;Remove subtree public/stories&quot;
git remote rm fezcodex-stories
</code></pre>
<h3>Conclusion</h3>
<p><code>git subtree</code> provides a robust and integrated way to manage external content within your main Git repository. It simplifies collaboration by making external content directly available upon cloning and streamlines the update process. By following these steps, you can effectively incorporate and maintain your <code>fezcodex.stories</code> content, or any other external project, within your <code>public/stories</code> directory.</p>
<p><a href="https://fezcode.com/blog/fezcodex-stories-with-git-subtrees">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Publishing to NPM]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/publish-to-npm</link>
            <guid isPermaLink="false">https://fezcode.com/blog/publish-to-npm</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Thu, 13 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>How to Publish a Node.js Library to npm</h1>
<p>This document outlines the steps taken to publish the <code>piml.js</code> library to the npm registry.</p>
<h2>1. Initial Setup and Conversion</h2>
<ul>
<li>Creating a <code>piml.js</code> file to house the JavaScript library.</li>
<li>Creating a <code>piml.test.js</code> file to test the JavaScript library.</li>
</ul>
<h2>2. Setting up the Node.js Project</h2>
<p>To prepare the project for npm, the following steps were taken:</p>
<ul>
<li><p><strong><code>package.json</code>:</strong> A <code>package.json</code> file was created to manage the project&#39;s metadata and dependencies. It was populated with the following information:</p>
<ul>
<li><code>name</code>: The name of the package on npm (e.g., &quot;piml&quot;).</li>
<li><code>version</code>: The initial version of the package (e.g., &quot;1.0.0&quot;).</li>
<li><code>description</code>: A brief description of the package.</li>
<li><code>main</code>: The entry point of the package (e.g., &quot;piml.js&quot;).</li>
<li><code>scripts</code>: A &quot;test&quot; script to run the tests using Jest.</li>
<li><code>keywords</code>: Keywords to help users find the package on npm.</li>
<li><code>author</code>: The author of the package.</li>
<li><code>license</code>: The license of the package (e.g., &quot;MIT&quot;).</li>
<li><code>devDependencies</code>: The development dependencies, such as <code>jest</code>.</li>
</ul>
</li>
<li><p><strong><code>.gitignore</code>:</strong> A <code>.gitignore</code> file was created to prevent unnecessary files from being committed to the repository, such as <code>node_modules</code>, logs, and system files.</p>
</li>
<li><p><strong>Dependencies Installation:</strong> The development dependencies were installed by running <code>npm install</code>.</p>
</li>
</ul>
<h2>3. Testing</h2>
<p>With the project set up, the tests were run to ensure the library was working correctly:</p>
<pre><code class="language-bash">npm test
</code></pre>
<p>Any failing tests were debugged and fixed until all tests passed.</p>
<h2>4. Publishing to npm</h2>
<p>Once the library was tested and ready, the following steps were taken to publish it to npm:</p>
<ol>
<li><p><strong>Create an npm Account:</strong> An npm account is required to publish packages. You can create one at <a href="https://www.npmjs.com/signup">https://www.npmjs.com/signup</a>.</p>
</li>
<li><p><strong>Log in to npm:</strong> From the command line, you need to log in to your npm account:</p>
<pre><code class="language-bash">npm login
</code></pre>
<p>You will be prompted to enter your npm username, password, and email address.</p>
</li>
<li><p><strong>Check Package Name Availability:</strong> Before publishing, it&#39;s a good practice to check if the desired package name is available. This can be done by running:</p>
<pre><code class="language-bash">npm view &lt;package-name&gt;
</code></pre>
<p>If the package exists, you will see information about it. If it doesn&#39;t, you will get a 404 error, which means the name is available.</p>
</li>
<li><p><strong>Publish the Package:</strong> To publish the package, run the following command from the project&#39;s root directory:</p>
<pre><code class="language-bash">npm publish
</code></pre>
<p>If the package name is scoped (e.g., <code>@username/package-name</code>), you need to use the <code>--access public</code> flag:</p>
<pre><code class="language-bash">npm publish --access public
</code></pre>
</li>
<li><p><strong>Verify the Package:</strong> After publishing, you can verify that the package is available on npm by visiting <code>https://www.npmjs.com/package/&lt;your-package-name&gt;</code>.</p>
</li>
</ol>
<p>By following these steps, the <code>piml.js</code> library was successfully published to the npm registry.</p>
<p><a href="https://fezcode.com/blog/publish-to-npm">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Parenthesis Intended Markup Language]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/piml</link>
            <guid isPermaLink="false">https://fezcode.com/blog/piml</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 12 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>piml</h1>
<p><strong>Spec version</strong>: v1.1.0</p>
<p><a href="https://github.com/fezcode/piml/blob/main/LIBRARIES.md">Available Libraries</a></p>
<p><a href="/apps::jpc">JSON&lt;-&gt;PIML Converter</a></p>
<h2>Parenthesis Intended Markup Language</h2>
<p>In the ever-evolving landscape of data serialization formats, PIML (Parenthesis Intended Markup Language) emerges as a compelling alternative, prioritizing human readability and writability without compromising machine parseability. This post delves into the core tenets of PIML, exploring its syntax, data types, and how it stacks up against established formats like JSON, YAML, and TOML.</p>
<h2>What is PIML?</h2>
<p>PIML is a data serialization format designed for clarity and ease of use by both humans and machines. It leverages a unique <code>(key)</code> syntax and indentation-based nesting to create a visually intuitive representation of structured data. Conceived as a middle ground between the verbosity of JSON and the potential ambiguity of YAML, PIML aims to offer a clean, unambiguous, and highly readable format for various data exchange and configuration needs.</p>
<h2>Syntax Rules: The Building Blocks of PIML</h2>
<p>PIML&#39;s syntax is intentionally minimal, focusing on consistency and clarity.</p>
<h3>Keys</h3>
<p>Keys are the identifiers for data elements and are always enclosed in parentheses. This explicit demarcation makes keys instantly recognizable.</p>
<pre><code class="language-piml">(my_key) my_value
(another key with spaces) another_value
</code></pre>
<h3>Indentation</h3>
<p>Indentation is fundamental to PIML&#39;s structure, defining hierarchical relationships between data elements.</p>
<ul>
<li><strong>Recommendation:</strong> Use <strong>2 spaces</strong> for each level of indentation to maintain visual consistency.</li>
<li><strong>Strict Rule:</strong> Mixing tabs and spaces for indentation is <strong>prohibited</strong> to prevent parsing ambiguities.</li>
</ul>
<h3>Comments</h3>
<p>PIML supports single-line comments using the <code>#</code> symbol. Anything from <code>#</code> to the end of the line is ignored by parsers, allowing for clear inline documentation.</p>
<ul>
<li><strong>Rule:</strong> Only lines that <em>start</em> with <code>#</code> are treated as comments. Inline comments (e.g., <code>(key) value # comment</code>) are not supported and will be considered part of the value.</li>
</ul>
<pre><code class="language-piml"># This explains the data
(data) value # This entire line is the value, not a comment
</code></pre>
<h3>Escaping</h3>
<p>The backslash (<code>\</code>) character is used to escape special characters within string values, ensuring that characters like <code>(</code> or <code>#</code> can be part of the data itself.</p>
<ul>
<li>Common escapes include <code>\n</code> (newline), <code>\t</code> (tab), and <code>\\</code> (literal backslash).</li>
<li>Example: <code>(title) My \(Awesome\) Title</code></li>
<li>To include a <code>#</code> character at the beginning of a line within a multi-line string, escape it with a backslash (<code>\</code>), e.g., <code>\# This is not a comment</code>.</li>
</ul>
<h2>Data Types: Representing Information in PIML</h2>
<p>PIML supports a range of data types, from simple primitives to complex nested structures.</p>
<h3>Primitive Types</h3>
<ul>
<li><strong>Single-line Strings:</strong> Unquoted text values that reside on a single line.<pre><code class="language-piml">(name) John Doe
</code></pre>
</li>
<li><strong>Integers:</strong> Whole numbers.<pre><code class="language-piml">(age) 30
</code></pre>
</li>
<li><strong>Floats:</strong> Decimal numbers.<pre><code class="language-piml">(price) 19.99
</code></pre>
</li>
<li><strong>Booleans:</strong> Logical values, represented as <code>true</code> or <code>false</code>.<pre><code class="language-piml">(is_active) true
</code></pre>
</li>
</ul>
<h3>Null and Empty Representations: The <code>nil</code> Unifier</h3>
<p>One of PIML&#39;s distinctive features is the use of <code>nil</code> as a unified representation for three distinct states:</p>
<ol>
<li><strong>Null:</strong> The absence of a value.</li>
<li><strong>Empty Array:</strong> An empty list.</li>
<li><strong>Empty Object:</strong> An empty map.</li>
</ol>
<pre><code class="language-piml">(optional_setting) nil
(empty_items) nil
(empty_config) nil
</code></pre>
<p>This design choice prioritizes syntactic simplicity, though it means the specific <em>type</em> of an empty collection (array vs. object) is not preserved when <code>nil</code> is used.</p>
<h3>Multi-line Strings</h3>
<p>For text spanning multiple lines, PIML allows the content to start on the next indented line, preserving newlines.</p>
<pre><code class="language-piml">(description)
    This is a multi-line string example.
    It can hold extensive textual content.
</code></pre>
<h3>Arrays (Lists)</h3>
<p>Ordered collections of items are denoted by a <code>&gt;</code> prefix on each indented item.</p>
<pre><code class="language-piml">(fruits)
    &gt; apple
    &gt; banana
    &gt; orange
</code></pre>
<h3>Sets (Unique, Unordered Collections)</h3>
<p>PIML introduces <code>&gt;|</code> for sets, which are collections of unique, unordered items. Duplicate values are ignored by parsers.</p>
<pre><code class="language-piml">(unique_ids)
    &gt;| id_a
    &gt;| id_b
    &gt;| id_a # This duplicate will be ignored
</code></pre>
<h3>Objects (Maps)</h3>
<p>Unordered key-value pairs are defined through indentation, creating nested structures.</p>
<pre><code class="language-piml">(user)
    (name) Alice
    (email) alice@example.com
</code></pre>
<h4>List of Objects</h4>
<p>PIML provides a clear way to represent lists of objects, combining the array <code>&gt;</code> marker with nested object syntax. The key within the object (e.g., <code>(contributor)</code>) serves as metadata for readability and is ignored by parsers.</p>
<pre><code class="language-piml">(contributors)
    &gt; (contributor)
        (id) 1
        (name) Bob
    &gt; (contributor)
        (id) 2
        (name) Carol
</code></pre>
<h3>Specialized Types (by Convention)</h3>
<p>PIML encourages conventions for specialized data:</p>
<ul>
<li><strong>Dates/Times:</strong> Typically represented as strings in ISO 8601 format (e.g., <code>2023-10-27T10:30:00Z</code>).</li>
<li><strong>Binary Data:</strong> Usually encoded as base64 strings.</li>
</ul>
<h2>PIML in Action: A Comprehensive Example</h2>
<p>Let&#39;s look at a more complete example demonstrating various PIML features:</p>
<pre><code class="language-piml">(document_metadata)
    (title) PIML Specification Document
    (version) 1.0.0
    (author) Fezcodex
    (creation_date) 2025-11-12T10:00:00Z
    (is_draft) true
    (tags)
        &gt; data-format
        &gt; serialization
        &gt; piml
    (abstract)
        This document outlines the PIML format,
        its syntax, and its design philosophy.
        It aims for human-centric data representation.
    (contact)
        (email) contact@example.com
        (website) [https://fezcode.github.io](https://fezcode.github.io)
    (empty_settings) nil

(configuration)
    (database)
        (type) SQLite
        (path) /data/app.db
        (max_connections) 50
    (api_keys)
        &gt;| key_abc
        &gt;| key_xyz
    (feature_toggles)
        (new_ui) true
        (beta_analytics) false
</code></pre>
<h2>PIML vs. The World: A Comparison</h2>
<p>PIML carves its niche by offering a distinct balance of features compared to other popular data formats.</p>
<h3>PIML vs. JSON</h3>
<ul>
<li><strong>PIML Advantages:</strong><ul>
<li><strong>Readability:</strong> Significantly less visual noise due to the absence of quotes for keys and single-line strings, and no mandatory commas.</li>
<li><strong>Comments:</strong> Native support for <code>#</code> comments, a feature lacking in JSON.</li>
<li><strong>Multi-line Strings:</strong> More natural and cleaner syntax for multi-line text.</li>
</ul>
</li>
<li><strong>PIML Trade-off:</strong><ul>
<li><strong><code>nil</code> Ambiguity:</strong> The unified <code>nil</code> for null, empty arrays, and empty objects simplifies syntax but means PIML cannot perfectly distinguish between these empty collection types, potentially affecting round-trip conversions from JSON.</li>
</ul>
</li>
</ul>
<h3>PIML vs. YAML</h3>
<ul>
<li><strong>PIML Advantages:</strong><ul>
<li><strong>Simplicity &amp; Predictability:</strong> PIML&#39;s syntax is much smaller and more constrained, avoiding the &quot;many ways to do one thing&quot; complexity and the subtle whitespace pitfalls often associated with YAML. The explicit <code>(key)</code> syntax reduces ambiguity.</li>
</ul>
</li>
<li><strong>PIML Trade-off:</strong><ul>
<li><strong>Feature Set:</strong> PIML deliberately omits advanced YAML features like anchors, aliases, and complex document structures, focusing on a simpler, more direct representation.</li>
</ul>
</li>
</ul>
<h3>PIML vs. TOML</h3>
<ul>
<li><strong>PIML Advantages:</strong><ul>
<li><strong>Nesting Flexibility:</strong> PIML&#39;s indentation-based nesting is more adaptable for arbitrarily deep and complex data structures, contrasting with TOML&#39;s more rigid <code>[table]</code> and <code>[[array of tables]]</code> approach.</li>
</ul>
</li>
<li><strong>PIML Trade-off:</strong><ul>
<li><strong>Configuration Focus:</strong> TOML excels as a configuration file format due to its flat, key-value pair nature. PIML is more general-purpose, though it can certainly be used for configuration.</li>
</ul>
</li>
</ul>
<h2>Conclusion</h2>
<p>PIML offers a refreshing perspective on data serialization, emphasizing human-centric design while maintaining machine parseability. Its explicit key syntax, indentation-driven structure, and thoughtful approach to data types make it a strong contender for scenarios where clarity, readability, and ease of writing are paramount. As data continues to grow in complexity, formats like PIML provide valuable alternatives for developers seeking more intuitive ways to manage and exchange information.</p>
<p><a href="https://fezcode.com/blog/piml">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Image Toolkit Deep Dive]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/image-toolkit-deep-dive</link>
            <guid isPermaLink="false">https://fezcode.com/blog/image-toolkit-deep-dive</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Mon, 10 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Image Toolkit Deep Dive</h1>
<p>In this blog post, we&#39;ll take a deep dive into the implementation of the Image Toolkit app. We&#39;ll explore the various image filters and their algorithms, and we&#39;ll also discuss a common React Hook-related warning and how to fix it.</p>
<p>You can try it here <a href="/apps::itk">apps::itk</a></p>
<h2>The Filters</h2>
<p>The Image Toolkit app provides a variety of filters that you can apply to your images. Let&#39;s take a look at each one and the algorithm behind it.</p>
<h3>Monochrome</h3>
<p>The monochrome filter converts an image to grayscale. The algorithm for this is quite simple. For each pixel in the image, we calculate the average of the red, green, and blue values. Then, we set the red, green, and blue values of the pixel to this average value.</p>
<pre><code class="language-javascript">const avg = (data[i] + data[i + 1] + data[i + 2]) / 3;
data[i] = avg; // red
data[i + 1] = avg; // green
data[i + 2] = avg; // blue
</code></pre>
<h3>Blur</h3>
<p>The blur filter applies a blur effect to the image. We use the <code>stackblur-canvas</code> library to achieve this effect. The <code>canvasRGBA</code> function from this library takes the canvas, the coordinates of the area to blur, and the blur radius as input.</p>
<pre><code class="language-javascript">canvasRGBA(canvas, 0, 0, canvas.width, canvas.height, blurAmount);
</code></pre>
<h3>Dithering</h3>
<p>Dithering is a technique used to create the illusion of more colors than are actually available. We use the Bayer dithering algorithm. This algorithm uses a threshold map (the Bayer matrix) to determine whether a pixel should be black or white.</p>
<pre><code class="language-javascript">const bayerMatrix = [
  [1, 9, 3, 11],
  [13, 5, 15, 7],
  [4, 12, 2, 10],
  [16, 8, 14, 6]
];
const threshold = bayerMatrix[y % matrixSize][x % matrixSize] * 16;
const newValue = gray &lt; threshold ? 0 : 255;
</code></pre>
<h3>Cel Shading</h3>
<p>Cel shading is a non-photorealistic rendering technique that makes 3D computer graphics appear to be flat. To achieve this effect, we first apply color quantization to reduce the number of colors in the image. Then, we use the Sobel operator to detect the edges in the image. Finally, we combine the quantized image and the edges to create the cel-shaded effect.</p>
<h3>Halftone</h3>
<p>The halftone filter simulates the effect of printing an image with a series of dots. We first convert the image to grayscale. Then, for each grid of pixels, we calculate the average brightness and draw a circle with a radius proportional to the brightness.</p>
<h3>Solarization</h3>
<p>Solarization is an effect where the image is partially reversed. We set a threshold and for each pixel, if the color component is less than the threshold, we invert it.</p>
<pre><code class="language-javascript">if (r &lt; threshold) data[i] = 255 - r;
if (g &lt; threshold) data[i + 1] = 255 - g;
if (b &lt; threshold) data[i + 2] = 255 - b;
</code></pre>
<h3>Posterization</h3>
<p>Posterization is a process in which the number of colors in an image is reduced. For each color component of a pixel, we round it to the nearest value in a smaller set of values.</p>
<h3>Sepia</h3>
<p>The sepia filter gives the image a warm, brownish tone. We use a set of coefficients to calculate the new red, green, and blue values for each pixel.</p>
<pre><code class="language-javascript">data[i] = Math.min(255, (r * 0.393) + (g * 0.769) + (b * 0.189));
data[i + 1] = Math.min(255, (r * 0.349) + (g * 0.686) + (b * 0.168));
data[i + 2] = Math.min(255, (r * 0.272) + (g * 0.534) + (b * 0.131));
</code></pre>
<h3>Pixelization</h3>
<p>The pixelization filter creates a blocky, pixelated effect. We divide the image into a grid of blocks and fill each block with the color of the top-left pixel in that block.</p>
<h3>Duotone</h3>
<p>The duotone filter uses two colors to create a two-toned image. We first convert the image to grayscale. Then, we interpolate between a dark color and a light color based on the brightness of each pixel.</p>
<h3>ASCII Art</h3>
<p>The ASCII art filter converts the image to ASCII characters. We first convert the image to grayscale. Then, for each pixel, we map its brightness to a character from a character ramp.</p>
<pre><code class="language-javascript">const ascii = asciiArt(imageData, &#39;@%#*+=-:. &#39;);
</code></pre>
<h2>How It Works: A Deeper Look</h2>
<p>Here&#39;s a breakdown of how the Image Toolkit is built, covering the React structure, canvas manipulation, and the filter algorithms.</p>
<h3>1. React Component Structure</h3>
<p>The <code>ImageToolkitPage</code> is a functional React component that uses hooks to manage its state and behavior:</p>
<ul>
<li><strong><code>useState</code></strong>: This hook manages the application&#39;s state:<ul>
<li><code>image</code>: Stores the uploaded image as a data URL.</li>
<li><code>activeEffect</code>: Tracks the currently selected filter.</li>
<li><code>blurAmount</code>: Holds the value for the blur filter&#39;s intensity.</li>
<li><code>asciiArtOutput</code>: Stores the generated ASCII art string.</li>
</ul>
</li>
<li><strong><code>useRef</code></strong>: This provides direct references to the <code>&lt;canvas&gt;</code> and the original <code>&lt;img&gt;</code> elements in the DOM.</li>
<li><strong><code>useEffect</code></strong>: This is the core of the image processing. It runs whenever the <code>image</code> or <code>activeEffect</code> state changes, triggering the drawing and filtering logic.</li>
<li><strong><code>useToast</code></strong>: This is a custom hook used to display toast notifications for actions like copying text.</li>
</ul>
<h3>2. Image Upload</h3>
<p>When you click &quot;Select Image,&quot; the <code>handleImageUpload</code> function is triggered. It uses the browser&#39;s <code>FileReader</code> API to read the selected image file and convert it into a data URL. This data URL is then stored in the <code>image</code> state, which causes the component to re-render and display the uploaded image.</p>
<h3>3. Canvas and <code>useEffect</code></h3>
<p>The <code>useEffect</code> hook orchestrates the image manipulation:</p>
<ol>
<li>It waits for an <code>image</code> to be present and for the <code>canvasRef</code> to be attached to the canvas element.</li>
<li>It gets the 2D rendering context of the canvas (<code>ctx</code>).</li>
<li>An <code>Image</code> object is created, and its <code>src</code> is set to the data URL of the uploaded image.</li>
<li>In the image&#39;s <code>onload</code> event, the original image is drawn onto the canvas using <code>ctx.drawImage()</code>.</li>
<li>A series of <code>if/else if</code> statements checks the <code>activeEffect</code> state. Based on which filter is active, it calls the corresponding function to manipulate the image on the canvas.</li>
</ol>
<h3>4. Filter Implementation: Pixel-by-Pixel Manipulation</h3>
<p>The magic of the filters happens by directly manipulating the pixel data of the canvas.</p>
<ol>
<li><strong><code>getImageData()</code></strong>: To get the pixel data, we call <code>ctx.getImageData()</code>. This returns an <code>ImageData</code> object.</li>
<li><strong><code>ImageData.data</code></strong>: This object contains a <code>data</code> property, which is a <code>Uint8ClampedArray</code>. This array is a flat list of RGBA (Red, Green, Blue, Alpha) values for every pixel in the image. For example, the first four values in the array (<code>data[0]</code> to <code>data[3]</code>) represent the RGBA of the very first pixel.</li>
<li><strong>Manipulation</strong>: Each filter&#39;s algorithm iterates through this <code>data</code> array and modifies the R, G, and B values according to its logic.</li>
<li><strong><code>putImageData()</code></strong>: After the <code>data</code> array has been modified, <code>ctx.putImageData()</code> is called to draw the new pixel data back onto the canvas, displaying the filtered image.</li>
</ol>
<p>For the <strong>Blur</strong> filter specifically, the <code>stackblur-canvas</code> library is used. It provides a highly optimized and performant blur algorithm that is much faster than a manual implementation.</p>
<h3>5. UI and Event Handling</h3>
<p>The user interface is built with standard React components and styled using <strong>Tailwind CSS</strong> for a clean and modern look.</p>
<ul>
<li><strong>Filter Buttons</strong>: Each filter button has an <code>onClick</code> event handler (e.g., <code>handleConvertToMonochrome</code>). When clicked, this handler updates the <code>activeEffect</code> state with the name of the filter. This state change triggers the <code>useEffect</code> hook, which then applies the selected filter&#39;s logic to the canvas.</li>
<li><strong>Download Button</strong>: The &quot;Download Image&quot; button creates a temporary <code>&lt;a&gt;</code> (link) element. Its <code>href</code> is set to the canvas&#39;s current content as a data URL (<code>canvas.toDataURL()</code>), and the <code>download</code> attribute is set. The link is then programmatically &quot;clicked&quot; to initiate the download.</li>
<li><strong>Copy Button</strong>: The &quot;Copy&quot; button for the ASCII art uses the modern <code>navigator.clipboard.writeText()</code> API to easily copy the generated ASCII string to the user&#39;s clipboard.</li>
</ul>
<h2>The <code>useCallback</code> and <code>useEffect</code> Dependency Array Error</h2>
<p>You might have encountered this warning while developing the Image Toolkit app:</p>
<pre><code>The &#39;toGrayscale&#39; function makes the dependencies of useEffect Hook (at line 348) change on every render. To fix this, wrap the definition of &#39;toGrayscale&#39; in its own useCallback() Hook
</code></pre>
<p>This warning occurs because the <code>toGrayscale</code> function is defined inside the <code>ImageToolkitPage</code> component. This means that on every render of the component, a new <code>toGrayscale</code> function is created. Since <code>toGrayscale</code> is a dependency of the <code>useEffect</code> hook, the hook will run on every render, causing an infinite loop.</p>
<p>To fix this, we can wrap the definition of <code>toGrayscale</code> in its own <code>useCallback</code> hook. The <code>useCallback</code> hook will memoize the function, so that it is not recreated on every render.</p>
<pre><code class="language-javascript">const toGrayscale = useCallback((imageData) =&gt; {
  // ...
}, []);
</code></pre>
<p>By wrapping all the image processing functions in <code>useCallback</code>, we can prevent the <code>useEffect</code> hook from running on every render and fix the infinite loop.</p>
<p><a href="https://fezcode.com/blog/image-toolkit-deep-dive">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Picker Wheel Deep Dive]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/picker-wheel-deep-dive</link>
            <guid isPermaLink="false">https://fezcode.com/blog/picker-wheel-deep-dive</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sun, 09 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>A Deep Dive into the Picker Wheel: Canvas, React, and CSS</h1>
<p>In this post, we&#39;ll take a deep dive into the implementation of the Picker Wheel app, a fun and interactive way to pick a random winner from a list of entries. We&#39;ll explore how it&#39;s built using React, the Canvas API, and Tailwind CSS, and we&#39;ll cover the key concepts and techniques used in its development.</p>
<p>You can play with it here <a href="/apps::pw">apps::pw</a></p>
<h2>The Canvas Wheel</h2>
<p>The heart of the Picker Wheel is the wheel itself, which is drawn using the HTML5 Canvas API. The canvas provides a powerful and flexible way to draw graphics and animations, and it&#39;s perfect for creating the dynamic and interactive wheel we need.</p>
<p>The <code>drawWheel</code> function is responsible for drawing the wheel. It takes the list of entries and the current rotation angle as input, and it uses them to draw the segments of the wheel. Each segment is a pie slice, and it&#39;s filled with a unique color from a predefined color palette. The text of the entry is then drawn on top of the segment, rotated to align with the segment&#39;s angle.</p>
<pre><code class="language-javascript">// This function is responsible for drawing the wheel on the canvas.
const drawWheel = () =&gt; {
  // Get a reference to the canvas element.
  const canvas = canvasRef.current;
  // If the canvas element doesn&#39;t exist, do nothing.
  if (!canvas) return;
  // Get the 2D rendering context for the canvas.
  const ctx = canvas.getContext(&#39;2d&#39;);
  // Get the width and height of the canvas.
  const { width, height } = canvas;
  // Calculate the size of each arc (segment) of the wheel.
  const arc = 2 * Math.PI / (entries.length || 1);

  // Clear the canvas before drawing.
  ctx.clearRect(0, 0, width, height);
  // Save the current state of the canvas context.
  ctx.save();
  // Move the origin of the canvas to the center.
  ctx.translate(width / 2, height / 2);
  // Rotate the canvas by the current rotation angle.
  ctx.rotate(rotation);
  // Move the origin back to the top-left corner.
  ctx.translate(-width / 2, -height / 2);

  // Loop through each entry and draw a segment for it.
  for (let i = 0; i &lt; entries.length; i++) {
    // Calculate the angle of the current segment.
    const angle = i * arc;
    // Set the fill style to a color from the color palette.
    ctx.fillStyle = colorPalette[i % colorPalette.length];
    // Begin a new path.
    ctx.beginPath();
    // Draw the outer arc of the segment.
    ctx.arc(width / 2, height / 2, width / 2 - 10, angle, angle + arc);
    // Draw the inner arc of the segment.
    ctx.arc(width / 2, height / 2, 0, angle + arc, angle, true);
    // Fill the segment with the current fill style.
    ctx.fill();

    // Save the current state of the canvas context.
    ctx.save();
    // Set the fill style for the text.
    ctx.fillStyle = &#39;#000&#39;;
    // Set the font for the text.
    ctx.font = &#39;30px Arial&#39;;
    // Move the origin to the center of the segment.
    ctx.translate(width / 2 + Math.cos(angle + arc / 2) * (width / 2 - 80), height / 2 + Math.sin(angle + arc / 2) * (height / 2 - 80));
    // Rotate the canvas to align the text with the segment.
    ctx.rotate(angle + arc / 2 + Math.PI / 2);
    // Get the text for the current entry.
    const text = entries[i];
    // Draw the text on the canvas.
    ctx.fillText(text, -ctx.measureText(text).width / 2, 0);
    // Restore the canvas context to its previous state.
    ctx.restore();
  }
  // Restore the canvas context to its previous state.
  ctx.restore();
};
</code></pre>
<h2>React Hooks</h2>
<p>The Picker Wheel app is built using React, and it makes extensive use of React Hooks to manage its state and side effects.</p>
<ul>
<li><strong><code>useState</code>:</strong> The <code>useState</code> hook is used to manage the component&#39;s state, including the list of entries, the new entry input, the winner, and the spinning state.</li>
<li><strong><code>useRef</code>:</strong> The <code>useRef</code> hook is used to get a reference to the canvas element, which is needed for drawing the wheel. It&#39;s also used to store the ID of the animation frame, which is used to cancel the animation when the component unmounts.</li>
<li><strong><code>useEffect</code>:</strong> The <code>useEffect</code> hook is used to draw the wheel whenever the list of entries or the rotation angle changes.</li>
</ul>
<h2>Timers and Animation</h2>
<p>The spinning animation is created using a combination of <code>requestAnimationFrame</code> and an easing function. The <code>requestAnimationFrame</code> function provides a smooth and efficient way to create animations, and the easing function is used to create the &quot;fast spin then slow down&quot; effect.</p>
<p>The <code>spin</code> function is responsible for starting the animation. It calculates the start and end rotation angles, and then it uses <code>requestAnimationFrame</code> to update the rotation angle on each frame of the animation. The easing function is used to calculate the new rotation angle based on the progress of the animation.</p>
<pre><code class="language-javascript">// This function creates a &quot;fast spin then slow down&quot; effect.
const easeOut = (t) =&gt; 1 - Math.pow(1 - t, 3);

// This function starts the spinning animation.
const spin = () =&gt; {
  // Only spin if there are more than one entry and the wheel is not already spinning.
  if (entries.length &gt; 1 &amp;&amp; !spinning) {
    // Set the spinning state to true.
    setSpinning(true);
    // Clear the winner.
    setWinner(null);
    // Set the duration of the animation.
    const duration = 7000;
    // Get the start time of the animation.
    const startTime = performance.now();
    // Get the start rotation of the wheel.
    const startRotation = rotation;
    // Calculate a random number of spins.
    const randomSpins = Math.random() * 5 + 5;
    // Calculate the end rotation of the wheel.
    const endRotation = startRotation + randomSpins * 2 * Math.PI;

    // This function is called on each frame of the animation.
    const animate = (currentTime) =&gt; {
      // Calculate the elapsed time.
      const elapsedTime = currentTime - startTime;
      // Calculate the progress of the animation.
      const progress = Math.min(elapsedTime / duration, 1);
      // Calculate the eased progress of the animation.
      const easedProgress = easeOut(progress);

      // Calculate the new rotation of the wheel.
      const newRotation = startRotation + (endRotation - startRotation) * easedProgress;
      // Set the new rotation of the wheel.
      setRotation(newRotation);

      // If the animation is not finished, request another frame.
      if (progress &lt; 1) {
        animationFrameId.current = requestAnimationFrame(animate);
      } else {
        // ...
      }
    };

    // Start the animation.
    animationFrameId.current = requestAnimationFrame(animate);
  }
};
</code></pre>
<h2>CSS and Styling</h2>
<p>The Picker Wheel app is styled using a combination of CSS and Tailwind CSS. The CSS is used to style the wheel and the pin, and Tailwind CSS is used for the layout and the rest of the styling.</p>
<p>The wheel is styled with a border and a box shadow to give it a 3D look. The pin is a simple triangle created with CSS borders.</p>
<pre><code class="language-css">/* Style for the wheel */
.wheel {
  border-radius: 50%;
  border: 5px solid #333;
  box-shadow: 0 0 20px rgba(0,0,0,0.5);
}

/* Style for the pin */
.pin {
  position: absolute;
  top: -20px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 15px solid transparent;
  border-right: 15px solid transparent;
  border-top: 30px solid #333;
  z-index: 10;
}
</code></pre>
<h2>Event Handling</h2>
<p>The Picker Wheel app uses a variety of event handlers to respond to user input.</p>
<ul>
<li><strong><code>onClick</code>:</strong> The <code>onClick</code> event handler is used to handle clicks on the &quot;Spin&quot;, &quot;Add&quot;, &quot;Delete&quot;, and &quot;Load from List&quot; buttons.</li>
<li><strong><code>onChange</code>:</strong> The <code>onChange</code> event handler is used to handle changes to the new entry input field.</li>
<li><strong><code>onKeyDown</code>:</strong> The <code>onKeyDown</code> event handler is used to handle key presses on the new entry input field. When the &quot;Enter&quot; key is pressed, the <code>addEntry</code> function is called.</li>
</ul>
<h2>Reading from a List</h2>
<p>The &quot;Load from List&quot; feature allows users to paste a list of entries, which are then added to the picker wheel. The list is parsed using the newline character as a delimiter, and the entries are added to the list of entries. The number of entries is limited to 30.</p>
<pre><code class="language-javascript">// This function is called when the user saves a list of entries from the modal.
const handleSaveList = (list) =&gt; {
  // Split the list into an array of entries, using the newline character as a delimiter.
  const newEntries = list.split(&#39;\n&#39;).map(entry =&gt; entry.trim()).filter(entry =&gt; entry);
  // Add the new entries to the existing list of entries, and limit the total number of entries to 30.
  setEntries([...entries, ...newEntries].slice(0, 30));
};
</code></pre>
<h2>Tailwind CSS</h2>
<p>The Picker Wheel app uses Tailwind CSS for its layout and styling. Tailwind CSS is a utility-first CSS framework that provides a set of low-level utility classes that can be used to build custom designs.</p>
<p>The app uses a flexbox layout to arrange the wheel and the entry list side-by-side. The <code>flex</code> and <code>gap-8</code> classes are used to create the flex container and the gap between the two elements.</p>
<pre><code class="language-html">&lt;!-- This is the main container for the wheel and the entry list. --&gt;
&lt;div class=&quot;flex gap-8&quot;&gt;
  &lt;!-- This is the container for the wheel. --&gt;
  &lt;div class=&quot;flex flex-col items-center&quot;&gt;
    {/* ... */}
  &lt;/div&gt;
  &lt;!-- This is the container for the entry list. --&gt;
  &lt;div class=&quot;w-full max-w-xs ml-16&quot;&gt;
    {/* ... */}
  &lt;/div&gt;
&lt;/div&gt;
</code></pre>
<h2>Conclusion</h2>
<p>The Picker Wheel app is a fun and interactive way to pick a random winner from a list of entries. It&#39;s built using a combination of React, the Canvas API, and Tailwind CSS, and it demonstrates a variety of concepts and techniques used in modern web development.</p>
<p><a href="https://fezcode.com/blog/picker-wheel-deep-dive">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Find Minimum in Rotated Sorted Array]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/find-minimum-in-rotated-sorted-array</link>
            <guid isPermaLink="false">https://fezcode.com/blog/find-minimum-in-rotated-sorted-array</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 08 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h2>Problem Description</h2>
<p>Suppose an array sorted in ascending order is rotated at some pivot unknown to you beforehand.
(i.e., <code>[0,1,2,4,5,6,7]</code> might become <code>[4,5,6,7,0,1,2]</code>).</p>
<p>Find the minimum element.</p>
<p>You may assume no duplicate exists in the array.</p>
<h2>Solution</h2>
<p>This problem can be solved efficiently using a modified binary search approach.
The key idea is to observe that if we pick an arbitrary element <code>mid</code>, one of the two halves (left or right) must be sorted.
The minimum element will always be in the unsorted half.</p>
<ol>
<li>Initialize <code>left</code> to 0 and <code>right</code> to <code>len(nums) - 1</code>.</li>
<li>While <code>left &lt; right</code>:
a.  Calculate <code>mid = left + (right - left) / 2</code>.
b.  If <code>nums[mid] &gt; nums[right]</code>, it means the minimum element is in the right half (from <code>mid + 1</code> to <code>right</code>), because the right part is unsorted. So, set <code>left = mid + 1</code>.
c.  Else (<code>nums[mid] &lt; nums[right]</code>), it means the minimum element is in the left half (from <code>left</code> to <code>mid</code>), because the right part is sorted, and <code>nums[mid]</code> could be the minimum. So, set <code>right = mid</code>.</li>
<li>Return <code>nums[left]</code> (or <code>nums[right]</code>, as <code>left</code> and <code>right</code> will converge to the minimum element&#39;s index).</li>
</ol>
<h2>Code (GoLang)</h2>
<pre><code class="language-go">package main

import &quot;fmt&quot;

func findMin(nums []int) int {
    left, right := 0, len(nums)-1

    for left &lt; right {
        mid := left + (right-left)/2
        if nums[mid] &gt; nums[right] {
            // Minimum is in the right half (mid+1 to right)
            left = mid + 1
        } else {
            // Minimum is in the left half (left to mid)
            // nums[mid] could be the minimum
            right = mid
        }
    }

    return nums[left]
}

func main() {
    fmt.Println(findMin([]int{3, 4, 5, 1, 2})) // Output: 1
    fmt.Println(findMin([]int{4, 5, 6, 7, 0, 1, 2})) // Output: 0
    fmt.Println(findMin([]int{1})) // Output: 1
    fmt.Println(findMin([]int{1, 2})) // Output: 1
    fmt.Println(findMin([]int{2, 1})) // Output: 1
}
</code></pre>
<p><a href="https://fezcode.com/blog/find-minimum-in-rotated-sorted-array">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Demystifying Tailwind CSS]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/demystifying-tailwind-css</link>
            <guid isPermaLink="false">https://fezcode.com/blog/demystifying-tailwind-css</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 07 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Demystifying Tailwind CSS in <code>fezcodex</code>: A Utility-First Approach</h1>
<p>In the <code>fezcodex</code> project, you&#39;ll notice that our components are styled <strong>cool</strong>(!). Instead of writing custom CSS for every element, <strong>Tailwind CSS</strong> is used. This post will explain what Tailwind CSS is, how it&#39;s configured in my project,
and why it&#39;s a powerful tool for building user interfaces.</p>
<h2>Part 1: The Core Concept - Utility-First CSS</h2>
<p>Traditionally, when styling a webpage, you might write CSS like this:</p>
<pre><code class="language-css">.my-button {
  background-color: blue;
  color: white;
  padding: 1rem;
  border-radius: 0.5rem;
}
</code></pre>
<p>And then apply it in your HTML:</p>
<pre><code class="language-html">&lt;button class=&quot;my-button&quot;&gt;Click Me&lt;/button&gt;
</code></pre>
<p>Tailwind CSS takes a <strong>utility-first</strong> approach. Instead of writing custom CSS classes for every component, you apply small, single-purpose utility classes directly in your HTML (or JSX, in our case). Each class does one thing, and one thing only.</p>
<p>For example, the same button in Tailwind would look like this:</p>
<pre><code class="language-html">&lt;button class=&quot;bg-blue-500 text-white p-4 rounded-md&quot;&gt;Click Me&lt;/button&gt;
</code></pre>
<p><strong>Benefits of Utility-First CSS:</strong></p>
<ul>
<li><strong>Rapid UI Development:</strong> You can build complex UIs much faster because you&#39;re not constantly switching between HTML/JSX and CSS files. All the styling happens directly in your markup.</li>
<li><strong>Consistent Design:</strong> By using a predefined set of utility classes (which are based on a design system), it&#39;s much easier to maintain a consistent look and feel across your application.</li>
<li><strong>No More Unused CSS:</strong> Tailwind, especially with its JIT (Just-In-Time) mode, only generates the CSS that you actually use in your project. This results in incredibly small and optimized CSS bundles, improving performance.</li>
<li><strong>Avoid Naming Headaches:</strong> You no longer have to come up with semantic class names for every single element, which can be a surprisingly difficult task in larger projects.</li>
</ul>
<h2>Part 2: Tailwind in <code>fezcodex</code> - Configuration and Customization</h2>
<p>Our project customizes Tailwind to fit its specific design needs. This is primarily managed through two files:</p>
<h3><code>tailwind.config.js</code></h3>
<p>This is the central configuration file for Tailwind CSS. It tells Tailwind how to behave and what custom styles to include.</p>
<ul>
<li><strong><code>content</code></strong>: This array (<code>./src/**/*.{js,jsx,ts,tsx}</code>) tells Tailwind which files to scan for utility classes. This is crucial for the build process to identify and include only the necessary CSS.</li>
<li><strong><code>theme.extend.colors</code></strong>: This is where we integrate our custom color palette. You&#39;ll see it imports <code>colors</code> from <code>./src/config/colors.js</code>. This means that any color defined in <code>colors.js</code> (like <code>article: &#39;#FA8072&#39;</code>) becomes available as a Tailwind utility class. For example:<ul>
<li><code>text-article</code> will apply the <code>article</code> color to text.</li>
<li><code>bg-article</code> will apply the <code>article</code> color to the background.</li>
<li><code>border-article</code> will apply the <code>article</code> color to the border.
This is why we use <code>text-article</code> and not just <code>article</code> – the <code>text-</code> prefix tells Tailwind <em>what</em> CSS property to apply the color to.</li>
</ul>
</li>
<li><strong><code>theme.extend.fontFamily</code></strong>: Similar to colors, this section allows us to define and use custom fonts (imported from <code>./src/config/fonts.js</code>) throughout the project using Tailwind&#39;s <code>font-{name}</code> classes.</li>
<li><strong><code>plugins</code></strong>: We use <code>@tailwindcss/typography</code> here. This plugin provides a set of <code>prose</code> classes that can be used to style raw HTML (like the content generated from Markdown files) with beautiful, readable typography, without having to manually style every heading, paragraph, and list item.</li>
</ul>
<h3><code>src/config/colors.js</code> and <code>src/config/fonts.js</code></h3>
<p>These files act as our project&#39;s design token repositories. They centralize all our custom colors and font definitions, making it easy to manage and update our design system from a single source.</p>
<h2>Part 3: How It All Comes Together - Building UI</h2>
<p>When you look at a component like <code>AppCard.js</code> or <code>WordCounterPage.js</code>, you&#39;ll see a lot of classes directly in the JSX. For example, a card might have classes like:</p>
<pre><code class="language-html">&lt;div class=&quot;bg-transparent border rounded-lg shadow-lg p-6 flex flex-col justify-between relative transform transition-all duration-300 ease-in-out hover:scale-105 hover:shadow-2xl overflow-hidden h-full&quot;&gt;
  &lt;!-- ... content ... --&gt;
&lt;/div&gt;
</code></pre>
<p>Let&#39;s break down a few of these:</p>
<ul>
<li><code>bg-transparent</code>: Sets the background to transparent.</li>
<li><code>border</code>: Adds a default border.</li>
<li><code>rounded-lg</code>: Applies a large border-radius.</li>
<li><code>shadow-lg</code>: Adds a large box shadow.</li>
<li><code>p-6</code>: Adds padding of <code>1.5rem</code> on all sides.</li>
<li><code>flex flex-col justify-between</code>: Configures the element as a flex container, arranging its children in a column and distributing space between them.</li>
<li><code>hover:scale-105 hover:shadow-2xl</code>: These are <strong>variant</strong> classes. They apply <code>scale-105</code> (makes the element 5% larger) and <code>shadow-2xl</code> (a larger shadow) <em>only when the element is hovered over</em>.</li>
<li><code>transition-all duration-300 ease-in-out</code>: Ensures that changes to properties like <code>transform</code> (for <code>scale</code>) and <code>box-shadow</code> (for <code>shadow</code>) happen smoothly over 300 milliseconds.</li>
</ul>
<p>Tailwind also makes responsive design easy with prefixes like <code>sm:</code>, <code>md:</code>, <code>lg:</code>, and <code>xl:</code>. For example, <code>md:flex</code> would make an element a flex container only on medium screens and larger.</p>
<h2>Part 4: The Build Process</h2>
<p>During development and when building for production, a tool like <strong>Craco</strong> (which sits on top of Create React App&#39;s Webpack configuration) processes your code. It uses <strong>PostCSS</strong> and the Tailwind plugin to scan all your files for Tailwind utility classes. It then generates a minimal CSS file containing <em>only</em> the styles corresponding to the classes you&#39;ve actually used. This ensures that your final application bundle is as small and performant as possible.</p>
<h2>Part 5: Let&#39;s Change the Color of Horizontal Rule</h2>
<p>You can change the color of an <code>&lt;hr&gt;</code> (horizontal rule) element in Tailwind CSS using a couple of common methods:</p>
<h3>1. Using <code>border-color</code> utilities (Most Common)</h3>
<p>By default, an <code>&lt;hr&gt;</code> element is rendered as a line using its <code>border-top</code> or <code>border-bottom</code> property. You can directly apply Tailwind&#39;s border color utilities to change its color.</p>
<pre><code class="language-html">&lt;!-- A simple gray HR --&gt;
&lt;hr class=&quot;border-gray-300&quot; /&gt;

&lt;!-- A red HR --&gt;
&lt;hr class=&quot;border-red-500&quot; /&gt;

&lt;!-- A thicker, blue HR --&gt;
&lt;hr class=&quot;border-t-4 border-blue-500&quot; /&gt;

&lt;!-- An HR with a custom color from your config (e.g., primary.400) --&gt;
&lt;hr class=&quot;border-primary-400&quot; /&gt;
</code></pre>
<p><strong>Explanation:</strong></p>
<ul>
<li><code>border-gray-300</code>: Sets the border color to a light gray.</li>
<li><code>border-red-500</code>: Sets the border color to red.</li>
<li><code>border-t-4</code>: Makes the top border 4 pixels thick. You can use <code>border-t</code>, <code>border-b</code>, <code>border-l</code>, <code>border-r</code> or just <code>border</code> for all sides.</li>
<li><code>border-blue-500</code>: Sets the border color to blue.</li>
<li><code>border-primary-400</code>: Uses a custom color defined in your <code>tailwind.config.js</code> (like the <code>primary</code> color you have).</li>
</ul>
<h3>2. Using <code>background-color</code> utilities with a defined height</h3>
<p>Another way is to treat the <code>&lt;hr&gt;</code> as a block element with a specific height and then apply a background color. This gives you more control over its appearance, especially if you want a solid block of color rather than just a border line.</p>
<pre><code class="language-html">&lt;!-- A red HR with a height of 1px --&gt;
&lt;hr class=&quot;h-px bg-red-500 border-0&quot; /&gt;

&lt;!-- A thicker, blue HR --&gt;
&lt;hr class=&quot;h-2 bg-blue-500 border-0&quot; /&gt;

&lt;!-- An HR with a custom color from your config --&gt;
&lt;hr class=&quot;h-1 bg-primary-400 border-0&quot; /&gt;
</code></pre>
<p><strong>Explanation:</strong></p>
<ul>
<li><code>h-px</code>: Sets the height to 1 pixel. You can use any height utility (e.g., <code>h-1</code>, <code>h-2</code>, <code>h-4</code>, etc.).</li>
<li><code>bg-red-500</code>: Sets the background color to red.</li>
<li><code>border-0</code>: It&#39;s important to remove the default border of the <code>&lt;hr&gt;</code> when using this method, otherwise, you might see both the border and the background color.</li>
</ul>
<p>Choose the method that best suits your design needs. The <code>border-color</code> method is generally more semantic for an <code>&lt;hr&gt;</code>, but the <code>background-color</code> method offers more flexibility for solid bars.</p>
<h2>Part 6: Let&#39;s Put Two <code>&lt;Span&gt;</code> Element Next To Each Other</h2>
<p>When your parent <code>div</code> has the classes <code>flex flex-col text-sm group w-max flex-grow</code>, the <code>flex-col</code> class specifically instructs the flex container to arrange its direct children in a <strong>column</strong>, meaning they will stack vertically, one below the other.</p>
<p>To put two <code>&lt;span&gt;</code> elements next to each other (horizontally) within this <code>flex-col</code> parent, you need to introduce a <strong>nested flex container</strong> that is configured for a row layout.</p>
<p>Here&#39;s how you can do it:</p>
<pre><code class="language-html">&lt;div class=&quot;flex flex-col text-sm group w-max flex-grow&quot;&gt;
  &lt;!-- Other content that might stack vertically due to parent&#39;s flex-col --&gt;
  &lt;div&gt;Other content 1&lt;/div&gt;
  &lt;div&gt;Other content 2&lt;/div&gt;

  &lt;!-- This is the new nested flex container for your two spans --&gt;
  &lt;div class=&quot;flex items-center space-x-2&quot;&gt;
    &lt;span&gt;Span 1&lt;/span&gt;
    &lt;span&gt;Span 2&lt;/span&gt;
  &lt;/div&gt;

  &lt;!-- More content that might stack vertically --&gt;
  &lt;div&gt;More content 3&lt;/div&gt;
&lt;/div&gt;
</code></pre>
<p><strong>Explanation:</strong></p>
<ol>
<li><strong><code>flex flex-col text-sm group w-max flex-grow</code></strong>: This is your existing parent <code>div</code>. It will arrange its direct children (like &quot;Other content 1&quot;, &quot;Other content 2&quot;, the new nested <code>div</code>, and &quot;More content 3&quot;) in a vertical column.</li>
<li><strong><code>&lt;div class=&quot;flex items-center space-x-2&quot;&gt;</code></strong>: This is the crucial part.<ul>
<li><code>flex</code>: This makes this new <code>div</code> a flex container. By default, a flex container arranges its items in a <strong>row</strong> (<code>flex-row</code>).</li>
<li><code>items-center</code>: This vertically aligns the items (your <code>&lt;span&gt;</code>s) in the center of this nested flex container.</li>
<li><code>space-x-2</code>: This Tailwind utility adds horizontal space between the direct children of this flex container (your two <code>&lt;span&gt;</code>s). You can adjust <code>2</code> to any spacing you need (e.g., <code>space-x-1</code>, <code>space-x-4</code>).</li>
</ul>
</li>
</ol>
<p>By using this nested <code>div</code>, you create a horizontal layout context specifically for your two <code>&lt;span&gt;</code> elements, while the overall parent <code>div</code> still maintains its <code>flex-col</code> behavior for its other children.</p>
<h2>Conclusion</h2>
<p>Tailwind CSS provides a powerful and efficient way to build and maintain the UI of the <code>fezcodex</code> project. By embracing its utility-first philosophy and leveraging its extensive configuration options, we can rapidly develop consistent, responsive, and performant user interfaces. It streamlines the styling process, allowing developers to focus more on functionality and less on managing complex CSS stylesheets.</p>
<p><a href="https://fezcode.com/blog/demystifying-tailwind-css">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Lowest Common Ancestor with Binary Search Tree]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/lca</link>
            <guid isPermaLink="false">https://fezcode.com/blog/lca</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 07 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Understanding Trees, Binary Search Trees, and Finding the Lowest Common Ancestor</h1>
<p>In the world of computer science, data structures are the building blocks of efficient algorithms. One of the most fundamental and versatile data structures is the <strong>Tree</strong>.</p>
<p>This post will take you on a journey from the basics of trees to a specific type called a Binary Search Tree (BST), explore common algorithms used with them, and finally, solve a classic problem: finding the Lowest Common Ancestor of two nodes in a BST.</p>
<h2>What is a Tree?</h2>
<p>In computer science, a <strong>Tree</strong> is a hierarchical data structure that consists of nodes connected by edges.</p>
<p>Unlike linear data structures like arrays or linked lists, trees are non-linear and are used to represent hierarchical relationships.</p>
<h3>Key Terminology:</h3>
<ul>
<li><strong>Node:</strong> The fundamental part of a tree that stores data.</li>
<li><strong>Edge:</strong> The connection between two nodes.</li>
<li><strong>Root:</strong> The topmost node in a tree. It&#39;s the only node that doesn&#39;t have a parent.</li>
<li><strong>Parent:</strong> A node that has a child node.</li>
<li><strong>Child:</strong> A node that has a parent node.</li>
<li><strong>Leaf:</strong> A node that does not have any children.</li>
<li><strong>Subtree:</strong> A tree consisting of a node and its descendants.</li>
<li><strong>Depth:</strong> The length of the path from the root to a specific node.</li>
<li><strong>Height:</strong> The length of the longest path from a specific node to a leaf.</li>
</ul>
<p>Trees are used in various applications, such as file systems, organization charts, and even in parsing expressions in compilers.</p>
<h2>Binary Search Trees (BSTs)</h2>
<p>A <strong>Binary Search Tree (BST)</strong> is a special type of binary tree where the nodes are ordered in a specific way.</p>
<p>This ordering makes operations like searching, insertion, and deletion very efficient.</p>
<p>A binary tree is a BST if it satisfies the following properties:</p>
<ol>
<li>The left subtree of a node contains only nodes with keys <strong>lesser</strong> than the node&#39;s key.</li>
<li>The right subtree of a node contains only nodes with keys <strong>greater</strong> than the node&#39;s key.</li>
<li>Both the left and right subtrees must also be binary search trees.</li>
</ol>
<p>This structure ensures that for any given node, all the values in its left subtree are smaller, and all the values in its right subtree are larger.</p>
<h2>Common Tree Algorithms</h2>
<p>Trees have a variety of algorithms for traversal and manipulation. The most common are traversal algorithms, which visit each node in the tree exactly once.</p>
<h3>Tree Traversal</h3>
<p>There are two main approaches to traversing a tree:</p>
<ol>
<li><p><strong>Depth-First Search (DFS):</strong> This approach explores as far as possible down one branch before backtracking. There are three common ways to perform DFS:</p>
<ul>
<li><strong>In-order Traversal:</strong> Visit the left subtree, then the root, then the right subtree. For a BST, this traversal visits the nodes in ascending order.</li>
<li><strong>Pre-order Traversal:</strong> Visit the root, then the left subtree, then the right subtree. This is useful for creating a copy of the tree.</li>
<li><strong>Post-order Traversal:</strong> Visit the left subtree, then the right subtree, then the root. This is useful for deleting nodes from the tree.</li>
</ul>
</li>
<li><p><strong>Breadth-First Search (BFS):</strong> This approach explores the tree level by level. It visits all the nodes at a given depth before moving on to the next level. BFS is typically implemented using a queue.</p>
</li>
</ol>
<h2>More Tree Algorithms in Go</h2>
<p>Let&#39;s explore how to implement some of these fundamental tree algorithms in Go.</p>
<h3>Finding the Height/Depth of a Binary Tree</h3>
<p>The <strong>height</strong> of a binary tree is the number of edges on the longest path from the root node to a leaf node. A tree with only a root node has a height of 0.</p>
<p>The concept is closely related to the <strong>depth</strong> of a node, which is its distance from the root. The height of the tree is, therefore, the maximum depth of any node in the tree.</p>
<p>We can calculate the height recursively. The height of a node is 1 plus the maximum height of its left or right subtree.</p>
<pre><code class="language-go">import &quot;math&quot;

// TreeNode definition from before
type TreeNode struct {
    Val int
    Left *TreeNode
    Right *TreeNode
}

func max(a, b int) int {
    if a &gt; b {
        return a
    }
    return b
}

func height(node *TreeNode) int {
    if node == nil {
        return -1 // Height of a null tree is -1
    }
    leftHeight := height(node.Left)
    rightHeight := height(node.Right)
    return 1 + max(leftHeight, rightHeight)
}
</code></pre>
<h3>DFS Traversals in Go</h3>
<p>Here are the Go implementations for the three DFS traversal methods.</p>
<h4>In-order Traversal</h4>
<pre><code class="language-go">func inOrderTraversal(node *TreeNode) {
    if node == nil {
        return
    }
    inOrderTraversal(node.Left)
    fmt.Println(node.Val) // Process the node
    inOrderTraversal(node.Right)
}
</code></pre>
<h4>Pre-order Traversal</h4>
<pre><code class="language-go">func preOrderTraversal(node *TreeNode) {
    if node == nil {
        return
    }
    fmt.Println(node.Val) // Process the node
    preOrderTraversal(node.Left)
    preOrderTraversal(node.Right)
}
</code></pre>
<h4>Post-order Traversal</h4>
<pre><code class="language-go">func postOrderTraversal(node *TreeNode) {
    if node == nil {
        return
    }
    postOrderTraversal(node.Left)
    postOrderTraversal(node.Right)
    fmt.Println(node.Val) // Process the node
}
</code></pre>
<h2>LeetCode 235: Lowest Common Ancestor of a Binary Search Tree</h2>
<p>Now, let&#39;s apply our knowledge to a classic problem.</p>
<p>The <strong>Lowest Common Ancestor (LCA)</strong> of two nodes, <code>p</code> and <code>q</code>, in a tree is the lowest (i.e., deepest) node that has both <code>p</code> and <code>q</code> as descendants.</p>
<h3>The Problem</h3>
<p>Given a binary search tree (BST), find the lowest common ancestor (LCA) of two given nodes in the BST.</p>
<p>For example, consider the following BST:</p>
<pre><code>      6
     / \
    2   8
   / \ / \
  0  4 7  9
    / \
   3   5
</code></pre>
<ul>
<li>The LCA of nodes <code>2</code> and <code>8</code> is <code>6</code>.</li>
<li>The LCA of nodes <code>2</code> and <code>4</code> is <code>2</code>, since a node can be a descendant of itself.</li>
<li>The LCA of nodes <code>3</code> and <code>5</code> is <code>4</code>.</li>
</ul>
<h3>The Solution</h3>
<p>The properties of a BST make finding the LCA particularly efficient.</p>
<p>We can start at the root of the tree and use the values of <code>p</code> and <code>q</code> to decide where to go next.</p>
<p>Let&#39;s consider the current node we are at, let&#39;s call it <code>current</code>.</p>
<ol>
<li>If both <code>p</code> and <code>q</code> are <strong>greater</strong> than <code>current.val</code>, it means that the LCA must be in the <strong>right</strong> subtree. So, we move to the right child.</li>
<li>If both <code>p</code> and <code>q</code> are <strong>less</strong> than <code>current.val</code>, it means that the LCA must be in the <strong>left</strong> subtree. So, we move to the left child.</li>
<li>If one of <code>p</code> or <code>q</code> is greater than <code>current.val</code> and the other is less than <code>current.val</code> (or if one of them is equal to <code>current.val</code>), then <code>current</code> is the LCA.</li>
</ol>
<p>This is because <code>p</code> and <code>q</code> are on opposite sides of the current node, meaning it&#39;s the split point and thus the lowest common ancestor.</p>
<p>We can implement this logic both iteratively and recursively.</p>
<h3>Iterative Solution</h3>
<pre><code class="language-go">/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */

func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {
    current := root
    for current != nil {
        if p.Val &gt; current.Val &amp;&amp; q.Val &gt; current.Val {
            current = current.Right
        } else if p.Val &lt; current.Val &amp;&amp; q.Val &lt; current.Val {
            current = current.Left
        } else {
            return current
        }
    }
    return nil // Should not be reached in a valid BST
}
</code></pre>
<h3>Recursive Solution</h3>
<pre><code class="language-go">/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */

func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {
    if root == nil {
        return nil
    }

    if p.Val &gt; root.Val &amp;&amp; q.Val &gt; root.Val {
        return lowestCommonAncestor(root.Right, p, q)
    } else if p.Val &lt; root.Val &amp;&amp; q.Val &lt; root.Val {
        return lowestCommonAncestor(root.Left, p, q)
    } else {
        return root
    }
}
</code></pre>
<p>Both of these solutions are very efficient, with a time complexity of O(H), where H is the height of the tree. In a balanced BST, this is O(log N), where N is the number of nodes.</p>
<p>The space complexity for the iterative solution is O(1), while the recursive solution has a space complexity of O(H) due to the call stack.</p>
<h3>Alternative: Stack-Based Solution</h3>
<p>Another way to solve this problem is by finding the path from the root to each of the two nodes, <code>p</code> and <code>q</code>. Once we have both paths, we can compare them to find the last common node, which is the LCA.</p>
<p>This method is more generic and would also work for a regular binary tree, but it&#39;s less efficient for a BST than the previous solutions because it requires traversing parts of the tree multiple times and uses extra space to store the paths.</p>
<p>Here is the implementation in Go:</p>
<pre><code class="language-go">// Helper function to find the path from the root to a target node
func getPath(root, target *TreeNode) []*TreeNode {
    path := []*TreeNode{}
    current := root
    for current != nil {
        path = append(path, current)
        if target.Val &lt; current.Val {
            current = current.Left
        } else if target.Val &gt; current.Val {
            current = current.Right
        } else {
            break // Found the target
        }
    }
    return path
}

func lowestCommonAncestorWithStacks(root, p, q *TreeNode) *TreeNode {
    pathP := getPath(root, p)
    pathQ := getPath(root, q)

    var lca *TreeNode

    // Iterate through both paths until they diverge
    for i := 0; i &lt; len(pathP) &amp;&amp; i &lt; len(pathQ); i++ {
        if pathP[i] == pathQ[i] {
            lca = pathP[i]
        } else {
            break
        }
    }

    return lca
}
</code></pre>
<h2>Conclusion</h2>
<p>Trees and Binary Search Trees are powerful data structures that are essential for any programmer&#39;s toolkit. By understanding their properties and the algorithms that operate on them, you can solve a wide range of problems efficiently.</p>
<p>The Lowest Common Ancestor problem is a perfect example of how the structure of a BST can be leveraged to find an elegant and optimal solution.</p>
<p><a href="https://fezcode.com/blog/lca">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Monotonic Stack with Daily Temperatures]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/monotonic-stack</link>
            <guid isPermaLink="false">https://fezcode.com/blog/monotonic-stack</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 05 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Monotonic Stack &amp; LeetCode&#39;s &quot;Daily Temperatures&quot;</h1>
<p>Let&#39;s dive into a cool data structure called the <strong>Monotonic Stack</strong> and see how it helps us solve a popular coding problem, &quot;Daily Temperatures&quot;.</p>
<h2>What&#39;s a Monotonic Stack?</h2>
<p>Imagine a stack of books, but with a rule: you can only place a new book on top if it&#39;s lighter than the book already there. This creates a stack of books sorted by weight, from heaviest at the bottom to lightest at the top.</p>
<p>That&#39;s a <strong>monotonic stack</strong>. It&#39;s a regular stack, but it enforces a specific order on its elements – either always increasing or always decreasing.</p>
<ul>
<li><strong>Monotonically Increasing Stack:</strong> Elements are always in increasing order from bottom to top. <code>[1, 2, 5, 8]</code></li>
<li><strong>Monotonically Decreasing Stack:</strong> Elements are always in decreasing order from bottom to top. <code>[10, 7, 4, 1]</code></li>
</ul>
<p>This simple rule makes them incredibly efficient for problems where you need to find the &quot;next greater element&quot; or &quot;previous smaller element&quot; for items in a sequence.</p>
<hr>
<h2>The Problem: Daily Temperatures (LeetCode 739)</h2>
<p>The problem is this: You&#39;re given a list of daily temperatures. For each day, you need to figure out how many days you have to wait for a warmer temperature. If no such day exists, the answer is 0.</p>
<p><strong>Example:</strong></p>
<p><code>temperatures = [73, 74, 75, 71, 69, 72, 76, 73]</code></p>
<p><strong>Expected Output:</strong></p>
<p><code>result = [1, 1, 4, 2, 1, 1, 0, 0]</code></p>
<p>Let&#39;s break down why:</p>
<ul>
<li>For day 0 (73°), the next day (day 1) is warmer (74°). So, wait is <code>1</code> day.</li>
<li>For day 1 (74°), the next day (day 2) is warmer (75°). So, wait is <code>1</code> day.</li>
<li>For day 2 (75°), you have to wait until day 6 (76°). So, wait is <code>6 - 2 = 4</code> days.</li>
<li>...and so on.</li>
</ul>
<hr>
<h2>The Solution: Using a Monotonically Decreasing Stack</h2>
<p>We&#39;ll use a stack to store the <em>indices</em> of the days. We&#39;ll keep this stack monotonically decreasing, meaning the temperatures corresponding to the indices in the stack will always be going down.</p>
<p>Let&#39;s trace our example: <code>temperatures = [73, 74, 75, 71, 69, 72, 76, 73]</code></p>
<p>Initialize <code>result = [0, 0, 0, 0, 0, 0, 0, 0]</code> and an empty <code>stack</code>.</p>
<p><strong>Day 0: Temp = 73</strong></p>
<ul>
<li>Stack is empty. Push index <code>0</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
               ^ (Current: 73)
Stack:
| 0 |  (Temp: 73)
+---+
</code></pre>
<p><strong>Day 1: Temp = 74</strong></p>
<ul>
<li><code>74</code> is warmer than <code>temperatures[0]</code> (73).</li>
<li>We found a warmer day for index <code>0</code>!</li>
<li>Pop <code>0</code> from the stack.</li>
<li><code>result[0] = 1 (current index) - 0 (popped index) = 1</code>.</li>
<li>Now the stack is empty. Push index <code>1</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
                   ^ (Current: 74)
Stack:
| 1 |  (Temp: 74)
+---+

Result: [1, 0, 0, 0, 0, 0, 0, 0]
</code></pre>
<p><strong>Day 2: Temp = 75</strong></p>
<ul>
<li><code>75</code> is warmer than <code>temperatures[1]</code> (74).</li>
<li>Pop <code>1</code>. <code>result[1] = 2 - 1 = 1</code>.</li>
<li>Stack is empty. Push index <code>2</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
                       ^ (Current: 75)
Stack:
| 2 |  (Temp: 75)
+---+

Result: [1, 1, 0, 0, 0, 0, 0, 0]
</code></pre>
<p><strong>Day 3: Temp = 71</strong></p>
<ul>
<li><code>71</code> is not warmer than <code>temperatures[2]</code> (75).</li>
<li>The stack needs to stay decreasing. Push index <code>3</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
                           ^ (Current: 71)
Stack:
| 3 |  (Temp: 71)
| 2 |  (Temp: 75)
+---+
</code></pre>
<p><strong>Day 4: Temp = 69</strong></p>
<ul>
<li><code>69</code> is not warmer than <code>temperatures[3]</code> (71).</li>
<li>Push index <code>4</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
                               ^ (Current: 69)
Stack:
| 4 |  (Temp: 69)
| 3 |  (Temp: 71)
| 2 |  (Temp: 75)
+---+
</code></pre>
<p><strong>Day 5: Temp = 72</strong></p>
<ul>
<li><code>72</code> is warmer than <code>temperatures[4]</code> (69).</li>
<li>Pop <code>4</code>. <code>result[4] = 5 - 4 = 1</code>.</li>
<li><code>72</code> is warmer than <code>temperatures[3]</code> (71).</li>
<li>Pop <code>3</code>. <code>result[3] = 5 - 3 = 2</code>.</li>
<li><code>72</code> is NOT warmer than <code>temperatures[2]</code> (75). Stop popping.</li>
<li>Push index <code>5</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
                                   ^ (Current: 72)
Stack:
| 5 |  (Temp: 72)
| 2 |  (Temp: 75)
+---+

Result: [1, 1, 0, 2, 1, 0, 0, 0]
</code></pre>
<p><strong>Day 6: Temp = 76</strong></p>
<ul>
<li><code>76</code> is warmer than <code>temperatures[5]</code> (72).</li>
<li>Pop <code>5</code>. <code>result[5] = 6 - 5 = 1</code>.</li>
<li><code>76</code> is warmer than <code>temperatures[2]</code> (75).</li>
<li>Pop <code>2</code>. <code>result[2] = 6 - 2 = 4</code>.</li>
<li>Stack is empty. Push index <code>6</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
                                       ^ (Current: 76)
Stack:
| 6 |  (Temp: 76)
+---+

Result: [1, 1, 4, 2, 1, 1, 0, 0]
</code></pre>
<p><strong>Day 7: Temp = 73</strong></p>
<ul>
<li><code>73</code> is not warmer than <code>temperatures[6]</code> (76).</li>
<li>Push index <code>7</code>.</li>
</ul>
<pre><code>Temperatures: [73, 74, 75, 71, 69, 72, 76, 73]
                                           ^ (Current: 73)
Stack:
| 7 |  (Temp: 73)
| 6 |  (Temp: 76)
+---+
</code></pre>
<p><strong>End of Loop</strong></p>
<ul>
<li>The loop finishes. Any indices left in the stack (<code>6</code> and <code>7</code>) don&#39;t have a warmer day after them, so their results remain <code>0</code>.</li>
</ul>
<p><strong>Final Result:</strong> <code>[1, 1, 4, 2, 1, 1, 0, 0]</code></p>
<hr>
<h3>Code Example (JavaScript)</h3>
<pre><code class="language-javascript">function dailyTemperatures(temperatures) {
  const result = new Array(temperatures.length).fill(0);
  const stack = []; // We&#39;ll store indices here

  for (let i = 0; i &lt; temperatures.length; i++) {
    // While stack is not empty AND current temp is warmer than the temp at the index on top of the stack
    while (stack.length &gt; 0 &amp;&amp; temperatures[i] &gt; temperatures[stack[stack.length - 1]]) {
      const prevIndex = stack.pop();
      result[prevIndex] = i - prevIndex;
    }
    // Push the current index onto the stack
    stack.push(i);
  }

  return result;
}
</code></pre>
<h3>Code Example (GoLang)</h3>
<pre><code class="language-go">func dailyTemperatures(temperatures []int) []int {
    n := len(temperatures)
    result := make([]int, n)
    stack := []int{} // We&#39;ll store indices here

    for i := 0; i &lt; n; i++ {
        // While stack is not empty AND current temp is warmer than the temp at the index on top of the stack
        for len(stack) &gt; 0 &amp;&amp; temperatures[i] &gt; temperatures[stack[len(stack)-1]] {
            prevIndex := stack[len(stack)-1]
            stack = stack[:len(stack)-1] // Pop
            result[prevIndex] = i - prevIndex
        }
        // Push the current index onto the stack
        stack = append(stack, i)
    }

    return result
}
</code></pre>
<p>And that&#39;s how a monotonic stack gives us an elegant and efficient solution!</p>
<p><a href="https://fezcode.com/blog/monotonic-stack">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Weighted Quick-Union with Path Compression]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/wquwpc</link>
            <guid isPermaLink="false">https://fezcode.com/blog/wquwpc</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 04 Nov 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>A Deep Dive into Weighted Quick-Union with Path Compression</h1>
<p>In the world of computer science, some algorithms are so efficient and elegant that they feel like magic. The <strong>Weighted Quick-Union with Path Compression</strong> algorithm is one of them. It&#39;s the gold standard for solving a class of problems known as &quot;dynamic connectivity&quot; problems.</p>
<p>Let&#39;s break it down.</p>
<h2>The Problem: Dynamic Connectivity</h2>
<p>Imagine you have a set of objects. Over time, you&#39;re told that certain pairs of these objects are now connected. The fundamental question you want to answer, at any point, is: &quot;Are object A and object B connected?&quot;</p>
<p>This &quot;connection&quot; could mean anything:</p>
<ul>
<li><strong>Social Networks:</strong> Are two people in the same network of friends?</li>
<li><strong>Computer Networks:</strong> Can two computers on a network send messages to each other?</li>
<li><strong>Image Processing:</strong> Are two pixels part of the same contiguous region of color?</li>
<li><strong>Maze Solving:</strong> Is there a path from the start to the end?</li>
</ul>
<p>The data structure that handles this is often called a <strong>Union-Find</strong> or <strong>Disjoint-Set Union (DSU)</strong>. It has two primary operations:</p>
<ol>
<li><code>union(p, q)</code>: Connect object <code>p</code> and object <code>q</code>.</li>
<li><code>find(p)</code>: Find the &quot;identifier&quot; of the group that <code>p</code> belongs to. If <code>find(p)</code> equals <code>find(q)</code>, then <code>p</code> and <code>q</code> are connected.</li>
</ol>
<h2>The Journey to Optimization</h2>
<p>Let&#39;s build up to the final, optimized algorithm by looking at simpler versions first. We&#39;ll represent our objects as nodes in a forest (a collection of trees). Each tree represents a connected component. The root of the tree is the unique identifier for that component.</p>
<h3>Attempt 1: Quick-Union</h3>
<p>In this approach, each node has a pointer to its parent. To find the root of a node, you just follow the parent pointers until you reach a node that points to itself.</p>
<ul>
<li><code>find(p)</code>: Follow <code>parent[p]</code> until you reach the root.</li>
<li><code>union(p, q)</code>: Find the root of <code>p</code> (let&#39;s call it <code>rootP</code>) and the root of <code>q</code> (<code>rootQ</code>). Then, simply set the parent of <code>rootP</code> to be <code>rootQ</code>.</li>
</ul>
<p><strong>The Problem:</strong> This can lead to very tall, skinny trees. Imagine connecting items in a line: <code>union(0,1)</code>, <code>union(1,2)</code>, <code>union(2,3)</code>, ...
The tree becomes a long chain. A <code>find</code> operation on the deepest node would have to traverse the entire chain, making it slow (O(N) in the worst case).</p>
<pre><code>union(0,1), union(1,2), union(2,3)

      3
      |
      2
      |
      1
      |
      0
</code></pre>
<h3>Attempt 2: Weighted Quick-Union (Union by Size/Rank)</h3>
<p>To avoid creating long chains, we can be smarter about our <code>union</code> operation. Instead of arbitrarily connecting one root to another, let&#39;s keep track of the &quot;size&quot; (number of nodes) of each tree.</p>
<p>When we perform <code>union(p, q)</code>, we find their roots (<code>rootP</code> and <code>rootQ</code>). We then connect the <em>smaller</em> tree to the root of the <em>larger</em> tree.</p>
<p>This simple change has a profound impact. It ensures our trees stay relatively short and bushy, preventing the worst-case scenario of a long chain. The maximum height of any tree is now guaranteed to be at most log(N), which makes our <code>find</code> operation much faster (O(log N)).</p>
<p><strong>Example:</strong></p>
<ul>
<li>Tree A has 5 nodes.</li>
<li>Tree B has 2 nodes.</li>
<li>To union them, we make the root of Tree B a child of the root of Tree A. The new combined tree has a size of 7.</li>
</ul>
<h3>The Final Touch: Path Compression</h3>
<p>We can do even better. This optimization is applied during the <code>find</code> operation and is incredibly clever.</p>
<p>When we call <code>find(p)</code>, we traverse a path of nodes from <code>p</code> up to the root. After we find the root, we can go back along that same path and make every node we visited point <em>directly</em> to the root.</p>
<p><strong>Before Path Compression:</strong>
<code>find(0)</code> requires traversing 0 -&gt; 1 -&gt; 2 -&gt; 3 -&gt; 4 (root)</p>
<pre><code>      4
      |
      3
     / \
    2   5
    |
    1
    |
    0
</code></pre>
<p><strong>After Path Compression on <code>find(0)</code>:</strong>
Now, nodes 0, 1, 2, and 3 all point directly to the root, 4.</p>
<pre><code>        4
      / | | \
     /  | |  \
    3   2 1   0
    |
    5
</code></pre>
<p>The next time we call <code>find</code> on any of those nodes (0, 1, 2, or 3), we&#39;ll get to the root in a single step! Over many operations, this keeps the trees incredibly flat.</p>
<h2>The Result: Nearly Constant Time</h2>
<p>When you combine <strong>Weighted Quick-Union</strong> with <strong>Path Compression</strong>, the performance becomes astonishingly good. The amortized time complexity for both <code>union</code> and <code>find</code> is nearly constant, often written as O(α(N)), where α(N) is the Inverse Ackermann function.</p>
<p>This function grows so slowly that for any input size you could possibly encounter in the real world (even larger than the number of atoms in the universe), α(N) is never greater than 5. For all practical purposes, the algorithm runs in constant time per operation.</p>
<h2>Golang Implementation</h2>
<p>Here is a full implementation in Go that combines both optimizations.</p>
<pre><code class="language-go">package main

import &quot;fmt&quot;

// WeightedQuickUnionPathCompression implements the union-find data structure with
// both weighting and path compression optimizations.
type WeightedQuickUnionPathCompression struct {
	// parent[i] = parent of i
	parent []int
	// size[i] = number of nodes in the subtree rooted at i
	size []int
	// count is the number of disjoint sets
	count int
}

// New initializes a new union-find data structure with n elements.
// Each element initially is in its own set.
func New(n int) *WeightedQuickUnionPathCompression {
	parent := make([]int, n)
	size := make([]int, n)
	for i := 0; i &lt; n; i++ {
		parent[i] = i
		size[i] = 1
	}
	return &amp;WeightedQuickUnionPathCompression{
		parent: parent,
		size:   size,
		count:  n,
	}
}

// Find returns the root of the component/set containing element p.
// It uses path compression to flatten the tree structure.
func (uf *WeightedQuickUnionPathCompression) Find(p int) int {
	// Find the root
	root := p
	for root != uf.parent[root] {
		root = uf.parent[root]
	}
	// Path compression: make every node on the path point to the root
	for p != root {
		newp := uf.parent[p]
		uf.parent[p] = root
		p = newp
	}
	return root
}

// Connected returns true if elements p and q are in the same set.
func (uf *WeightedQuickUnionPathCompression) Connected(p, q int) bool {
	return uf.Find(p) == uf.Find(q)
}

// Union merges the set containing element p with the set containing element q.
// It uses weighting (union by size) to keep the trees flat.
func (uf *WeightedQuickUnionPathCompression) Union(p, q int) {
	rootP := uf.Find(p)
	rootQ := uf.Find(q)

	if rootP == rootQ {
		return
	}

	// Weighted union: attach the smaller tree to the root of the larger tree.
	if uf.size[rootP] &lt; uf.size[rootQ] {
		uf.parent[rootP] = rootQ
		uf.size[rootQ] += uf.size[rootP]
	} else {
		uf.parent[rootQ] = rootP
		uf.size[rootP] += uf.size[rootQ]
	}
	uf.count--
}

// Count returns the number of disjoint sets.
func (uf *WeightedQuickUnionPathCompression) Count() int {
	return uf.count
}

func main() {
	// Example Usage:
	// Consider 10 elements, 0 through 9.
	uf := New(10)
	fmt.Printf(&quot;Initial components: %d\n&quot;, uf.Count()) // 10

	uf.Union(4, 3)
	uf.Union(3, 8)
	uf.Union(6, 5)
	uf.Union(9, 4)
	uf.Union(2, 1)

	fmt.Printf(&quot;Are 8 and 9 connected? %t\n&quot;, uf.Connected(8, 9)) // true
	fmt.Printf(&quot;Are 5 and 4 connected? %t\n&quot;, uf.Connected(5, 4)) // false

	uf.Union(5, 0)
	uf.Union(7, 2)
	uf.Union(6, 1)
	uf.Union(1, 8)

	fmt.Printf(&quot;Are 5 and 4 connected now? %t\n&quot;, uf.Connected(5, 4)) // true
	fmt.Printf(&quot;Final components: %d\n&quot;, uf.Count())                  // 1
}
</code></pre>
<h2>Conclusion</h2>
<p>The Weighted Quick-Union with Path Compression algorithm is a testament to how clever optimizations can turn a slow, impractical solution into one that is breathtakingly fast. It&#39;s a fundamental tool in a programmer&#39;s arsenal, perfect for any problem that can be modeled as a set of objects with evolving connections. Its elegance and efficiency make it a classic and beautiful piece of computer science.</p>
<p><a href="https://fezcode.com/blog/wquwpc">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Ubuntu Once More]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/ubuntu-once-more</link>
            <guid isPermaLink="false">https://fezcode.com/blog/ubuntu-once-more</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 29 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Ubuntu Once More</h1>
<h2>Trying Ubuntu 25.10</h2>
<p>More than once a year, I get the itch to change the Linux distro I use daily. To make this easier, I bought a Lenovo IdeaPad Slim 3 to serve as my dedicated &quot;distrohopper&quot; laptop.</p>
<p>This time, however, I took a bigger leap and installed it on my main desktop PC. I had a spare SSD full of video games, which I formatted for the occasion. I downloaded the ISO, ran <a href="https://etcher.balena.io/">balenaEtcher</a>, and hoped for the best.</p>
<p>My current PC setup has two displays: one 4K and one 2K. The 2K monitor is connected via HDMI, so most Linux distros default to it as the main display. However, my 4K IPS display, connected via DisplayPort, is my actual primary. It has a 144Hz refresh rate and vibrant colors, making it perfect for my needs.</p>
<p>Unfortunately, Linux installations often disagree. Whenever I tried to install Ubuntu with both displays connected, the installation would abruptly fail. I spent two hours debugging the issue, but error messages, error codes, and online forums offered no clear explanation.</p>
<p>Finally, I spotted the word &quot;display&quot; in an error message. Drawing on past experiences with Linux distros, I decided to disconnect my 4K display. It worked! Ubuntu 25.10 installed successfully on my main PC.</p>
<p>The NTFS support is fantastic, and the EXT4 support in WSL2 is also great. It&#39;s wonderful that Windows and Linux can finally read and write to each other&#39;s filesystems.</p>
<p>GRUB, however, is currently a disaster. I can&#39;t edit the entries for some reason, and I don&#39;t want to risk breaking my setup, so I&#39;m leaving it alone for now. I might look into it tomorrow...</p>
<h2>What to do after installation</h2>
<p>The first thing I did was install <code>zsh</code>. For reasons I can&#39;t quite explain, I always install <code>oh-my-zsh</code> and <code>git</code> right away. Here&#39;s a list of my essential (not really) apps:</p>
<ul>
<li><strong>Zen Browser</strong> (my current favorite)</li>
<li><strong>Zed</strong></li>
<li><strong>CLion</strong> (Linux is the only platform I use for C++ development)</li>
<li><strong>VSCode</strong></li>
<li><strong>Sublime Text</strong> (Zed has mostly replaced it for now)</li>
<li><strong>Insomnia</strong> (and Postman)</li>
<li><strong>Spotify</strong></li>
<li><strong>Youtube Music (pear)</strong></li>
<li><strong>VLC</strong></li>
<li><strong>Obsidian</strong></li>
<li><strong>dotnet</strong></li>
<li><strong>go + golangci-lint</strong></li>
<li><strong>node.js runtime</strong></li>
<li><strong>GH CLI</strong></li>
<li><strong>Gemini CLI</strong></li>
<li><strong>GNOME Tweak Tool</strong></li>
<li><strong>GNOME Extensions</strong><ul>
<li>Apps Menu</li>
<li>ISO Clock</li>
</ul>
</li>
<li><strong>Grub Customizer</strong> (which doesn&#39;t seem to be working)</li>
</ul>
<h2>Why</h2>
<p>I&#39;m a <strong>Debian</strong> fan who loves using <strong>Fedora</strong>. I know it sounds weird, but it&#39;s true. Fedora has always been the only OS that works as seamlessly with my peripherals as Windows. I&#39;ve tried to install Debian on every machine I&#39;ve owned but could never get it to run properly. So, as a Debian enthusiast, I enjoy trying its different flavours. For some reason, Ubuntu just works. I&#39;m currently very happy with my setup. :yay:</p>
<p><a href="https://fezcode.com/blog/ubuntu-once-more">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Memoization Hooks]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-memoization-hooks</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-memoization-hooks</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>016 - React: Memoization Hooks (<code>useCallback</code>, <code>useMemo</code>) and <code>React.memo</code></h1>
<p>In React, components re-render when their state or props change. While React is highly optimized, unnecessary re-renders can sometimes impact performance, especially for complex components or frequently updated lists. Memoization techniques help prevent these unnecessary re-renders by caching computation results or function definitions.</p>
<h2>1. <code>useCallback</code> Hook</h2>
<p><code>useCallback</code> is a Hook that returns a memoized callback function. It&#39;s useful when passing callbacks to optimized child components that rely on reference equality to prevent unnecessary re-renders.</p>
<h3>Syntax</h3>
<pre><code class="language-javascript">const memoizedCallback = useCallback(
  () =&gt; {
    doSomething(a, b);
  },
  [a, b], // dependencies
);
</code></pre>
<ul>
<li>The function <code>() =&gt; { doSomething(a, b); }</code> will only be re-created if <code>a</code> or <code>b</code> changes.</li>
</ul>
<h3>Example from <code>src/components/ToastContext.js</code></h3>
<pre><code class="language-javascript">// src/components/ToastContext.js
import React, { createContext, useState, useCallback } from &#39;react&#39;;
// ...

export const ToastContext = ({ children }) =&gt; {
  const [toasts, setToasts] = useState([]);

  const addToast = useCallback((toast) =&gt; {
    const newToast = { ...toast, id: id++ };
    setToasts((prevToasts) =&gt; {
      if (prevToasts.length &gt;= 5) {
        const updatedToasts = prevToasts.slice(0, prevToasts.length - 1);
        return [newToast, ...updatedToasts];
      }
      return [newToast, ...prevToasts];
    });
  }, []); // Empty dependency array: addToast is created only once

  const removeToast = useCallback((id) =&gt; {
    setToasts((prevToasts) =&gt; prevToasts.filter((toast) =&gt; toast.id !== id));
  }, []); // Empty dependency array: removeToast is created only once

  return (
    &lt;ToastContext.Provider value={{ addToast, removeToast }}&gt;
      {/* ... */}
    &lt;/ToastContext.Provider&gt;
  );
};
</code></pre>
<p><strong>Explanation:</strong></p>
<ul>
<li>Both <code>addToast</code> and <code>removeToast</code> functions are wrapped in <code>useCallback</code> with an empty dependency array (<code>[]</code>). This means these functions are created only once when the <code>ToastContext</code> component first renders and will not change on subsequent re-renders.</li>
<li>This is important because <code>addToast</code> and <code>removeToast</code> are passed down as part of the <code>value</code> to <code>ToastContext.Provider</code>. If these functions were re-created on every render, any child component consuming this context and relying on reference equality (e.g., with <code>React.memo</code> or <code>useMemo</code>) might unnecessarily re-render.</li>
</ul>
<h2>2. <code>useMemo</code> Hook</h2>
<p><code>useMemo</code> is a Hook that returns a memoized value. It&#39;s useful for optimizing expensive calculations that don&#39;t need to be re-computed on every render.</p>
<h3>Syntax</h3>
<pre><code class="language-javascript">const memoizedValue = useMemo(() =&gt; computeExpensiveValue(a, b), [a, b]);
</code></pre>
<ul>
<li>The function <code>() =&gt; computeExpensiveValue(a, b)</code> will only execute if <code>a</code> or <code>b</code> changes. Otherwise, it returns the previously computed value.</li>
</ul>
<h3>Conceptual Example (Not directly in project, but common use case)</h3>
<p>Imagine a component that filters a large list based on some criteria:</p>
<pre><code class="language-javascript">function ProductList({ products, filterText }) {
  // This filtering operation can be expensive if products is a very large array
  const filteredProducts = products.filter(product =&gt;
    product.name.includes(filterText)
  );

  // With useMemo, the filtering only re-runs if products or filterText changes
  const memoizedFilteredProducts = useMemo(() =&gt; {
    return products.filter(product =&gt;
      product.name.includes(filterText)
    );
  }, [products, filterText]);

  return (
    &lt;div&gt;
      {memoizedFilteredProducts.map(product =&gt; (
        &lt;ProductItem key={product.id} product={product} /&gt;
      ))}
    &lt;/div&gt;
  );
}
</code></pre>
<h2>3. <code>React.memo</code> (Higher-Order Component)</h2>
<p><code>React.memo</code> is a higher-order component (HOC) that memoizes a functional component. It works similarly to <code>PureComponent</code> for class components. If the component&#39;s props are the same as the previous render, <code>React.memo</code> will skip rendering the component and reuse the last rendered result.</p>
<h3>Syntax</h3>
<pre><code class="language-javascript">const MyMemoizedComponent = React.memo(MyComponent, [arePropsEqual]);
</code></pre>
<ul>
<li><code>MyComponent</code>: The functional component to memoize.</li>
<li><code>arePropsEqual</code> (optional): A custom comparison function. If provided, React will use it to compare <code>prevProps</code> and <code>nextProps</code>. If it returns <code>true</code>, the component will not re-render.</li>
</ul>
<h3>Conceptual Example (Not directly in project, but common use case)</h3>
<pre><code class="language-javascript">// ProductItem.js
function ProductItem({ product }) {
  console.log(&#39;Rendering ProductItem&#39;, product.name);
  return &lt;li&gt;{product.name}&lt;/li&gt;;
}

export default React.memo(ProductItem);

// In ProductList component (from useMemo example)
// If ProductItem is memoized, it will only re-render if its &#39;product&#39; prop changes.
</code></pre>
<p><strong>Explanation:</strong></p>
<ul>
<li>By wrapping <code>ProductItem</code> with <code>React.memo</code>, React will perform a shallow comparison of its props. If the <code>product</code> prop (and any other props) remains the same between renders of its parent, <code>ProductItem</code> will not re-render, saving computational resources.</li>
</ul>
<h2>Summary</h2>
<p><code>useCallback</code>, <code>useMemo</code>, and <code>React.memo</code> are powerful tools for optimizing the performance of React applications by preventing unnecessary re-renders. They are particularly useful in scenarios involving expensive computations, frequently updated components, or when passing functions/objects as props to child components that rely on reference equality. While not every component needs memoization, understanding when and how to apply these techniques is crucial for building high-performance React applications.</p>
<p><a href="https://fezcode.com/blog/react-memoization-hooks">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Refs Useref]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-refs-useref</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-refs-useref</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>015 - React: <code>useRef</code> Hook</h1>
<p>The <code>useRef</code> Hook is a fundamental part of React that allows you to create mutable <code>ref</code> objects. These <code>ref</code> objects can hold a reference to a DOM element or any mutable value that persists across re-renders without causing a re-render when its value changes.</p>
<h2>Why Use <code>useRef</code>?</h2>
<p><code>useRef</code> serves two primary purposes:</p>
<ol>
<li><strong>Accessing the DOM directly</strong>: While React encourages a declarative approach to UI, there are times when you need to interact with the DOM directly (e.g., managing focus, text selection, media playback, or integrating with third-party DOM libraries).</li>
<li><strong>Storing mutable values that don&#39;t trigger re-renders</strong>: <code>useRef</code> can hold any mutable value, similar to an instance variable in a class component. Unlike <code>useState</code>, updating a <code>ref</code>&#39;s <code>.current</code> property does not trigger a re-render of the component. This is useful for storing values that need to persist across renders but whose changes don&#39;t need to be reflected in the UI immediately.</li>
</ol>
<h2>How <code>useRef</code> Works</h2>
<p><code>useRef</code> returns a plain JavaScript object with a single property called <code>current</code>. This <code>current</code> property can be initialized with an argument passed to <code>useRef</code>.</p>
<h3>Syntax</h3>
<pre><code class="language-javascript">const myRef = useRef(initialValue);
</code></pre>
<ul>
<li><code>myRef</code>: The <code>ref</code> object returned by <code>useRef</code>.</li>
<li><code>myRef.current</code>: The actual mutable value or DOM element reference.</li>
<li><code>initialValue</code>: The initial value for <code>myRef.current</code>.</li>
</ul>
<h2>Example: <code>contentRef</code> in <code>src/pages/BlogPostPage.js</code></h2>
<p>In <code>BlogPostPage.js</code>, <code>useRef</code> is used to get a direct reference to the main content <code>div</code> of the blog post. This reference is then used to calculate the reading progress based on scroll position.</p>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
import React, { useState, useEffect, useRef } from &#39;react&#39;;
// ...

const BlogPostPage = () =&gt; {
  // ...
  const contentRef = useRef(null); // Initialize contentRef with null
  // ...

  useEffect(() =&gt; {
    const handleScroll = () =&gt; {
      if (contentRef.current) { // Access the DOM element via .current
        const { scrollTop, scrollHeight, clientHeight } =
          document.documentElement;
        const totalHeight = scrollHeight - clientHeight;
        const currentProgress = (scrollTop / totalHeight) * 100;
        setReadingProgress(currentProgress);
        setIsAtTop(scrollTop === 0);
      }
    };

    window.addEventListener(&#39;scroll&#39;, handleScroll);
    return () =&gt; window.removeEventListener(&#39;scroll&#39;, handleScroll);
  }, [post]);

  return (
    // ...
    &lt;div
      ref={contentRef} // Attach the ref to the div element
      className=&quot;prose prose-xl prose-dark max-w-none&quot;
    &gt;
      {/* ... Markdown content ... */}
    &lt;/div&gt;
    // ...
  );
};
</code></pre>
<p><strong>Explanation:</strong></p>
<ol>
<li><strong><code>const contentRef = useRef(null);</code></strong>: A <code>ref</code> object named <code>contentRef</code> is created and initialized with <code>null</code>. At this point, <code>contentRef.current</code> is <code>null</code>.</li>
<li><strong><code>&lt;div ref={contentRef}&gt;</code></strong>: The <code>ref</code> object is attached to the <code>div</code> element that contains the blog post&#39;s Markdown content. Once the component renders, React will set <code>contentRef.current</code> to point to this actual DOM <code>div</code> element.</li>
<li><strong><code>if (contentRef.current)</code></strong>: Inside the <code>useEffect</code>&#39;s <code>handleScroll</code> function, <code>contentRef.current</code> is checked to ensure that the DOM element is available before attempting to access its properties (like <code>scrollHeight</code> or <code>clientHeight</code>).</li>
<li><strong><code>document.documentElement</code></strong>: While <code>contentRef.current</code> gives a reference to the specific content <code>div</code>, the scroll calculation here uses <code>document.documentElement</code> (the <code>&lt;html&gt;</code> element) to get the overall page scroll position and dimensions. This is a common pattern for tracking global scroll progress.</li>
</ol>
<h3><code>useRef</code> vs. <code>useState</code></h3>
<p>It&#39;s important to understand when to use <code>useRef</code> versus <code>useState</code>:</p>
<table>
<thead>
<tr>
<th align="left">Feature</th>
<th align="left"><code>useState</code></th>
<th align="left"><code>useRef</code></th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>Purpose</strong></td>
<td align="left">Manages state that triggers re-renders.</td>
<td align="left">Accesses DOM elements or stores mutable values that <em>don&#39;t</em> trigger re-renders.</td>
</tr>
<tr>
<td align="left"><strong>Re-renders</strong></td>
<td align="left">Updates to state variables cause component re-renders.</td>
<td align="left">Updates to <code>ref.current</code> do <em>not</em> cause re-renders.</td>
</tr>
<tr>
<td align="left"><strong>Value Persistence</strong></td>
<td align="left">Value persists across re-renders.</td>
<td align="left">Value persists across re-renders.</td>
</tr>
<tr>
<td align="left"><strong>Mutability</strong></td>
<td align="left">State is generally treated as immutable (updated via <code>setState</code>).</td>
<td align="left"><code>ref.current</code> is directly mutable.</td>
</tr>
</tbody></table>
<p><strong>When to use <code>useRef</code>:</strong></p>
<ul>
<li>Managing focus, text selection, or media playback.</li>
<li>Triggering imperative animations.</li>
<li>Integrating with third-party DOM libraries.</li>
<li>Storing any mutable value that you don&#39;t want to trigger a re-render when it changes (e.g., a timer ID, a previous value of a prop).</li>
</ul>
<h2>Summary</h2>
<p><code>useRef</code> provides a way to &quot;escape&quot; React&#39;s declarative paradigm when necessary, offering direct access to the underlying DOM or a persistent mutable storage for values that don&#39;t need to be part of the component&#39;s reactive state. It&#39;s a powerful tool for specific use cases where direct imperative manipulation or persistent non-state values are required.</p>
<p><a href="https://fezcode.com/blog/react-refs-useref">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[How React Toasts Work in `fezcodex`]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-toast-explanation-in-details</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-toast-explanation-in-details</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Deep Dive: How React Toasts Work in <code>fezcodex</code></h1>
<p>Toast notifications are a staple of modern web applications. They provide non-intrusive feedback to users about the result of their actions. In the <code>fezcodex</code> project, we have a robust and reusable toast system. This article will break down how it works, from its architecture to the React magic that holds it all together.</p>
<h2>Part 1: The Architecture - A Tale of Three Components</h2>
<p>The toast system is elegantly designed around three key parts that work in harmony:</p>
<ol>
<li><p><strong><code>ToastContext.js</code> (The Brains):</strong> This is the central manager. It wraps our entire application, creating a &quot;context&quot; that any component can plug into. It holds the list of all active toasts and provides the functions (<code>addToast</code>, <code>removeToast</code>) to modify that list. It&#39;s also responsible for rendering the container where the toasts appear.</p>
</li>
<li><p><strong><code>useToast.js</code> (The Public API):</strong> This is a custom React Hook that acts as a clean and simple gateway. Instead of components needing to know about the underlying context, they can just use this hook to get access to the <code>addToast</code> function. It&#39;s the &quot;button&quot; that other components press to request a toast.</p>
</li>
<li><p><strong><code>Toast.js</code> (The Notification UI):</strong> This component represents a single toast message. It&#39;s responsible for its own appearance, animations, and, most importantly, its own demise. It knows how long it should be on screen and contains the logic to remove itself after its time is up.</p>
</li>
</ol>
<h2>Part 2: The Magic of <code>useState</code> - Where Does the State Go?</h2>
<p>This is the crucial question. In <code>ToastContext.js</code>, we have this line:</p>
<pre><code class="language-javascript">const [toasts, setToasts] = useState([]);
</code></pre>
<p>When a component function runs, all its internal variables are created and then discarded when it&#39;s done. So how does the <code>toasts</code> array not just reset to <code>[]</code> every single time?</p>
<p><strong>React Remembers.</strong></p>
<p>The <code>useState</code> hook is a request to React to create and manage a piece of state <em>on behalf of</em> your component.</p>
<ol>
<li><p><strong>First Render:</strong> The very first time <code>ToastContext</code> renders, React sees <code>useState([])</code>. It creates a &quot;memory cell&quot; for this specific component instance and puts an empty array <code>[]</code> inside it. It then returns that array to the component as the <code>toasts</code> variable.</p>
</li>
<li><p><strong>State Updates:</strong> When you call <code>addToast</code>, it eventually calls <code>setToasts(...)</code>. This function doesn&#39;t change the state directly. Instead, it sends a message to React saying, &quot;I have a new value for this state. Please update it and re-render the component.&quot;</p>
</li>
<li><p><strong>Subsequent Renders:</strong> When React re-renders <code>ToastContext</code>, it arrives at the <code>useState([])</code> line again. But this time, React knows it has already created a state for this component. It <strong>ignores the initial value (<code>[]</code>)</strong> and instead provides the <em>current</em> value from its internal memory—the updated array of toasts.</p>
</li>
</ol>
<p>This is the fundamental principle of React Hooks: they allow your function components to have stateful logic that persists across renders, managed by React itself.</p>
<h2>Part 3: The Full Lifecycle of a Toast</h2>
<p>Let&#39;s tie it all together by following a single toast from birth to death.</p>
<ol>
<li><p><strong>The Call:</strong> A user performs an action in a component (e.g., the Word Counter). That component calls <code>addToast({ title: &#39;Success!&#39;, ... })</code>.</p>
</li>
<li><p><strong>The Context:</strong> The <code>useToast</code> hook provides the <code>addToast</code> function from the <code>ToastContext</code>&#39;s context.</p>
</li>
<li><p><strong>The State Update:</strong> The <code>addToast</code> function in <code>ToastContext</code> runs. It creates a new toast object with a unique ID and calls <code>setToasts([newToast, ...otherToasts])</code>.</p>
</li>
<li><p><strong>The Re-render:</strong> React receives the state update request and schedules a re-render for <code>ToastContext</code>.</p>
</li>
<li><p><strong>The Render:</strong> <code>ToastContext</code> runs again. It calls <code>useState</code>, and React hands it the <strong>new array</strong> containing our new toast. The component&#39;s <code>return</code> statement is executed, and its <code>.map()</code> function now loops over an array that includes the new toast.</p>
</li>
<li><p><strong>The Birth:</strong> A new <code>&lt;Toast /&gt;</code> component is rendered on the screen. It receives its <code>id</code>, <code>title</code>, <code>message</code>, and <code>duration</code> as props.</p>
</li>
<li><p><strong>The Countdown:</strong> Inside the new <code>&lt;Toast /&gt;</code> component, a <code>useEffect</code> hook fires. It starts a <code>setTimeout</code> timer for the given <code>duration</code>.</p>
</li>
<li><p><strong>The End:</strong> When the timer finishes, it calls the <code>removeToast(id)</code> function that was passed down as a prop.</p>
</li>
<li><p><strong>The Cleanup:</strong> <code>removeToast</code> in the <code>ToastContext</code> calls <code>setToasts(...)</code> again, this time with an array that <em>filters out</em> the toast with the matching ID.</p>
</li>
<li><p><strong>The Final Re-render:</strong> React processes the state update, re-renders the <code>ToastContext</code>, and the toast is no longer in the array. It vanishes from the screen.</p>
</li>
</ol>
<h2>Conclusion</h2>
<p>The <code>fezcodex</code> toast system is a perfect microcosm of modern React development. It shows how to use <strong>Context</strong> to provide global functionality without cluttering components, and it relies on the magic of the <strong><code>useState</code> hook</strong> to give components a memory that persists between renders. By letting React manage the state, we can write declarative UI that simply reacts to state changes.</p>
<p><a href="https://fezcode.com/blog/react-toast-explanation-in-details">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Custom Hooks]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-custom-hooks</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-custom-hooks</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>014 - React: Custom Hooks</h1>
<p>Custom Hooks are a powerful feature in React that allow you to extract reusable stateful logic from components. They are JavaScript functions whose names start with <code>use</code> and that can call other Hooks. Custom Hooks solve the problem of sharing logic between components without relying on prop drilling or complex patterns like render props or higher-order components.</p>
<h2>Why Use Custom Hooks?</h2>
<ol>
<li><strong>Reusability</strong>: Extract common logic (state, effects, context) into a single function that can be used across multiple components.</li>
<li><strong>Readability</strong>: Components become cleaner and easier to understand as their logic is separated from their UI concerns.</li>
<li><strong>Maintainability</strong>: Changes to shared logic only need to be made in one place.</li>
<li><strong>Testability</strong>: Logic extracted into custom hooks can often be tested more easily in isolation.</li>
</ol>
<h2>How to Create a Custom Hook</h2>
<p>A custom Hook is a JavaScript function that:</p>
<ul>
<li>Starts with the word <code>use</code> (e.g., <code>useFriendStatus</code>, <code>useToast</code>). This naming convention is crucial for React to know that it&#39;s a Hook and to apply the rules of Hooks (e.g., only call Hooks at the top level of a React function).</li>
<li>Can call other Hooks (e.g., <code>useState</code>, <code>useEffect</code>, <code>useContext</code>).</li>
<li>Can return anything: stateful values, functions, or nothing.</li>
</ul>
<h2>Example: <code>useToast</code> Custom Hook (<code>src/hooks/useToast.js</code>)</h2>
<p>This project provides an excellent example of a custom hook: <code>useToast</code>. It encapsulates the logic for accessing the toast notification system&#39;s <code>addToast</code> and <code>removeToast</code> functions.</p>
<h3><code>src/hooks/useToast.js</code></h3>
<pre><code class="language-javascript">import { useContext } from &#39;react&#39;;
import { ToastContext } from &#39;../components/ToastContext&#39;;

export const useToast = () =&gt; {
  return useContext(ToastContext);
};
</code></pre>
<p><strong>Explanation:</strong></p>
<ol>
<li><strong><code>import { useContext } from &#39;react&#39;;</code></strong>: The custom hook itself uses another built-in Hook, <code>useContext</code>, to access the value provided by the <code>ToastContext</code>.</li>
<li><strong><code>import { ToastContext } from &#39;../components/ToastContext&#39;;</code></strong>: It imports the <code>ToastContext</code> object, which was created in <code>ToastContext.js</code>.</li>
<li><strong><code>export const useToast = () =&gt; { ... };</code></strong>: This defines the custom hook. Its name <code>useToast</code> clearly indicates its purpose and follows the naming convention.</li>
<li><strong><code>return useContext(ToastContext);</code></strong>: The core of this hook. It retrieves the <code>value</code> (which contains <code>addToast</code> and <code>removeToast</code> functions) from the nearest <code>ToastContext.Provider</code> in the component tree and returns it. This means any component calling <code>useToast()</code> will receive these functions.</li>
</ol>
<h3>How <code>useToast</code> is Used in a Component (e.g., <code>BlogPostPage.js</code>)</h3>
<pre><code class="language-javascript">// Inside BlogPostPage.js (or any other component that needs toasts)
import { useToast } from &#39;../hooks/useToast&#39;;

const CodeBlock = ({ /* ... */ }) =&gt; {
  const { addToast } = useToast(); // Access addToast function

  const handleCopy = () =&gt; {
    // ... copy logic ...
    addToast({
      title: &#39;Success&#39;,
      message: &#39;Copied to clipboard!&#39;,
      duration: 3000,
    });
    // ...
  };
  // ...
};
</code></pre>
<p>By calling <code>const { addToast } = useToast();</code>, the <code>CodeBlock</code> component (or any other component) gains direct access to the <code>addToast</code> function without needing to know where <code>ToastContext</code> is defined or how the toast state is managed. This makes the <code>CodeBlock</code> component cleaner and more focused on its primary responsibility.</p>
<h2>Another Potential Custom Hook (Conceptual Example)</h2>
<p>Consider the scroll tracking logic in <code>BlogPostPage.js</code>:</p>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js - inside BlogPostPage component
const [readingProgress, setReadingProgress] = useState(0);
const [isAtTop, setIsAtTop] = useState(true);
const contentRef = useRef(null);

useEffect(() =&gt; {
  const handleScroll = () =&gt; {
    if (contentRef.current) {
      const { scrollTop, scrollHeight, clientHeight } =
        document.documentElement;
      const totalHeight = scrollHeight - clientClientHeight;
      const currentProgress = (scrollTop / totalHeight) * 100;
      setReadingProgress(currentProgress);
      setIsAtTop(scrollTop === 0);
    }
  };

  window.addEventListener(&#39;scroll&#39;, handleScroll);
  return () =&gt; window.removeEventListener(&#39;scroll&#39;, handleScroll);
}, [post]);
</code></pre>
<p>This logic could be extracted into a custom hook, for example, <code>useScrollProgress</code>:</p>
<pre><code class="language-javascript">// src/hooks/useScrollProgress.js (Conceptual)
import { useState, useEffect, useRef } from &#39;react&#39;;

const useScrollProgress = (contentRef, dependency) =&gt; {
  const [readingProgress, setReadingProgress] = useState(0);
  const [isAtTop, setIsAtTop] = useState(true);

  useEffect(() =&gt; {
    const handleScroll = () =&gt; {
      if (contentRef.current) {
        const { scrollTop, scrollHeight, clientHeight } =
          document.documentElement;
        const totalHeight = scrollHeight - clientHeight;
        const currentProgress = (scrollTop / totalHeight) * 100;
        setReadingProgress(currentProgress);
        setIsAtTop(scrollTop === 0);
      }
    };

    window.addEventListener(&#39;scroll&#39;, handleScroll);
    return () =&gt; window.removeEventListener(&#39;scroll&#39;, handleScroll);
  }, [contentRef, dependency]); // Re-run if contentRef or dependency changes

  return { readingProgress, isAtTop };
};

export default useScrollProgress;
</code></pre>
<p>Then, <code>BlogPostPage.js</code> would become cleaner:</p>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js - inside BlogPostPage component
const contentRef = useRef(null);
const { readingProgress, isAtTop } = useScrollProgress(contentRef, post);
// ...
</code></pre>
<p>This demonstrates how custom hooks can abstract away complex logic, making components more focused and easier to read.</p>
<h2>Summary</h2>
<p>Custom Hooks are a fundamental pattern in modern React development for sharing stateful logic. By following the <code>use</code> naming convention and leveraging other built-in Hooks, you can create highly reusable and maintainable code that enhances the overall architecture of your React applications.</p>
<p><a href="https://fezcode.com/blog/react-custom-hooks">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Document Fetching Api]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/document-fetching-api</link>
            <guid isPermaLink="false">https://fezcode.com/blog/document-fetching-api</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>013 - Document Fetching with the <code>fetch</code> API</h1>
<p>In modern web applications, fetching data from a server is a fundamental operation. The <code>fetch</code> API provides a powerful and flexible interface for making network requests, replacing older methods like <code>XMLHttpRequest</code>. This project uses <code>fetch</code> to retrieve blog post content and metadata.</p>
<h2>The <code>fetch</code> API Basics</h2>
<p>The <code>fetch()</code> method starts the process of fetching a resource from the network, returning a <code>Promise</code> that fulfills once the response is available. A <code>fetch()</code> call takes one mandatory argument, the path to the resource you want to fetch.</p>
<h3>Basic Usage</h3>
<pre><code class="language-javascript">fetch(url)
  .then(response =&gt; response.json()) // or .text(), .blob(), etc.
  .then(data =&gt; console.log(data))
  .catch(error =&gt; console.error(&#39;Error:&#39;, error));
</code></pre>
<ul>
<li><strong><code>fetch(url)</code></strong>: Initiates the request. Returns a <code>Promise</code> that resolves to a <code>Response</code> object.</li>
<li><strong><code>response.json()</code> / <code>response.text()</code></strong>: The <code>Response</code> object has methods to extract the body content. <code>json()</code> parses the response as JSON, while <code>text()</code> parses it as plain text. Both return a <code>Promise</code>.</li>
<li><strong><code>.then()</code></strong>: Handles the successful resolution of a Promise.</li>
<li><strong><code>.catch()</code></strong>: Handles any errors that occur during the fetch operation or in the subsequent <code>.then()</code> blocks.</li>
</ul>
<h2>Example from <code>src/pages/BlogPostPage.js</code></h2>
<p>Let&#39;s look at how <code>fetch</code> is used in <code>BlogPostPage.js</code> to get both the blog post&#39;s text content and its metadata.</p>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js - inside the useEffect&#39;s fetchPost function
// ...
try {
  const [postContentResponse, shownPostsResponse] = await Promise.all([
    fetch(`/posts/${currentSlug}.txt`),
    fetch(&#39;/posts/shownPosts.json&#39;),
  ]);

  // Handling post content response
  let postBody = &#39;&#39;;
  if (postContentResponse.ok) { // Check if the HTTP status code is in the 200-299 range
    postBody = await postContentResponse.text(); // Extract response body as text
    // Additional check for HTML fallback content
    if (postBody.trim().startsWith(&#39;&lt;!DOCTYPE html&gt;&#39;)) {
      console.error(&#39;Fetched content is HTML, not expected post content for:&#39;, currentSlug);
      navigate(&#39;/404&#39;);
      return;
    }
  } else {
    console.error(&#39;Failed to fetch post content for:&#39;, currentSlug);
    navigate(&#39;/404&#39;);
    return;
  }

  // Handling metadata response
  let postMetadata = null;
  if (shownPostsResponse.ok) { // Check if the HTTP status code is in the 200-299 range
    const allPosts = await shownPostsResponse.json(); // Extract response body as JSON
    postMetadata = allPosts.find((item) =&gt; item.slug === currentSlug);
    // ... further processing of series posts
  } else {
    console.error(&#39;Failed to fetch shownPosts.json&#39;);
  }

  // Final check and state update
  if (postMetadata &amp;&amp; postContentResponse.ok) {
    setPost({ attributes: postMetadata, body: postBody, seriesPosts });
  } else {
    setPost({ attributes: { title: &#39;Post not found&#39; }, body: &#39;&#39; });
  }
} catch (error) {
  console.error(&#39;Error fetching post or shownPosts.json:&#39;, error);
  setPost({ attributes: { title: &#39;Error loading post&#39; }, body: &#39;&#39; });
} finally {
  setLoading(false);
}
// ...
</code></pre>
<h3>Explanation of <code>fetch</code> Usage in <code>BlogPostPage.js</code>:</h3>
<ol>
<li><p><strong><code>Promise.all([...])</code></strong>: As discussed in <code>011-javascript-fundamentals.md</code>, <code>Promise.all</code> is used to concurrently fetch two resources:</p>
<ul>
<li><code>fetch(&quot;/posts/${currentSlug}.txt&quot;)</code>: Fetches the actual Markdown content of the blog post. The <code>currentSlug</code> is dynamically inserted into the URL.</li>
<li><code>fetch(&#39;/posts/shownPosts.json&#39;)</code>: Fetches a JSON file containing metadata for all blog posts.</li>
</ul>
</li>
<li><p><strong><code>response.ok</code> Property</strong>: After a <code>fetch</code> call, the <code>Response</code> object has an <code>ok</code> property. This is a boolean that indicates whether the HTTP response status is in the <code>200-299</code> range (inclusive). It&#39;s crucial to check <code>response.ok</code> because <code>fetch</code> does <em>not</em> throw an error for HTTP error statuses (like 404 or 500) by default; it only throws an error for network failures.</p>
</li>
<li><p><strong><code>response.text()</code> and <code>response.json()</code></strong>: These methods are used to parse the response body:</p>
<ul>
<li><code>postContentResponse.text()</code>: Used for the <code>.txt</code> file, as it contains plain text (Markdown).</li>
<li><code>shownPostsResponse.json()</code>: Used for the <code>.json</code> file, as it contains structured JSON data.</li>
</ul>
</li>
<li><p><strong>Error Handling (HTTP Status)</strong>:</p>
<ul>
<li>If <code>postContentResponse.ok</code> is <code>false</code> (meaning the <code>.txt</code> file was not found or returned an error status), an error is logged, and the application navigates to the <code>/404</code> page using <code>navigate(&#39;/404&#39;)</code>.</li>
<li>A specific check <code>if (postBody.trim().startsWith(&#39;&lt;!DOCTYPE html&gt;&#39;))</code> was added to handle the scenario where the development server might return the <code>index.html</code> (with a 200 status) instead of a 404 for a non-existent file. This ensures that even in such cases, the user is redirected to the 404 page.</li>
<li>If <code>shownPostsResponse.ok</code> is <code>false</code>, an error is logged, but the application doesn&#39;t navigate to 404 directly, as the post content might still be available, just without rich metadata.</li>
</ul>
</li>
<li><p><strong><code>try...catch</code> Block</strong>: The entire asynchronous operation is wrapped in a <code>try...catch</code> block. This catches any network errors (e.g., server unreachable) or errors that occur during the processing of the Promises (e.g., <code>json()</code> parsing error). If an error occurs, it&#39;s logged, and the <code>post</code> state is set to indicate an error.</p>
</li>
<li><p><strong><code>finally</code> Block</strong>: The <code>setLoading(false)</code> call is placed in a <code>finally</code> block. This ensures that the loading state is always turned off, regardless of whether the <code>fetch</code> operation succeeded or failed.</p>
</li>
</ol>
<h2>Summary</h2>
<p>The <code>fetch</code> API is a modern, Promise-based way to make network requests in JavaScript. By understanding how to use <code>fetch</code> with <code>async/await</code>, handle <code>Response</code> objects (especially <code>response.ok</code>), and implement robust error handling with <code>try...catch</code>, developers can effectively retrieve and process data from various sources, as demonstrated in the Fezcode project&#39;s <code>BlogPostPage.js</code> component.</p>
<p><a href="https://fezcode.com/blog/document-fetching-api">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Html Structure]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/html-structure</link>
            <guid isPermaLink="false">https://fezcode.com/blog/html-structure</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>012 - HTML Structure (<code>public/index.html</code>)</h1>
<p><code>public/index.html</code> is the single HTML page that serves as the entry point for your React application. When a user visits your website, this is the file their browser first loads. The React application then takes over to dynamically render content into this HTML structure.</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot; class=&quot;dark&quot;&gt;
  &lt;head&gt;
    &lt;meta charset=&quot;utf-8&quot; /&gt;
    &lt;link rel=&quot;icon&quot; href=&quot;%PUBLIC_URL%/favicon.ico&quot; /&gt;
    &lt;link rel=&quot;icon&quot; type=&quot;image/svg+xml&quot; href=&quot;%PUBLIC_URL%/favicon.svg&quot; /&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot; /&gt;
    &lt;meta name=&quot;theme-color&quot; content=&quot;#000000&quot; /&gt;
    &lt;meta
      name=&quot;description&quot;
      content=&quot;codex by fezcode...&quot;
    /&gt;
    &lt;link rel=&quot;apple-touch-icon&quot; href=&quot;%PUBLIC_URL%/logo192.png&quot; /&gt;
    &lt;link rel=&quot;manifest&quot; href=&quot;%PUBLIC_URL%/manifest.json&quot; /&gt;
    &lt;link rel=&quot;preconnect&quot; href=&quot;https://fonts.googleapis.com&quot;&gt;
    &lt;link rel=&quot;preconnect&quot; href=&quot;https://fonts.gstatic.com&quot; crossorigin&gt;
    &lt;link href=&quot;https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&amp;family=Space+Mono:wght@400;700&amp;display=swap&quot; rel=&quot;stylesheet&quot;&gt;
    &lt;link href=&quot;https://fonts.googleapis.com/css2?family=Arvo&amp;family=Inter&amp;family=Playfair+Display&amp;display=swap&quot; rel=&quot;stylesheet&quot;&gt;
    &lt;title&gt;fezcodex&lt;/title&gt;
  &lt;/head&gt;
  &lt;body class=&quot;bg-slate-950&quot;&gt;
    &lt;noscript&gt;You need to enable JavaScript to run this app.&lt;/noscript&gt;
    &lt;div id=&quot;root&quot;&gt;&lt;/div&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h2>Explanation of Key Sections</h2>
<h3><code>&lt;!DOCTYPE html&gt;</code></h3>
<ul>
<li>This declaration defines the document type to be HTML5.</li>
</ul>
<h3><code>&lt;html lang=&quot;en&quot; class=&quot;dark&quot;&gt;</code></h3>
<ul>
<li>The root element of an HTML page.</li>
<li><code>lang=&quot;en&quot;</code>: Specifies the primary language of the document content as English, which is important for accessibility and search engines.</li>
<li><code>class=&quot;dark&quot;</code>: This class is likely used in conjunction with Tailwind CSS&#39;s dark mode configuration (<code>darkMode: &#39;class&#39;</code> in <code>tailwind.config.js</code>). When this class is present on the <code>&lt;html&gt;</code> element, Tailwind will apply dark mode styles.</li>
</ul>
<h3><code>&lt;head&gt;</code> Section</h3>
<p>The <code>&lt;head&gt;</code> section contains metadata about the HTML document, which is not displayed on the web page itself but is crucial for browsers, search engines, and other web services.</p>
<ul>
<li><strong><code>&lt;meta charset=&quot;utf-8&quot; /&gt;</code></strong>: Specifies the character encoding for the document, ensuring proper display of various characters.</li>
<li><strong><code>&lt;link rel=&quot;icon&quot; href=&quot;%PUBLIC_URL%/favicon.ico&quot; /&gt;</code></strong>: Links to the favicon, the small icon displayed in the browser tab or bookmark list. <code>%PUBLIC_URL%</code> is a placeholder that will be replaced with the public URL of your app during the build process.</li>
<li><strong><code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot; /&gt;</code></strong>: Configures the viewport for responsive design. It sets the width of the viewport to the device width and the initial zoom level to 1, ensuring the page scales correctly on different devices.</li>
<li><strong><code>&lt;meta name=&quot;theme-color&quot; content=&quot;#000000&quot; /&gt;</code></strong>: Suggests a color that browsers should use to tint the UI elements (like the address bar in mobile browsers) of the page.</li>
<li><strong><code>&lt;meta name=&quot;description&quot; content=&quot;codex by fezcode...&quot; /&gt;</code></strong>: Provides a brief, high-level description of the web page content. This is often used by search engines in search results.</li>
<li><strong><code>&lt;link rel=&quot;apple-touch-icon&quot; href=&quot;%PUBLIC_URL%/logo192.png&quot; /&gt;</code></strong>: Specifies an icon for web clips on iOS devices.</li>
<li><strong><code>&lt;link rel=&quot;manifest&quot; href=&quot;%PUBLIC_URL%/manifest.json&quot; /&gt;</code></strong>: Links to a web app manifest file, which provides information about the web application (like name, icons, start URL) in a JSON text file. This is essential for Progressive Web Apps (PWAs).</li>
<li><strong><code>&lt;link rel=&quot;preconnect&quot; ...&gt;</code> and <code>&lt;link href=&quot;https://fonts.googleapis.com/css2?...&quot; rel=&quot;stylesheet&quot;&gt;</code></strong>: These lines are used to preconnect to Google Fonts and import custom fonts (<code>JetBrains Mono</code>, <code>Space Mono</code>, <code>Arvo</code>, <code>Inter</code>, <code>Playfair Display</code>). <code>preconnect</code> helps establish early connections to improve font loading performance.</li>
<li><strong><code>&lt;title&gt;fezcodex&lt;/title&gt;</code></strong>: Sets the title of the HTML document, which appears in the browser tab or window title bar.</li>
</ul>
<h3><code>&lt;body&gt;</code> Section</h3>
<p>The <code>&lt;body&gt;</code> section contains all the content that is visible to the user.</p>
<ul>
<li><strong><code>&lt;body class=&quot;bg-slate-950&quot;&gt;</code></strong>: The main content area of the page. The <code>bg-slate-950</code> class is a Tailwind CSS utility class that sets the background color of the body to a very dark slate color, consistent with the project&#39;s dark theme.</li>
<li><strong><code>&lt;noscript&gt;You need to enable JavaScript to run this app.&lt;/noscript&gt;</code></strong>: This content is displayed only if the user&#39;s browser has JavaScript disabled. Since React is a JavaScript library, the application cannot function without JavaScript.</li>
<li><strong><code>&lt;div id=&quot;root&quot;&gt;&lt;/div&gt;</code></strong>: This is the most crucial part for a React application. It&#39;s an empty <code>div</code> element with the ID <code>root</code>. This is the DOM node where your React application (specifically, the <code>App</code> component rendered by <code>src/index.js</code>) will be mounted and take control. All of your React components will be rendered as children of this <code>div</code>.</li>
</ul>
<h2>How React Mounts</h2>
<p>As explained in <code>003-index-js-entry-point.md</code>:</p>
<pre><code class="language-javascript">// src/index.js
const root = ReactDOM.createRoot(document.getElementById(&#39;root&#39;));
root.render(
  &lt;React.StrictMode&gt;
    &lt;App /&gt;
  &lt;/React.StrictMode&gt;,
);
</code></pre>
<ol>
<li>The JavaScript code in <code>src/index.js</code> (which is eventually bundled and loaded by the browser) finds the <code>&lt;div id=&quot;root&quot;&gt;</code> element.</li>
<li><code>ReactDOM.createRoot()</code> creates a React root, which is the entry point for React to manage the DOM inside that element.</li>
<li><code>root.render(&lt;App /&gt;)</code> then tells React to render your main <code>App</code> component (and all its children) inside this <code>root</code> div. From this point on, React efficiently updates and manages the content within this <code>div</code> based on your component&#39;s state and props.</li>
</ol>
<h2>Summary</h2>
<p><code>public/index.html</code> provides the foundational HTML structure and metadata for the web page. It&#39;s a relatively simple file because the React application dynamically generates and manages most of the visible content within the designated <code>&lt;div id=&quot;root&quot;&gt;</code>. This separation allows for a highly dynamic and interactive user experience powered by React.</p>
<p><a href="https://fezcode.com/blog/html-structure">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Javascript Fundamentals]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/javascript-fundamentals</link>
            <guid isPermaLink="false">https://fezcode.com/blog/javascript-fundamentals</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>011 - JavaScript Fundamentals in the Project</h1>
<p>This project heavily utilizes modern JavaScript features to build a dynamic and interactive user interface. Understanding these fundamental concepts is crucial for comprehending the codebase. This document will highlight several key JavaScript concepts with examples drawn from the project.</p>
<h2>1. <code>async</code>/<code>await</code> for Asynchronous Operations</h2>
<p>Asynchronous operations (like fetching data from a server) are common in web applications. <code>async</code>/<code>await</code> provides a cleaner, more readable way to handle Promises.</p>
<ul>
<li><strong><code>async</code> function</strong>: A function declared with <code>async</code> always returns a Promise. It allows you to use the <code>await</code> keyword inside it.</li>
<li><strong><code>await</code> keyword</strong>: Can only be used inside an <code>async</code> function. It pauses the execution of the <code>async</code> function until the Promise it&#39;s waiting for settles (either resolves or rejects), and then resumes the <code>async</code> function&#39;s execution with the resolved value.</li>
</ul>
<h3>Example from <code>src/pages/BlogPostPage.js</code></h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
useEffect(() =&gt; {
  const fetchPost = async () =&gt; { // async function
    setLoading(true);
    try {
      const [postContentResponse, shownPostsResponse] = await Promise.all([ // await Promise.all
        fetch(`/posts/${currentSlug}.txt`),
        fetch(&#39;/posts/shownPosts.json&#39;),
      ]);

      let postBody = &#39;&#39;;
      if (postContentResponse.ok) {
        postBody = await postContentResponse.text(); // await fetch response
        // ...
      }
      // ...
    } catch (error) {
      console.error(&#39;Error fetching post or shownPosts.json:&#39;, error);
      // ...
    } finally {
      setLoading(false);
    }
  };

  fetchPost();
}, [currentSlug]);
</code></pre>
<ul>
<li>The <code>fetchPost</code> function is declared <code>async</code> because it performs asynchronous network requests.</li>
<li><code>await Promise.all([...])</code> is used to wait for multiple <code>fetch</code> calls (which return Promises) to complete concurrently. This is more efficient than awaiting them one after another if they don&#39;t depend on each other.</li>
<li><code>await postContentResponse.text()</code> waits for the response body to be fully read as text.</li>
<li>The <code>try...catch...finally</code> block is used for error handling and ensuring <code>setLoading(false)</code> is always called.</li>
</ul>
<h2>2. <code>Promise.all</code> for Concurrent Promises</h2>
<p><code>Promise.all</code> is a Promise combinator that takes an iterable of Promises as input and returns a single Promise. This returned Promise fulfills when all of the input&#39;s Promises have fulfilled, or rejects as soon as any of the input&#39;s Promises rejects.</p>
<h3>Example from <code>src/pages/BlogPostPage.js</code></h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
const [postContentResponse, shownPostsResponse] = await Promise.all([
  fetch(`/posts/${currentSlug}.txt`),
  fetch(&#39;/posts/shownPosts.json&#39;),
]);
</code></pre>
<ul>
<li>Here, <code>Promise.all</code> is used to initiate two network requests (<code>fetch</code> for the post content and <code>fetch</code> for the metadata JSON) at the same time. The <code>await</code> keyword then waits for both of them to complete. The results are destructured into <code>postContentResponse</code> and <code>shownPostsResponse</code>.</li>
</ul>
<h2>3. Array Methods (<code>filter</code>, <code>find</code>, <code>sort</code>)</h2>
<p>Modern JavaScript provides powerful array methods that make working with collections of data much easier and more declarative.</p>
<h3>Example from <code>src/pages/BlogPostPage.js</code></h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
// ... inside fetchPost function
if (shownPostsResponse.ok) {
  const allPosts = await shownPostsResponse.json();
  postMetadata = allPosts.find((item) =&gt; item.slug === currentSlug); // find

  if (postMetadata &amp;&amp; postMetadata.series) {
    seriesPosts = allPosts
      .filter((item) =&gt; item.series === postMetadata.series) // filter
      .sort((a, b) =&gt; a.seriesIndex - b.seriesIndex); // sort
  }
}
</code></pre>
<ul>
<li><strong><code>Array.prototype.find()</code></strong>: Returns the value of the first element in the provided array that satisfies the provided testing function. Otherwise, <code>undefined</code> is returned.<ul>
<li><code>allPosts.find((item) =&gt; item.slug === currentSlug)</code>: Finds the first post object in <code>allPosts</code> whose <code>slug</code> property matches <code>currentSlug</code>.</li>
</ul>
</li>
<li><strong><code>Array.prototype.filter()</code></strong>: Creates a new array with all elements that pass the test implemented by the provided function.<ul>
<li><code>allPosts.filter((item) =&gt; item.series === postMetadata.series)</code>: Creates a new array containing only posts that belong to the same series as the current post.</li>
</ul>
</li>
<li><strong><code>Array.prototype.sort()</code></strong>: Sorts the elements of an array in place and returns the sorted array. The default sort order is ascending, built upon converting the elements into strings, then comparing their sequences of UTF-16 code units.<ul>
<li><code>.sort((a, b) =&gt; a.seriesIndex - b.seriesIndex)</code>: Sorts the <code>seriesPosts</code> array numerically based on their <code>seriesIndex</code> property in ascending order.</li>
</ul>
</li>
</ul>
<h2>4. Object Destructuring</h2>
<p>Object destructuring is a JavaScript expression that makes it possible to unpack values from arrays, or properties from objects, into distinct variables.</p>
<h3>Example from <code>src/pages/BlogPostPage.js</code></h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
const { slug, seriesSlug, episodeSlug } = useParams();
// ...
</code></pre>
<ul>
<li>Here, <code>useParams()</code> returns an object containing URL parameters. Object destructuring is used to extract the <code>slug</code>, <code>seriesSlug</code>, and <code>episodeSlug</code> properties directly into variables with the same names.</li>
</ul>
<h3>Example from <code>src/components/Layout.js</code></h3>
<pre><code class="language-javascript">// src/components/Layout.js
const Layout = ({ children }) =&gt; {
  // ...
};
</code></pre>
<ul>
<li>In this functional component definition, <code>({ children })</code> is using object destructuring to directly extract the <code>children</code> prop from the <code>props</code> object that React passes to the component.</li>
</ul>
<h2>5. Ternary Operator</h2>
<p>The ternary operator (<code>condition ? exprIfTrue : exprIfFalse</code>) is a shorthand for an <code>if...else</code> statement, often used for conditional rendering or assigning values.</p>
<h3>Example from <code>src/pages/BlogPostPage.js</code></h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
const currentSlug = episodeSlug || slug; // Use episodeSlug if present, otherwise use slug
// ...
const backLink = seriesSlug ? `/blog/series/${seriesSlug}` : &#39;/blog&#39;;
const backLinkText = seriesSlug ? &#39;Back to Series&#39; : &#39;Back to Blog&#39;;
</code></pre>
<ul>
<li><code>episodeSlug || slug</code>: This uses the logical OR operator (<code>||</code>) to assign <code>episodeSlug</code> if it&#39;s truthy, otherwise it assigns <code>slug</code>. This is a common pattern for providing fallback values.</li>
<li><code>seriesSlug ? </code>/blog/series/${seriesSlug}<code> : &#39;/blog&#39;</code>: If <code>seriesSlug</code> is truthy, <code>backLink</code> is set to the series URL; otherwise, it defaults to the general blog URL.</li>
</ul>
<h2>Summary</h2>
<p>These JavaScript fundamentals, including asynchronous programming with <code>async</code>/<code>await</code> and <code>Promise.all</code>, efficient data manipulation with array methods, concise variable assignment with object destructuring, and conditional logic with the ternary operator, are extensively used throughout the Fezcode project. Mastering these concepts is key to understanding and contributing to modern React applications.</p>
<p><a href="https://fezcode.com/blog/javascript-fundamentals">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Css And Tailwind Css]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/css-and-tailwind-css</link>
            <guid isPermaLink="false">https://fezcode.com/blog/css-and-tailwind-css</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>010 - CSS and Tailwind CSS</h1>
<p>This project leverages a combination of traditional CSS and the utility-first framework Tailwind CSS for styling. This approach allows for both rapid development using pre-defined utility classes and fine-grained control with custom CSS when necessary.</p>
<h2><code>src/index.css</code> - Global Styles and Tailwind Directives</h2>
<p><code>src/index.css</code> serves as the main entry point for all CSS in the application. It&#39;s where Tailwind CSS is integrated and where global base styles and overrides are defined.</p>
<pre><code class="language-css">@tailwind base;
@tailwind components;
@tailwind utilities;

html, body {
  height: 100%;
}

body {
  margin: 0;
  background-color: #020617;
  font-family: &#39;Space Mono&#39;, &#39;JetBrains Mono&#39;, monospace, sans-serif !important;
  font-weight: 400 !important;
  font-style: normal !important;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

code {
  font-family:
    source-code-pro, Menlo, Monaco, Consolas, &#39;Courier New&#39;, monospace;
}

/* ... other custom styles and overrides ... */

:root {
  --color-dev-badge: #44403c; /* stone-700 */
  --color-takes-badge: #065f46; /* emerald-800 */
  --color-series-badge: #e11d48; /* rose-600 */
  --color-dnd-badge: #583fa3; /* violet-400 */
}
</code></pre>
<h3>Explanation:</h3>
<ul>
<li><strong><code>@tailwind base;</code></strong>: This directive injects Tailwind&#39;s base styles, which are a set of opinionated defaults that normalize browser styles and provide a solid foundation for building on.</li>
<li><strong><code>@tailwind components;</code></strong>: This injects Tailwind&#39;s component classes. These are typically larger, more complex classes that you might extract from repeated utility patterns (though this project might not use many custom components).</li>
<li><strong><code>@tailwind utilities;</code></strong>: This injects all of Tailwind&#39;s utility classes (e.g., <code>flex</code>, <code>pt-4</code>, <code>text-lg</code>, <code>bg-gray-950</code>). These are the core of Tailwind&#39;s utility-first approach.</li>
<li><strong>Global CSS Resets/Defaults</strong>: After the <code>@tailwind</code> directives, you see standard CSS rules that apply globally:<ul>
<li><code>html, body { height: 100%; }</code>: Ensures the <code>html</code> and <code>body</code> elements take up the full viewport height.</li>
<li><code>body { ... }</code>: Sets a default <code>margin</code>, <code>background-color</code>, <code>font-family</code>, <code>font-weight</code>, <code>font-style</code>, and font smoothing properties for the entire application.</li>
<li><code>code { ... }</code>: Defines a specific font stack for <code>&lt;code&gt;</code> elements.</li>
</ul>
</li>
<li><strong>Custom Styles and Overrides</strong>: The file also contains custom CSS rules, such as those for <code>.prose</code> (likely related to the <code>@tailwindcss/typography</code> plugin) and specific styling for images and inline code blocks within prose content. These demonstrate how to override or extend Tailwind&#39;s defaults with custom CSS when needed.</li>
<li><strong>CSS Variables</strong>: The <code>:root</code> block defines custom CSS variables (e.g., <code>--color-dev-badge</code>). These can be used throughout the CSS and even in JavaScript to maintain consistent theming.</li>
</ul>
<h2><code>tailwind.config.js</code> - Customizing Tailwind CSS</h2>
<p><code>tailwind.config.js</code> is the configuration file for Tailwind CSS. It allows you to customize Tailwind&#39;s default theme, add new utility classes, and integrate plugins.</p>
<pre><code class="language-javascript">const defaultTheme = require(&#39;tailwindcss/defaultTheme&#39;)
const colors = require(&#39;./src/config/colors&#39;);
const fonts = require(&#39;./src/config/fonts&#39;); // New import

/** @type {import(&#39;tailwindcss&#39;).Config} */
module.exports = {
  darkMode: &#39;class&#39;,
  content: [
    &quot;./src/**/*.{js,jsx,ts,tsx}&quot;,
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: [&#39;Space Mono&#39;, ...defaultTheme.fontFamily.sans],
        mono: [&#39;JetBrains Mono&#39;, ...defaultTheme.fontFamily.mono],
        arvo: fonts.arvo, // New custom font
        playfairDisplay: fonts.playfairDisplay, // New custom font
        inter: fonts.inter, // New custom font
      },
      colors: colors,
      typography: (theme) =&gt; ({
        dark: {
          css: {
            color: theme(&#39;colors.gray.300&#39;),
            a: {
              color: theme(&#39;colors.primary.400&#39;),
              &#39;&amp;:hover&#39;: {
                color: theme(&#39;colors.primary.600&#39;),
              },
            },
            // ... other typography customizations
          },
        },
      }),
    },
  },
  plugins: [
    require(&#39;@tailwindcss/typography&#39;),
  ],
}
</code></pre>
<h3>Explanation:</h3>
<ul>
<li><strong><code>darkMode: &#39;class&#39;</code></strong>: Configures Tailwind to use class-based dark mode. This means you can toggle dark mode by adding or removing the <code>dark</code> class (e.g., <code>&lt;html class=&quot;dark&quot;&gt;</code>) to an ancestor element.</li>
<li><strong><code>content</code></strong>: This array specifies the files that Tailwind should scan for utility classes. This is crucial for Tailwind&#39;s JIT (Just-In-Time) mode, which only generates the CSS you actually use, resulting in smaller bundle sizes.<ul>
<li><code>&quot;./src/**/*.{js,jsx,ts,tsx}&quot;</code>: Tells Tailwind to look for classes in all <code>.js</code>, <code>.jsx</code>, <code>.ts</code>, and <code>.tsx</code> files within the <code>src</code> directory.</li>
</ul>
</li>
<li><strong><code>theme</code></strong>: This is where you customize Tailwind&#39;s default design system.<ul>
<li><strong><code>extend</code></strong>: Allows you to add to Tailwind&#39;s default theme without overwriting it entirely.<ul>
<li><strong><code>fontFamily</code></strong>: Customizes font stacks. Here, <code>Space Mono</code> and <code>JetBrains Mono</code> are added, and custom fonts like <code>arvo</code>, <code>playfairDisplay</code>, and <code>inter</code> are integrated, likely defined in <code>src/config/fonts.js</code>.</li>
<li><strong><code>colors</code></strong>: Customizes the color palette. It imports colors from <code>src/config/colors.js</code>, allowing for a centralized color definition.</li>
<li><strong><code>typography</code></strong>: This section customizes the <code>@tailwindcss/typography</code> plugin. It defines specific styles for elements within <code>prose</code> content (like Markdown rendered text) for a <code>dark</code> theme, ensuring readability and consistent styling for headings, links, code blocks, etc.</li>
</ul>
</li>
</ul>
</li>
<li><strong><code>plugins</code></strong>: This array is where you register Tailwind plugins.<ul>
<li><strong><code>require(&#39;@tailwindcss/typography&#39;)</code></strong>: Integrates the official Typography plugin, which provides a set of <code>prose</code> classes to style raw HTML or Markdown content with beautiful, readable typography defaults.</li>
</ul>
</li>
</ul>
<h2>How it Works Together</h2>
<ol>
<li><strong>Development</strong>: When you run <code>npm start</code>, Tailwind&#39;s JIT engine scans your <code>content</code> files, generates only the necessary CSS utility classes based on your usage and <code>tailwind.config.js</code> customizations, and injects them into your application via <code>src/index.css</code>.</li>
<li><strong>Production Build</strong>: When you run <code>npm run build</code>, Tailwind purges any unused CSS, resulting in a highly optimized and small CSS bundle.</li>
<li><strong>Usage in Components</strong>: In your React components, you apply styles by adding Tailwind utility classes directly to your JSX elements (e.g., <code>&lt;div className=&quot;bg-gray-950 text-white p-4&quot;&gt;</code>).</li>
</ol>
<p>This combination provides a powerful and efficient way to style modern web applications, offering both flexibility and maintainability.</p>
<p><a href="https://fezcode.com/blog/css-and-tailwind-css">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Routing With React Router Dom]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/routing-with-react-router-dom</link>
            <guid isPermaLink="false">https://fezcode.com/blog/routing-with-react-router-dom</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>009 - Routing with <code>react-router-dom</code></h1>
<p><code>react-router-dom</code> is the standard library for client-side routing in React applications. It allows you to define different URLs for different views of your application, enabling navigation without full page reloads. This project uses <code>react-router-dom</code> to manage its various pages like blog posts, projects, and an about page.</p>
<h2>Core Concepts</h2>
<h3>1. <code>HashRouter</code></h3>
<p>As seen in <code>src/App.js</code>:</p>
<pre><code class="language-javascript">// src/App.js
import { HashRouter as Router } from &#39;react-router-dom&#39;;
// ...
function App() {
  return (
    &lt;Router&gt;
      {/* ... all other components are wrapped here */}
    &lt;/Router&gt;
  );
}
</code></pre>
<ul>
<li><strong>Purpose</strong>: <code>HashRouter</code> uses the hash portion of the URL (e.g., <code>http://localhost:3000/#/blog</code>) to keep your UI in sync with the URL. This is particularly useful for static site hosting (like GitHub Pages) because it doesn&#39;t require any special server-side configuration to handle routing. The server always serves <code>index.html</code>, and the React application handles the routing based on the hash.</li>
</ul>
<h3>2. <code>Routes</code> and <code>Route</code></h3>
<p>These components are used to define the mapping between URL paths and the React components that should be rendered for those paths. They are typically found in a central routing component, like <code>AnimatedRoutes.js</code> in this project.</p>
<h3>Example from <code>src/components/AnimatedRoutes.js</code></h3>
<pre><code class="language-javascript">// src/components/AnimatedRoutes.js
import React from &#39;react&#39;;
import { Routes, Route, useLocation } from &#39;react-router-dom&#39;;
import { AnimatePresence, motion } from &#39;framer-motion&#39;;
// ... page component imports

function AnimatedRoutes() {
  const location = useLocation();

  return (
    &lt;AnimatePresence mode=&quot;wait&quot;&gt;
      &lt;Routes location={location} key={location.pathname}&gt;
        &lt;Route
          path=&quot;/&quot;
          element={
            &lt;motion.div /* ... */ &gt;
              &lt;HomePage /&gt;
            &lt;/motion.div&gt;
          }
        /&gt;
        &lt;Route
          path=&quot;/blog/:slug&quot;
          element={
            &lt;motion.div /* ... */ &gt;
              &lt;BlogPostPage /&gt;
            &lt;/motion.div&gt;
          }
        /&gt;
        &lt;Route
          path=&quot;*&quot;
          element={
            &lt;motion.div /* ... */ &gt;
              &lt;NotFoundPage /&gt;
            &lt;/motion.div&gt;
          }
        /&gt;
        {/* ... other routes */}
      &lt;/Routes&gt;
    &lt;/AnimatePresence&gt;
  );
}

export default AnimatedRoutes;
</code></pre>
<ul>
<li><strong><code>Routes</code></strong>: This component is a container for all your <code>Route</code> components. It looks at the current URL and renders the first <code>Route</code> that matches.<ul>
<li><code>location={location}</code> and <code>key={location.pathname}</code>: These props are used in conjunction with <code>framer-motion</code>&#39;s <code>AnimatePresence</code> to enable exit animations when navigating between routes. By providing a <code>key</code> that changes with the path, <code>AnimatePresence</code> can detect when a component is being removed from the tree.</li>
</ul>
</li>
<li><strong><code>Route</code></strong>: Defines a single route.<ul>
<li><strong><code>path</code></strong>: Specifies the URL path pattern. Examples:<ul>
<li><code>&quot;/&quot;</code>: Matches the root URL.</li>
<li><code>&quot;/blog&quot;</code>: Matches <code>/blog</code>.</li>
<li><code>&quot;/blog/:slug&quot;</code>: Matches <code>/blog/any-value</code>. The <code>:slug</code> part is a URL parameter, meaning <code>any-value</code> will be captured and made available to the component.</li>
<li><code>&quot;/blog/series/:seriesSlug/:episodeSlug&quot;</code>: Matches more complex paths with multiple parameters.</li>
<li><code>&quot;*&quot;</code>: A wildcard route that matches any path not matched by previous routes. This is typically used for a 404 (Not Found) page.</li>
</ul>
</li>
<li><strong><code>element</code></strong>: The React element (component) to render when the <code>path</code> matches. In this project, each page component is wrapped in a <code>framer-motion</code> <code>motion.div</code> to apply page transition animations.</li>
</ul>
</li>
</ul>
<h3>3. <code>useLocation</code> Hook</h3>
<pre><code class="language-javascript">// src/components/AnimatedRoutes.js
import { Routes, Route, useLocation } from &#39;react-router-dom&#39;;
// ...
function AnimatedRoutes() {
  const location = useLocation();
  // ...
}
</code></pre>
<ul>
<li><strong>Purpose</strong>: <code>useLocation</code> is a hook that returns the current <code>location</code> object. This object contains information about the current URL, such as <code>pathname</code>, <code>search</code> (query parameters), and <code>hash</code>. In <code>AnimatedRoutes.js</code>, it&#39;s used to provide a <code>key</code> to <code>Routes</code> for animation purposes.</li>
</ul>
<h3>4. <code>useParams</code> Hook</h3>
<p>As seen in <code>src/pages/BlogPostPage.js</code>:</p>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
import { useParams, Link, useNavigate } from &#39;react-router-dom&#39;;
// ...
const BlogPostPage = () =&gt; {
  const { slug, seriesSlug, episodeSlug } = useParams();
  const currentSlug = episodeSlug || slug; // Use episodeSlug if present, otherwise use slug
  // ...
};
</code></pre>
<ul>
<li><strong>Purpose</strong>: <code>useParams</code> is a hook that returns an object of key/value pairs of URL parameters. For a route like <code>path=&quot;/blog/:slug&quot;</code>, if the URL is <code>/blog/my-first-post</code>, <code>useParams()</code> would return <code>{ slug: &#39;my-first-post&#39; }</code>.</li>
<li><strong>Example</strong>: In <code>BlogPostPage</code>, it extracts <code>slug</code>, <code>seriesSlug</code>, and <code>episodeSlug</code> from the URL, allowing the component to fetch the correct blog post content.</li>
</ul>
<h3>5. <code>useNavigate</code> Hook</h3>
<p>As seen in <code>src/pages/BlogPostPage.js</code>:</p>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
import { useParams, Link, useNavigate } from &#39;react-router-dom&#39;;
// ...
const BlogPostPage = () =&gt; {
  // ...
  const navigate = useNavigate();
  // ...
  if (postBody.trim().startsWith(&#39;&lt;!DOCTYPE html&gt;&#39;)) {
    console.error(&#39;Fetched content is HTML, not expected post content for:&#39;, currentSlug);
    navigate(&#39;/404&#39;); // Redirect to 404 page
    return; // Stop further processing
  }
  // ...
};
</code></pre>
<ul>
<li><strong>Purpose</strong>: <code>useNavigate</code> is a hook that returns a function that lets you navigate programmatically. This is useful for actions like redirecting after a form submission, or in this case, redirecting to a 404 page when content is not found.</li>
<li><strong>Example</strong>: In <code>BlogPostPage</code>, if the fetched content is determined to be an <code>index.html</code> fallback (indicating the actual post file was not found), <code>navigate(&#39;/404&#39;)</code> is called to redirect the user to the <code>NotFoundPage</code>.</li>
</ul>
<h3>6. <code>Link</code> Component</h3>
<p>As seen in <code>src/pages/BlogPostPage.js</code>:</p>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
// ...
&lt;Link
  to={backLink}
  className=&quot;text-primary-400 hover:underline flex items-center justify-center gap-2 text-lg mb-4&quot;
&gt;
  &lt;ArrowLeft size={24} /&gt; {backLinkText}
&lt;/Link&gt;
// ...
</code></pre>
<ul>
<li><strong>Purpose</strong>: The <code>Link</code> component is used to create navigation links within your application. It prevents a full page reload when clicked, allowing <code>react-router-dom</code> to handle the navigation client-side.</li>
<li><strong><code>to</code> prop</strong>: Specifies the destination path. It can be a string or an object.</li>
</ul>
<h2>Summary</h2>
<p><code>react-router-dom</code> provides a powerful and flexible way to manage navigation in React applications. By using <code>HashRouter</code>, <code>Routes</code>, <code>Route</code>, <code>useParams</code>, <code>useNavigate</code>, and <code>Link</code>, the Fezcode project creates a seamless single-page application experience with distinct URLs for different content, including dynamic routing for blog posts and projects, and robust handling for non-existent pages.</p>
<p><a href="https://fezcode.com/blog/routing-with-react-router-dom">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Context Usecontext]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-context-usecontext</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-context-usecontext</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>008 - React Context API and <code>useContext</code></h1>
<p>The React Context API provides a way to pass data through the component tree without having to pass props down manually at every level. This is particularly useful for global data (like user authentication, theme, or in this case, toast notifications) that many components might need access to.</p>
<h2>The Problem Context Solves (Prop Drilling)</h2>
<p>Imagine you have a deeply nested component tree, and a piece of data (e.g., a user object) is needed by a component several levels down. Without Context, you&#39;d have to pass that data as a prop through every intermediate component, even if those components don&#39;t directly use the data. This is known as &quot;prop drilling&quot; and can make your code verbose and harder to maintain.</p>
<h2>How Context API Works</h2>
<p>The Context API consists of three main parts:</p>
<ol>
<li><strong><code>createContext</code></strong>: Creates a Context object. When React renders a component that subscribes to this Context object, it will read the current context value from the closest matching <code>Provider</code> above it in the tree.</li>
<li><strong><code>Provider</code></strong>: A React component that allows consuming components to subscribe to context changes. It accepts a <code>value</code> prop to be passed to consuming components that are descendants of this Provider.</li>
<li><strong><code>useContext</code></strong>: A React Hook that lets you read context from a functional component.</li>
</ol>
<h2>Example: Toast Notification System</h2>
<p>This project uses the Context API to manage and display toast notifications globally. Let&#39;s examine <code>src/components/ToastContext.js</code> and <code>src/hooks/useToast.js</code>.</p>
<h3><code>src/components/ToastContext.js</code> (The Provider)</h3>
<pre><code class="language-javascript">import React, { createContext, useState, useCallback } from &#39;react&#39;;
import Toast from &#39;./Toast&#39;;

export const ToastContext = createContext();

let id = 0; // Simple counter for unique toast IDs

export const ToastContext = ({ children }) =&gt; {
  const [toasts, setToasts] = useState([]); // State to hold active toasts

  const addToast = useCallback((toast) =&gt; {
    const newToast = { ...toast, id: id++ };
    setToasts((prevToasts) =&gt; {
      if (prevToasts.length &gt;= 5) { // Limit to 5 toasts
        const updatedToasts = prevToasts.slice(0, prevToasts.length - 1);
        return [newToast, ...updatedToasts];
      }
      return [newToast, ...prevToasts];
    });
  }, []); // Memoize addToast function

  const removeToast = useCallback((id) =&gt; {
    setToasts((prevToasts) =&gt; prevToasts.filter((toast) =&gt; toast.id !== id));
  }, []); // Memoize removeToast function

  return (
    &lt;ToastContext.Provider value={{ addToast, removeToast }}&gt;
      {children}
      &lt;div className=&quot;fixed top-28 right-10 z-50&quot;&gt;
        {toasts.map((toast) =&gt; (
          &lt;Toast
            key={toast.id}
            id={toast.id}
            title={toast.title}
            message={toast.message}
            duration={toast.duration}
            removeToast={removeToast}
          /&gt;
        ))}
      &lt;/div&gt;
    &lt;/ToastContext.Provider&gt;
  );
};
</code></pre>
<p><strong>Explanation:</strong></p>
<ol>
<li><strong><code>export const ToastContext = createContext();</code></strong>: A Context object named <code>ToastContext</code> is created. This object will be used by both the Provider and the Consumer.</li>
<li><strong><code>ToastContext</code> Component</strong>: This is a functional component that will wrap parts of your application (as seen in <code>App.js</code>).<ul>
<li><strong><code>const [toasts, setToasts] = useState([]);</code></strong>: Manages the array of active toast notifications using <code>useState</code>.</li>
<li><strong><code>addToast</code> and <code>removeToast</code> functions</strong>: These functions are responsible for adding new toasts to the <code>toasts</code> array and removing them. They are wrapped in <code>useCallback</code> to prevent unnecessary re-creations, which is an optimization for performance.</li>
<li><strong><code>&lt;ToastContext.Provider value={{ addToast, removeToast }}&gt;</code></strong>: This is the core of the Provider. It makes the <code>addToast</code> and <code>removeToast</code> functions available to any component that consumes <code>ToastContext</code> and is rendered within this Provider&#39;s tree. The <code>value</code> prop is crucial here.</li>
<li><strong><code>{children}</code></strong>: This renders whatever components are passed as children to the <code>ToastContext</code>. These children (and their descendants) will have access to the context value.</li>
<li><strong>Toast Rendering</strong>: The <code>ToastContext</code> also directly renders the actual <code>Toast</code> components based on the <code>toasts</code> state, positioning them in the top-right corner of the screen.</li>
</ul>
</li>
</ol>
<h3><code>src/hooks/useToast.js</code> (The Consumer Hook)</h3>
<pre><code class="language-javascript">import { useContext } from &#39;react&#39;;
import { ToastContext } from &#39;../components/ToastContext&#39;;

export const useToast = () =&gt; {
  return useContext(ToastContext);
};
</code></pre>
<p><strong>Explanation:</strong></p>
<ol>
<li><strong><code>import { useContext } from &#39;react&#39;;</code></strong>: Imports the <code>useContext</code> Hook from React.</li>
<li><strong><code>import { ToastContext } from &#39;../components/ToastContext&#39;;</code></strong>: Imports the <code>ToastContext</code> object that was created in <code>ToastContext.js</code>.</li>
<li><strong><code>export const useToast = () =&gt; { ... };</code></strong>: This is a custom hook. Custom hooks are a powerful feature in React that allow you to extract reusable stateful logic from components. This <code>useToast</code> hook simplifies consuming the <code>ToastContext</code>.</li>
<li><strong><code>return useContext(ToastContext);</code></strong>: This line is where the magic happens. When <code>useContext(ToastContext)</code> is called, React looks up the component tree for the closest <code>ToastContext.Provider</code> and returns its <code>value</code> prop. In this case, it returns <code>{ addToast, removeToast }</code>.</li>
</ol>
<h3>How it&#39;s Used in a Component (e.g., <code>BlogPostPage.js</code>)</h3>
<pre><code class="language-javascript">// Inside BlogPostPage.js (or any other component that needs toasts)
import { useToast } from &#39;../hooks/useToast&#39;;

const CodeBlock = ({ /* ... */ }) =&gt; {
  const { addToast } = useToast(); // Access addToast function

  const handleCopy = () =&gt; {
    // ... copy logic ...
    addToast({
      title: &#39;Success&#39;,
      message: &#39;Copied to clipboard!&#39;,
      duration: 3000,
    });
    // ...
  };
  // ...
};
</code></pre>
<p>Any component that needs to display a toast simply imports and calls <code>useToast()</code>, and it immediately gets access to the <code>addToast</code> function without needing to receive it as a prop from its parent.</p>
<h2>Summary</h2>
<p>The React Context API, combined with the <code>useContext</code> hook, provides an elegant solution for managing global state and sharing functions across your component tree, avoiding prop drilling and making your application&#39;s architecture cleaner and more maintainable. The toast notification system in this project is a prime example of its effective use.</p>
<p><a href="https://fezcode.com/blog/react-context-usecontext">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Hooks Usestate Useeffect]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-hooks-usestate-useeffect</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-hooks-usestate-useeffect</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>007 - React Hooks: <code>useState</code> and <code>useEffect</code></h1>
<p>React Hooks are functions that let you &quot;hook into&quot; React state and lifecycle features from functional components. They allow you to use state and other React features without writing a class. The two most fundamental hooks are <code>useState</code> and <code>useEffect</code>.</p>
<h2><code>useState</code> Hook</h2>
<p><code>useState</code> is a Hook that lets you add React state to functional components. It returns a pair of values: the current state, and a function that updates it.</p>
<h3>Syntax</h3>
<pre><code class="language-javascript">const [stateVariable, setStateVariable] = useState(initialValue);
</code></pre>
<ul>
<li><code>stateVariable</code>: The current value of the state.</li>
<li><code>setStateVariable</code>: A function to update the <code>stateVariable</code>. When this function is called, React will re-render the component.</li>
<li><code>initialValue</code>: The initial value for the state. This can be any JavaScript data type (number, string, boolean, object, array, etc.).</li>
</ul>
<h3>Example from <code>src/pages/BlogPostPage.js</code></h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
const BlogPostPage = () =&gt; {
  // ...
  const [post, setPost] = useState(null);
  const [loading, setLoading] = useState(true);
  const [readingProgress, setReadingProgress] = useState(0);
  const [isAtTop, setIsAtTop] = useState(true); // New state for tracking if at top
  const [isModalOpen, setIsModalToOpen] = useState(false);
  const [modalContent, setModalContent] = useState(&#39;&#39;);
  // ...
};
</code></pre>
<p>In <code>BlogPostPage</code>:</p>
<ul>
<li><code>[post, setPost] = useState(null)</code>: <code>post</code> will hold the blog post data (attributes, body, series posts). It&#39;s initialized to <code>null</code> because the data is fetched asynchronously.</li>
<li><code>[loading, setLoading] = useState(true)</code>: <code>loading</code> is a boolean that indicates whether the post data is currently being fetched. It starts as <code>true</code>.</li>
<li><code>[readingProgress, setReadingProgress] = useState(0)</code>: <code>readingProgress</code> stores the user&#39;s scroll progress on the page, initialized to <code>0</code>.</li>
<li><code>[isAtTop, setIsAtTop] = useState(true)</code>: Tracks if the user is at the top of the page.</li>
<li><code>[isModalOpen, setIsModalToOpen] = useState(false)</code>: Controls the visibility of a modal, initialized to <code>false</code> (closed).</li>
<li><code>[modalContent, setModalContent] = useState(&#39;&#39;)</code>: Stores the content to be displayed inside the modal.</li>
</ul>
<h3>Example from <code>src/components/Layout.js</code></h3>
<pre><code class="language-javascript">// src/components/Layout.js
const Layout = ({ children }) =&gt; {
  const [isSidebarOpen, setIsSidebarOpen] = useState(window.innerWidth &gt; 768);
  // ...
};
</code></pre>
<p>In <code>Layout</code>:</p>
<ul>
<li><code>[isSidebarOpen, setIsSidebarOpen] = useState(window.innerWidth &gt; 768)</code>: <code>isSidebarOpen</code> controls the visibility of the sidebar. Its initial value depends on the window width, making the sidebar open by default on larger screens.</li>
</ul>
<h2><code>useEffect</code> Hook</h2>
<p><code>useEffect</code> is a Hook that lets you perform side effects in functional components. Side effects include data fetching, subscriptions, manually changing the DOM, and other operations that interact with the outside world. It runs after every render of the component by default, but you can control when it runs using its dependency array.</p>
<h3>Syntax</h3>
<pre><code class="language-javascript">useEffect(() =&gt; {
  // Side effect code here
  return () =&gt; {
    // Cleanup function (optional)
  };
}, [dependency1, dependency2]); // Dependency array (optional)
</code></pre>
<ul>
<li><strong>First argument (function)</strong>: This is where you put your side effect code. It can optionally return a cleanup function.</li>
<li><strong>Second argument (dependency array)</strong>: This array controls when the effect re-runs.<ul>
<li>If omitted, the effect runs after every render.</li>
<li>If an empty array <code>[]</code>, the effect runs only once after the initial render (like <code>componentDidMount</code>). The cleanup runs on unmount (like <code>componentWillUnmount</code>).</li>
<li>If it contains variables (e.g., <code>[prop1, state1]</code>), the effect runs after the initial render and whenever any of the variables in the array change.</li>
</ul>
</li>
</ul>
<h3>Example from <code>src/pages/BlogPostPage.js</code> (Data Fetching)</h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
useEffect(() =&gt; {
  const fetchPost = async () =&gt; {
    setLoading(true);
    // ... data fetching logic using fetch API ...
    setLoading(false);
  };

  fetchPost();
}, [currentSlug]); // Effect re-runs when currentSlug changes
</code></pre>
<p>This <code>useEffect</code> hook is responsible for fetching the blog post data. </p>
<ul>
<li>It defines an <code>async</code> function <code>fetchPost</code> to handle the asynchronous data retrieval.</li>
<li><code>setLoading(true)</code> is called at the start to show a loading indicator.</li>
<li>The <code>fetch</code> API is used to get the <code>.txt</code> content and <code>shownPosts.json</code> metadata.</li>
<li>Crucially, the dependency array <code>[currentSlug]</code> ensures that this effect runs only when the <code>currentSlug</code> (derived from the URL parameters) changes. This prevents unnecessary re-fetches and ensures the correct post is loaded when navigating between posts.</li>
</ul>
<h3>Example from <code>src/pages/BlogPostPage.js</code> (Scroll Event Listener)</h3>
<pre><code class="language-javascript">// src/pages/BlogPostPage.js
useEffect(() =&gt; {
  const handleScroll = () =&gt; {
    if (contentRef.current) {
      const { scrollTop, scrollHeight, clientHeight } =
        document.documentElement;
      const totalHeight = scrollHeight - clientHeight;
      const currentProgress = (scrollTop / totalHeight) * 100;
      setReadingProgress(currentProgress);
      setIsAtTop(scrollTop === 0); // Update isAtTop based on scroll position
    }
  };

  window.addEventListener(&#39;scroll&#39;, handleScroll);
  return () =&gt; window.removeEventListener(&#39;scroll&#39;, handleScroll);
}, [post]); // Re-attach scroll listener if post changes
</code></pre>
<p>This <code>useEffect</code> manages a scroll event listener to calculate reading progress and determine if the user is at the top of the page.</p>
<ul>
<li>It adds an event listener to the <code>window</code> when the component mounts or when the <code>post</code> state changes.</li>
<li>The <code>return () =&gt; { ... }</code> part is a <strong>cleanup function</strong>. This function runs when the component unmounts or before the effect re-runs due to a dependency change. It&#39;s essential here to remove the event listener to prevent memory leaks and unexpected behavior.</li>
<li>The dependency array <code>[post]</code> means the effect (and its cleanup) will re-run if the <code>post</code> object changes, ensuring the scroll listener is correctly attached to the relevant content.</li>
</ul>
<h3>Example from <code>src/components/Layout.js</code> (Window Resize Listener)</h3>
<pre><code class="language-javascript">// src/components/Layout.js
useEffect(() =&gt; {
  const handleResize = () =&gt; {
    if (window.innerWidth &lt;= 768) {
      setIsSidebarOpen(false);
    }
  };

  window.addEventListener(&#39;resize&#39;, handleResize);

  return () =&gt; {
    window.removeEventListener(&#39;resize&#39;, handleResize);
  };
}, []); // Empty dependency array: runs once on mount, cleans up on unmount
</code></pre>
<p>This <code>useEffect</code> in <code>Layout.js</code> handles the sidebar&#39;s initial state and responsiveness.</p>
<ul>
<li>It adds a <code>resize</code> event listener to the window.</li>
<li>The <code>handleResize</code> function closes the sidebar if the window width drops below 768 pixels.</li>
<li>The empty dependency array <code>[]</code> ensures that this effect runs only once after the initial render and its cleanup function runs only when the component unmounts. This is perfect for setting up global event listeners that don&#39;t need to be re-initialized unless the component is completely removed from the DOM.</li>
</ul>
<h2>Summary</h2>
<p><code>useState</code> and <code>useEffect</code> are powerful tools that bring state management and side effect handling to functional components, making them as capable as class components while often being more concise and easier to reason about. Understanding their usage, especially the role of the dependency array in <code>useEffect</code>, is fundamental to building robust React applications.</p>
<p><a href="https://fezcode.com/blog/react-hooks-usestate-useeffect">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[React Basics Components Props]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/react-basics-components-props</link>
            <guid isPermaLink="false">https://fezcode.com/blog/react-basics-components-props</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>006 - React Basics: Components and Props</h1>
<p>At the core of React applications are <strong>components</strong>. Components are independent, reusable pieces of UI. They can be thought of as JavaScript functions that return JSX (JavaScript XML), which describes what the UI should look like. React applications are built by composing these components.</p>
<h2>Functional Components</h2>
<p>The project primarily uses <strong>functional components</strong>, which are JavaScript functions that accept a single <code>props</code> (properties) object argument and return React elements.</p>
<h3>Example: <code>App</code> Component (<code>src/App.js</code>)</h3>
<pre><code class="language-javascript">// src/App.js
import React from &#39;react&#39;;
// ... imports

function App() {
  return (
    &lt;Router&gt;
      {/* ... other components */}
      &lt;Layout&gt;
        &lt;AnimatedRoutes /&gt;
      &lt;/Layout&gt;
      {/* ... */}
    &lt;/Router&gt;
  );
}

export default App;
</code></pre>
<ul>
<li><strong><code>function App() { ... }</code></strong>: This defines a functional component named <code>App</code>. </li>
<li>The <code>return</code> statement contains JSX, which is a syntax extension for JavaScript recommended by React to describe UI.</li>
<li><code>&lt;Layout&gt;</code> and <code>&lt;AnimatedRoutes&gt;</code> are other components being used within <code>App</code>.</li>
</ul>
<h3>Example: <code>Layout</code> Component (<code>src/components/Layout.js</code>)</h3>
<p>Let&#39;s look at <code>src/components/Layout.js</code> to see a slightly more complex functional component.</p>
<pre><code class="language-javascript">// src/components/Layout.js
import React, { useState, useEffect } from &#39;react&#39;;
import Navbar from &#39;./Navbar&#39;;
import Sidebar from &#39;./Sidebar&#39;;
import Footer from &#39;./Footer&#39;;
// ... other imports

const Layout = ({ children }) =&gt; {
  const [isSidebarOpen, setIsSidebarOpen] = useState(window.innerWidth &gt; 768);
  // ... other state and effects

  return (
    &lt;div className=&quot;bg-gray-950 min-h-screen font-sans flex&quot;&gt;
      &lt;Sidebar isOpen={isSidebarOpen} toggleSidebar={toggleSidebar} /&gt;
      &lt;div
        className={`flex-1 flex flex-col transition-all duration-300 ${isSidebarOpen ? &#39;md:ml-64&#39; : &#39;md:ml-0&#39;}`}&gt;
        &lt;Navbar toggleSidebar={toggleSidebar} isSidebarOpen={isSidebarOpen} /&gt;
        &lt;main className=&quot;flex-grow&quot;&gt;{children}&lt;/main&gt;
        &lt;Footer /&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  );
};

export default Layout;
</code></pre>
<ul>
<li><strong><code>const Layout = ({ children }) =&gt; { ... };</code></strong>: This defines another functional component, <code>Layout</code>, using an arrow function syntax. It directly destructures <code>children</code> from the <code>props</code> object. This is a common pattern.</li>
</ul>
<h2>Props (Properties)</h2>
<p><strong>Props</strong> are how you pass data from a parent component to a child component. They are read-only and allow components to be dynamic and reusable.</p>
<h3>Passing Props</h3>
<p>In the <code>App</code> component, you can see <code>Layout</code> being used:</p>
<pre><code class="language-jsx">// Inside App component&#39;s return
&lt;Layout&gt;
  &lt;AnimatedRoutes /&gt;
&lt;/Layout&gt;
</code></pre>
<p>Here, <code>AnimatedRoutes</code> is passed as a special prop called <code>children</code> to the <code>Layout</code> component. Whatever content you place between the opening and closing tags of a component becomes its <code>children</code> prop.</p>
<h3>Receiving and Using Props</h3>
<p>In the <code>Layout</code> component, <code>children</code> is received as a prop:</p>
<pre><code class="language-javascript">const Layout = ({ children }) =&gt; {
  // ...
  return (
    // ...
    &lt;main className=&quot;flex-grow&quot;&gt;{children}&lt;/main&gt;
    // ...
  );
};
</code></pre>
<p>The <code>Layout</code> component then renders <code>{children}</code> inside its <code>&lt;main&gt;</code> tag, meaning the <code>AnimatedRoutes</code> (or whatever was passed as children) will be rendered in that spot.</p>
<p>Another example of props in <code>Layout.js</code>:</p>
<pre><code class="language-jsx">&lt;Sidebar isOpen={isSidebarOpen} toggleSidebar={toggleSidebar} /&gt;
&lt;Navbar toggleSidebar={toggleSidebar} isSidebarOpen={isSidebarOpen} /&gt;
</code></pre>
<p>Here:</p>
<ul>
<li>The <code>Sidebar</code> component receives two props: <code>isOpen</code> (a boolean state variable) and <code>toggleSidebar</code> (a function).</li>
<li>The <code>Navbar</code> component also receives <code>toggleSidebar</code> and <code>isSidebarOpen</code>.</li>
</ul>
<p>These props are defined in the <code>Layout</code> component&#39;s scope and passed down to its child components (<code>Sidebar</code>, <code>Navbar</code>) to control their behavior or appearance. For instance, <code>isOpen</code> might control the visibility of the sidebar, and <code>toggleSidebar</code> would be a function to change that visibility when a button in the <code>Navbar</code> is clicked.</p>
<h2>Summary</h2>
<p>Functional components are the building blocks of React UIs, and props are the essential mechanism for communicating data and functionality between these components in a unidirectional flow (from parent to child). This modular approach makes React applications easier to manage, test, and scale.</p>
<p><a href="https://fezcode.com/blog/react-basics-components-props">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Blog Post Page Component]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/blog-post-page-component</link>
            <guid isPermaLink="false">https://fezcode.com/blog/blog-post-page-component</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>005 - <code>src/pages/BlogPostPage.js</code> Component Explained</h1>
<p><code>src/pages/BlogPostPage.js</code> is a critical component responsible for displaying individual blog posts. It handles fetching the post content and metadata, rendering Markdown, syntax highlighting code blocks, and managing UI interactivity like copying code or opening code in a modal. It also includes navigation for series posts and robust error handling for missing content.</p>
<pre><code class="language-javascript">import React, { useState, useEffect, useRef } from &#39;react&#39;;
import { useParams, Link, useNavigate } from &#39;react-router-dom&#39;;
import ReactMarkdown from &#39;react-markdown&#39;;
import { Prism as SyntaxHighlighter } from &#39;react-syntax-highlighter&#39;;
import {
  ArrowSquareOut,
  ArrowsOutSimple,
  Clipboard,
  ArrowLeft,
} from &#39;@phosphor-icons/react&#39;;
import { customTheme } from &#39;../utils/customTheme&#39;;
import PostMetadata from &#39;../components/PostMetadata&#39;;
import CodeModal from &#39;../components/CodeModal&#39;;
import { useToast } from &#39;../hooks/useToast&#39;;

// ... LinkRenderer and CodeBlock components (explained below)

const BlogPostPage = () =&gt; {
  const { slug, seriesSlug, episodeSlug } = useParams();
  const navigate = useNavigate();
  const currentSlug = episodeSlug || slug; // Use episodeSlug if present, otherwise use slug
  const [post, setPost] = useState(null);
  const [loading, setLoading] = useState(true);
  const [readingProgress, setReadingProgress] = useState(0);
  const [isAtTop, setIsAtTop] = useState(true); // New state for tracking if at top
  const contentRef = useRef(null);
  const [isModalOpen, setIsModalToOpen] = useState(false);
  const [modalContent, setModalContent] = useState(&#39;&#39;);

  const openModal = (content) =&gt; {
    setModalContent(content);
    setIsModalToOpen(true);
  };

  const closeModal = () =&gt; {
    setIsModalToOpen(false);
    setModalContent(&#39;&#39;);
  };

  useEffect(() =&gt; {
    const fetchPost = async () =&gt; {
      setLoading(true);
      console.log(&#39;Fetching post for currentSlug:&#39;, currentSlug);
      try {
        const [postContentResponse, shownPostsResponse] = await Promise.all([
          fetch(`/posts/${currentSlug}.txt`),
          fetch(&#39;/posts/shownPosts.json&#39;),
        ]);

        console.log(&#39;postContentResponse:&#39;, postContentResponse);
        console.log(&#39;shownPostsResponse:&#39;, shownPostsResponse);

        let postBody = &#39;&#39;;
        if (postContentResponse.ok) {
          postBody = await postContentResponse.text();
          // Check if the fetched content is actually HTML (indicating a fallback to index.html)
          if (postBody.trim().startsWith(&#39;&lt;!DOCTYPE html&gt;&#39;)) {
            console.error(&#39;Fetched content is HTML, not expected post content for:&#39;, currentSlug);
            navigate(&#39;/404&#39;); // Redirect to 404 page
            return; // Stop further processing
          }
        } else {
          console.error(&#39;Failed to fetch post content for:&#39;, currentSlug);
          navigate(&#39;/404&#39;); // Redirect to 404 page
          return; // Stop further processing
        }

        let postMetadata = null;
        let seriesPosts = [];
        if (shownPostsResponse.ok) {
          const allPosts = await shownPostsResponse.json();
          postMetadata = allPosts.find((item) =&gt; item.slug === currentSlug);

          if (postMetadata &amp;&amp; postMetadata.series) {
            seriesPosts = allPosts
              .filter((item) =&gt; item.series === postMetadata.series)
              .sort((a, b) =&gt; a.seriesIndex - b.seriesIndex);
          }
        } else {
          console.error(&#39;Failed to fetch shownPosts.json&#39;);
        }

        console.log(&#39;postMetadata:&#39;, postMetadata);
        console.log(&#39;postBody length:&#39;, postBody.length);

        if (postMetadata &amp;&amp; postContentResponse.ok) {
          setPost({ attributes: postMetadata, body: postBody, seriesPosts });
          console.log(&#39;Post set:&#39;, { attributes: postMetadata, body: postBody, seriesPosts });
        } else {
          setPost({ attributes: { title: &#39;Post not found&#39; }, body: &#39;&#39; });
          console.log(&#39;Post not found or content not fetched.&#39;);
        }
      } catch (error) {
        console.error(&#39;Error fetching post or shownPosts.json:&#39;, error);
        setPost({ attributes: { title: &#39;Error loading post&#39; }, body: &#39;&#39; });
      } finally {
        setLoading(false);
      }
    };

    fetchPost();
  }, [currentSlug]);

  useEffect(() =&gt; {
    const handleScroll = () =&gt; {
      if (contentRef.current) {
        const { scrollTop, scrollHeight, clientHeight } =
          document.documentElement;
        const totalHeight = scrollHeight - clientHeight;
        const currentProgress = (scrollTop / totalHeight) * 100;
        setReadingProgress(currentProgress);
        setIsAtTop(scrollTop === 0); // Update isAtTop based on scroll position
      }
    };

    window.addEventListener(&#39;scroll&#39;, handleScroll);
    return () =&gt; window.removeEventListener(&#39;scroll&#39;, handleScroll);
  }, [post]); // Re-attach scroll listener if post changes

  if (loading) {
    // Skeleton loading screen for BlogPostPage
    return (
      &lt;div className=&quot;bg-gray-900 py-16 sm:py-24 animate-pulse&quot;&gt;
        &lt;div className=&quot;mx-auto max-w-7xl px-6 lg:px-8&quot;&gt;
          &lt;div className=&quot;lg:grid lg:grid-cols-4 lg:gap-8&quot;&gt;
            &lt;div className=&quot;lg:col-span-3&quot;&gt;
              &lt;div className=&quot;h-8 bg-gray-800 rounded w-1/4 mb-4&quot;&gt;&lt;/div&gt;
              &lt;div className=&quot;h-12 bg-gray-800 rounded w-3/4 mb-8&quot;&gt;&lt;/div&gt;
              &lt;div className=&quot;space-y-4&quot;&gt;
                &lt;div className=&quot;h-6 bg-gray-800 rounded w-full&quot;&gt;&lt;/div&gt;
                &lt;div className=&quot;h-6 bg-gray-800 rounded w-5/6&quot;&gt;&lt;/div&gt;
                &lt;div className=&quot;h-6 bg-gray-800 rounded w-full&quot;&gt;&lt;/div&gt;
                &lt;div className=&quot;h-6 bg-gray-800 rounded w-2/3&quot;&gt;&lt;/div&gt;
              &lt;/div&gt;
            &lt;/div&gt;
            &lt;div className=&quot;hidden lg:block&quot;&gt;
              &lt;div className=&quot;bg-gray-800 rounded-lg shadow-lg p-6&quot;&gt;
                &lt;div className=&quot;h-8 bg-gray-700 rounded w-1/2 mb-4&quot;&gt;&lt;/div&gt;
                &lt;div className=&quot;space-y-2&quot;&gt;
                  &lt;div className=&quot;h-4 bg-gray-700 rounded w-full&quot;&gt;&lt;/div&gt;
                  &lt;div className=&quot;h-4 bg-gray-700 rounded w-3/4&quot;&gt;&lt;/div&gt;
                  &lt;div className=&quot;h-4 bg-gray-700 rounded w-1/2&quot;&gt;&lt;/div&gt;
                &lt;/div&gt;
              &lt;/div&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    );
  }

  // if (!post) { // This check is now mostly handled by the navigate(&#39;/404&#39;) above.
  //   return &lt;div className=&quot;text-center py-16&quot;&gt;Post not found&lt;/div&gt;;
  // }

  // Conditional rendering for post not found after loading or if attributes are missing
  if (!post || !post.attributes || post.body === &#39;&#39;) {
    // If post is null, or attributes are missing (e.g., from shownPosts.json), or body is empty,
    // it implies the post couldn&#39;t be fully loaded or found. Ideally, navigate would handle this.
    // This serves as a fallback display.
    return (
      &lt;div className=&quot;text-center py-16 text-gray-400&quot;&gt;
        &lt;h2 className=&quot;text-3xl font-bold mb-4&quot;&gt;Post Not Found&lt;/h2&gt;
        &lt;p className=&quot;text-lg&quot;&gt;The blog post you are looking for does not exist or could not be loaded.&lt;/p&gt;
        &lt;Link to=&quot;/blog&quot; className=&quot;text-primary-400 hover:underline mt-4 inline-block&quot;&gt;Go back to Blog&lt;/Link&gt;
      &lt;/div&gt;
    );
  }

  const currentPostIndex = post.seriesPosts ? post.seriesPosts.findIndex(
    (item) =&gt; item.slug === currentSlug,
  ) : -1;
  const prevPost = currentPostIndex &gt; 0 ? post.seriesPosts[currentPostIndex - 1] : null;
  const nextPost = post.seriesPosts &amp;&amp; currentPostIndex &lt; post.seriesPosts.length - 1
    ? post.seriesPosts[currentPostIndex + 1]
    : null;

  const backLink = seriesSlug ? `/blog/series/${seriesSlug}` : &#39;/blog&#39;;
  const backLinkText = seriesSlug ? &#39;Back to Series&#39; : &#39;Back to Blog&#39;;

  return (
    &lt;div className=&quot;bg-gray-900 py-16 sm:py-24&quot;&gt;
      &lt;div className=&quot;mx-auto max-w-7xl px-6 lg:px-8&quot;&gt;
        &lt;div className=&quot;lg:grid lg:grid-cols-4 lg:gap-8&quot;&gt;
          &lt;div className=&quot;lg:col-span-3&quot;&gt;
            &lt;Link
              to={backLink}
              className=&quot;text-primary-400 hover:underline flex items-center justify-center gap-2 text-lg mb-4&quot;
            &gt;
              &lt;ArrowLeft size={24} /&gt; {backLinkText}
            &lt;/Link&gt;
            &lt;div
              ref={contentRef}
              className=&quot;prose prose-xl prose-dark max-w-none&quot;
            &gt;
              &lt;ReactMarkdown
                components={{
                  a: LinkRenderer,
                  code: (props) =&gt; (
                    &lt;CodeBlock {...props} openModal={openModal} /&gt;
                  ),
                }}
              &gt;
                {post.body}
              &lt;/ReactMarkdown&gt;
            &lt;/div&gt;
            {(prevPost || nextPost) &amp;&amp; (
              &lt;div className=&quot;mt-8 flex justify-between items-center border-t border-gray-700 pt-8&quot;&gt;
                {prevPost &amp;&amp; (
                  &lt;Link
                    to={seriesSlug ? `/blog/series/${seriesSlug}/${prevPost.slug}` : `/blog/${prevPost.slug}`}
                    className=&quot;text-primary-400 hover:underline flex items-center gap-2&quot;
                  &gt;
                    &lt;ArrowLeft size={20} /&gt; Previous: {prevPost.title}
                  &lt;/Link&gt;
                )}
                {nextPost &amp;&amp; (
                  &lt;Link
                    to={seriesSlug ? `/blog/series/${seriesSlug}/${nextPost.slug}` : `/blog/${nextPost.slug}`}
                    className=&quot;text-primary-400 hover:underline flex items-center gap-2 ml-auto&quot;
                  &gt;
                    Next: {nextPost.title} &lt;ArrowLeft size={20} className=&quot;rotate-180&quot; /&gt;
                  &lt;/Link&gt;
                )}
              &lt;/div&gt;
            )}
          &lt;/div&gt;
          &lt;div className=&quot;hidden lg:block&quot;&gt;
            &lt;PostMetadata
              metadata={post.attributes}
              readingProgress={readingProgress}
              isAtTop={isAtTop}
              overrideDate={post.attributes.date}
              updatedDate={post.attributes.updated}
              seriesPosts={post.seriesPosts}
            /&gt;
          &lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;
      &lt;CodeModal isOpen={isModalOpen} onClose={closeModal}&gt;
        {modalContent}
      &lt;/CodeModal&gt;
    &lt;/div&gt;
  );
};

export default BlogPostPage;
</code></pre>
<p><a href="https://fezcode.com/blog/blog-post-page-component">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[App Js Main Component]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/app-js-main-component</link>
            <guid isPermaLink="false">https://fezcode.com/blog/app-js-main-component</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>004 - <code>src/App.js</code> Main Component Explained</h1>
<p><code>src/App.js</code> is the main component of your React application. It acts as the root of your component tree (after <code>index.js</code> renders it) and is responsible for setting up global configurations like routing, layout, and context providers that are available throughout your application.</p>
<pre><code class="language-javascript">import React from &#39;react&#39;;
import { HashRouter as Router } from &#39;react-router-dom&#39;;
import Layout from &#39;./components/Layout&#39;;
import AnimatedRoutes from &#39;./components/AnimatedRoutes&#39;;
import { ToastContext } from &#39;./components/ToastContext&#39;;
import ScrollToTop from &#39;./components/ScrollToTop&#39;;

function App() {
  return (
    &lt;Router&gt;
      &lt;ScrollToTop /&gt;
      &lt;ToastContext&gt;
        &lt;Layout&gt;
          &lt;AnimatedRoutes /&gt;
        &lt;/Layout&gt;
      &lt;/ToastContext&gt;
    &lt;/Router&gt;
  );
}

export default App;
</code></pre>
<h2>Line-by-Line Explanation</h2>
<h3>Imports</h3>
<pre><code class="language-javascript">import React from &#39;react&#39;;
</code></pre>
<ul>
<li><strong><code>import React from &#39;react&#39;;</code></strong>: Imports the React library, necessary for defining React components and using JSX.</li>
</ul>
<pre><code class="language-javascript">import { HashRouter as Router } from &#39;react-router-dom&#39;;
</code></pre>
<ul>
<li><strong><code>import { HashRouter as Router } from &#39;react-router-dom&#39;;</code></strong>: Imports <code>HashRouter</code> from the <code>react-router-dom</code> library and renames it to <code>Router</code> for convenience. <code>HashRouter</code> uses the hash portion of the URL (e.g., <code>/#/blog</code>) to keep your UI in sync with the URL. This is often preferred for static site deployments like GitHub Pages because it doesn&#39;t require server-side configuration for routing.</li>
</ul>
<pre><code class="language-javascript">import Layout from &#39;./components/Layout&#39;;
</code></pre>
<ul>
<li><strong><code>import Layout from &#39;./components/Layout&#39;;</code></strong>: Imports the <code>Layout</code> component. This component likely defines the overall structure of your application, such as headers, footers, and sidebars, and wraps the main content area.</li>
</ul>
<pre><code class="language-javascript">import AnimatedRoutes from &#39;./components/AnimatedRoutes&#39;;
</code></pre>
<ul>
<li><strong><code>import AnimatedRoutes from &#39;./components/AnimatedRoutes&#39;;</code></strong>: Imports the <code>AnimatedRoutes</code> component. This component is responsible for defining the application&#39;s routes and likely incorporates animation for page transitions, possibly using a library like <code>framer-motion</code>.</li>
</ul>
<pre><code class="language-javascript">import { ToastContext } from &#39;./components/ToastContext&#39;;
</code></pre>
<ul>
<li><strong><code>import { ToastContext } from &#39;./components/ToastContext&#39;;</code></strong>: Imports the <code>ToastContext</code> component. This component is part of React&#39;s Context API pattern. It makes a <code>toast</code> (a small, temporary notification) functionality available to all its child components without having to pass props down manually at every level.</li>
</ul>
<pre><code class="language-javascript">import ScrollToTop from &#39;./components/ScrollToTop&#39;;
</code></pre>
<ul>
<li><strong><code>import ScrollToTop from &#39;./components/ScrollToTop&#39;;</code></strong>: Imports the <code>ScrollToTop</code> component. This component is typically used in conjunction with routing to automatically scroll the window to the top of the page whenever the route changes, providing a better user experience.</li>
</ul>
<h3>The <code>App</code> Component</h3>
<pre><code class="language-javascript">function App() {
  return (
    &lt;Router&gt;
      &lt;ScrollToTop /&gt;
      &lt;ToastContext&gt;
        &lt;Layout&gt;
          &lt;AnimatedRoutes /&gt;
        &lt;/Layout&gt;
      &lt;/ToastContext&gt;
    &lt;/Router&gt;
  );
}
</code></pre>
<ul>
<li><p><strong><code>function App() { ... }</code></strong>: This defines a functional React component named <code>App</code>. Functional components are the modern way to write React components and are essentially JavaScript functions that return JSX.</p>
</li>
<li><p><strong><code>return (...)</code></strong>: The <code>return</code> statement contains the JSX (JavaScript XML) that defines the UI structure for the <code>App</code> component.</p>
<ul>
<li><p><strong><code>&lt;Router&gt;</code></strong>: This is the <code>HashRouter</code> component from <code>react-router-dom</code>. It wraps the entire application, enabling client-side routing. Any component within this <code>Router</code> can use routing features like <code>Link</code> and <code>useParams</code>.</p>
</li>
<li><p><strong><code>&lt;ScrollToTop /&gt;</code></strong>: This component is rendered directly inside the <code>Router</code>. Its effect (scrolling to top on route change) will apply globally to the application.</p>
</li>
<li><p><strong><code>&lt;ToastContext&gt;</code></strong>: This component wraps the <code>Layout</code> and <code>AnimatedRoutes</code>. This means that any component rendered within the <code>Layout</code> or <code>AnimatedRoutes</code> will have access to the toast functionality provided by the <code>ToastContext</code> via the <code>useContext</code> hook.</p>
</li>
<li><p><strong><code>&lt;Layout&gt;</code></strong>: This component defines the common structure (e.g., header, footer, navigation) that will be present on most pages. It wraps the <code>AnimatedRoutes</code> component, meaning the routed content will be displayed within this layout.</p>
</li>
<li><p><strong><code>&lt;AnimatedRoutes /&gt;</code></strong>: This component is where the actual route definitions (e.g., <code>/blog</code>, <code>/about</code>, <code>/projects</code>) are handled. When the URL changes, <code>AnimatedRoutes</code> will render the appropriate page component (e.g., <code>BlogPostPage</code>, <code>HomePage</code>) within the <code>Layout</code>.</p>
</li>
</ul>
</li>
</ul>
<h3>Export</h3>
<pre><code class="language-javascript">export default App;
</code></pre>
<ul>
<li><strong><code>export default App;</code></strong>: This makes the <code>App</code> component the default export of this module, allowing it to be imported by other files (like <code>src/index.js</code>).</li>
</ul>
<h2>Summary</h2>
<p><code>src/App.js</code> orchestrates the main structure and global functionalities of the application. It sets up routing, provides global context for notifications, and defines the overarching layout, ensuring a consistent user experience across different pages.</p>
<p><a href="https://fezcode.com/blog/app-js-main-component">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Index Js Entry Point]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/index-js-entry-point</link>
            <guid isPermaLink="false">https://fezcode.com/blog/index-js-entry-point</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>003 - <code>src/index.js</code> Entry Point Explained</h1>
<p><code>src/index.js</code> is the absolute entry point of your React application. It&#39;s the first JavaScript file that gets executed when your web page loads. Its primary responsibility is to render your root React component (<code>App</code> in this case) into the HTML document.</p>
<pre><code class="language-javascript">import React from &#39;react&#39;;
import ReactDOM from &#39;react-dom/client&#39;;
import &#39;./index.css&#39;;
import App from &#39;./App&#39;;
import reportWebVitals from &#39;./reportWebVitals&#39;;

const root = ReactDOM.createRoot(document.getElementById(&#39;root&#39;));
root.render(
  &lt;React.StrictMode&gt;
    &lt;App /&gt;
  &lt;/React.StrictMode&gt;,
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
reportWebVitals();
</code></pre>
<h2>Line-by-Line Explanation</h2>
<h3>Imports</h3>
<pre><code class="language-javascript">import React from &#39;react&#39;;
</code></pre>
<ul>
<li><strong><code>import React from &#39;react&#39;;</code></strong>: This line imports the <code>React</code> library. Even though you might not directly use <code>React.createElement</code> in JSX, importing <code>React</code> is traditionally required by Babel (the JavaScript compiler) to transform JSX into <code>React.createElement</code> calls. In newer versions of React and Babel, this might be optimized away, but it&#39;s still a common practice.</li>
</ul>
<pre><code class="language-javascript">import ReactDOM from &#39;react-dom/client&#39;;
</code></pre>
<ul>
<li><strong><code>import ReactDOM from &#39;react-dom/client&#39;;</code></strong>: This imports the <code>ReactDOM</code> client-specific library, which provides methods to interact with the DOM (Document Object Model) in a web browser. Specifically, <code>react-dom/client</code> is the modern API for client-side rendering with React 18+.</li>
</ul>
<pre><code class="language-javascript">import &#39;./index.css&#39;;
</code></pre>
<ul>
<li><strong><code>import &#39;./index.css&#39;;</code></strong>: This line imports the global CSS stylesheet for the application. When bundled, Webpack (or a similar tool used by Create React App/Craco) processes this import, often injecting the styles into the HTML document at runtime or extracting them into a separate CSS file.</li>
</ul>
<pre><code class="language-javascript">import App from &#39;./App&#39;;
</code></pre>
<ul>
<li><strong><code>import App from &#39;./App&#39;;</code></strong>: This imports the main <code>App</code> component, which serves as the root of your entire React component tree. The <code>App</code> component will contain the application&#39;s layout, routing, and other main functionalities.</li>
</ul>
<pre><code class="language-javascript">import reportWebVitals from &#39;./reportWebVitals&#39;;
</code></pre>
<ul>
<li><strong><code>import reportWebVitals from &#39;./reportWebVitals&#39;;</code></strong>: This imports a utility function that helps measure and report on your application&#39;s Web Vitals. Web Vitals are a set of metrics from Google that quantify the user experience of a web page.</li>
</ul>
<h3>Root Element Creation and Rendering</h3>
<pre><code class="language-javascript">const root = ReactDOM.createRoot(document.getElementById(&#39;root&#39;));
</code></pre>
<ul>
<li><strong><code>ReactDOM.createRoot(document.getElementById(&#39;root&#39;))</code></strong>: This is the modern way to initialize a React application for client-side rendering (React 18+). It finds the HTML element with the ID <code>root</code> (which is typically found in <code>public/index.html</code>) and creates a React root. This <code>root</code> object is where your React application will be attached to the DOM.</li>
</ul>
<pre><code class="language-javascript">root.render(
  &lt;React.StrictMode&gt;
    &lt;App /&gt;
  &lt;/React.StrictMode&gt;,
);
</code></pre>
<ul>
<li><p><strong><code>root.render(...)</code></strong>: This method tells React to display the <code>App</code> component inside the <code>root</code> DOM element. Whatever is rendered within <code>root.render</code> will be managed by React.</p>
<ul>
<li><strong><code>&lt;React.StrictMode&gt;</code></strong>: This is a wrapper component that helps identify potential problems in an application. It activates additional checks and warnings for its descendants during development mode. For example, it helps detect deprecated lifecycles, unexpected side effects, and more. It does <em>not</em> render any visible UI; it&#39;s purely a development tool.</li>
<li><strong><code>&lt;App /&gt;</code></strong>: This is your main application component, as imported earlier. All other components and the entire UI will be rendered as children of this <code>App</code> component.</li>
</ul>
</li>
</ul>
<h3>Web Vitals Reporting</h3>
<pre><code class="language-javascript">reportWebVitals();
</code></pre>
<ul>
<li><strong><code>reportWebVitals();</code></strong>: This function call initiates the measurement and reporting of Web Vitals metrics, which can be useful for performance monitoring and optimization. The function in <code>reportWebVitals.js</code> typically sends these metrics to an analytics endpoint or logs them to the console.</li>
</ul>
<h2>Summary</h2>
<p><code>src/index.js</code> is the foundational file where your React application begins its life in the browser. It sets up the bridge between your React code and the actual HTML document, ensuring your components are rendered and managed correctly, and optionally enables development tools like Strict Mode and performance monitoring with Web Vitals.</p>
<p><a href="https://fezcode.com/blog/index-js-entry-point">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Package Json Explained]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/package-json-explained</link>
            <guid isPermaLink="false">https://fezcode.com/blog/package-json-explained</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>002 - package.json Explained</h1>
<p>The <code>package.json</code> file is a crucial part of any Node.js project, including React applications. It acts as a manifest for the project, listing its metadata, scripts, and dependencies. Let&#39;s break down the key sections of this project&#39;s <code>package.json</code>.</p>
<pre><code class="language-json">{
  &quot;name&quot;: &quot;fezcodex&quot;,
  &quot;version&quot;: &quot;0.1.0&quot;,
  &quot;private&quot;: true,
  &quot;homepage&quot;: &quot;https://fezcode.com&quot;,
  &quot;dependencies&quot;: {
    &quot;@phosphor-icons/react&quot;: &quot;^2.1.10&quot;,
    &quot;@testing-library/dom&quot;: &quot;^10.4.1&quot;,
    &quot;@testing-library/jest-dom&quot;: &quot;^6.9.1&quot;,
    &quot;@testing-library/react&quot;: &quot;^16.3.0&quot;,
    &quot;@testing-library/user-event&quot;: &quot;^13.5.0&quot;,
    &quot;framer-motion&quot;: &quot;^12.23.24&quot;,
    &quot;front-matter&quot;: &quot;^4.0.2&quot;,
    &quot;react&quot;: &quot;^19.2.0&quot;,
    &quot;react-dom&quot;: &quot;^19.2.0&quot;,
    &quot;react-icons&quot;: &quot;^5.5.0&quot;,
    &quot;react-markdown&quot;: &quot;^10.1.0&quot;,
    &quot;react-router-dom&quot;: &quot;^7.9.4&quot;,
    &quot;react-scripts&quot;: &quot;5.0.1&quot;,
    &quot;react-slick&quot;: &quot;^0.31.0&quot;,
    &quot;react-syntax-highlighter&quot;: &quot;^15.6.6&quot;,
    &quot;slick-carousel&quot;: &quot;^1.8.1&quot;,
    &quot;web-vitals&quot;: &quot;^2.1.4&quot;
  },
  &quot;scripts&quot;: {
    &quot;prestart&quot;: &quot;node scripts/generateWallpapers.js&quot;,
    &quot;start&quot;: &quot;craco start&quot;,
    &quot;prebuild&quot;: &quot;node scripts/generateWallpapers.js&quot;,
    &quot;build&quot;: &quot;craco build&quot;,
    &quot;test&quot;: &quot;craco test&quot;,
    &quot;eject&quot;: &quot;react-scripts eject&quot;,
    &quot;lint&quot;: &quot;eslint \&quot;src/**/*.{js,jsx}\&quot; \&quot;scripts/**/*.js\&quot; --fix&quot;,
    &quot;format&quot;: &quot;prettier --write \&quot;src/**/*.{js,jsx,css,json}\&quot;&quot;,
    &quot;predeploy&quot;: &quot;npm run build&quot;,
    &quot;deploy&quot;: &quot;gh-pages -d build -b gh-pages&quot;
  },
  &quot;eslintConfig&quot;: {
    &quot;extends&quot;: [
      &quot;react-app&quot;,
      &quot;react-app/jest&quot;
    ]
  },
  &quot;browserslist&quot;: {
    &quot;production&quot;: [
      &quot;&gt;0.2%&quot;,
      &quot;not dead&quot;,
      &quot;not op_mini all&quot;
    ],
    &quot;development&quot;: [
      &quot;last 1 chrome version&quot;,
      &quot;last 1 firefox version&quot;,
      &quot;last 1 safari version&quot;
    ]
  },
  &quot;devDependencies&quot;: {
    &quot;@craco/craco&quot;: &quot;^7.1.0&quot;,
    &quot;@tailwindcss/typography&quot;: &quot;^0.5.19&quot;,
    &quot;autoprefixer&quot;: &quot;^10.4.21&quot;,
    &quot;cross-env&quot;: &quot;^10.1.0&quot;,
    &quot;gh-pages&quot;: &quot;^6.3.0&quot;,
    &quot;postcss&quot;: &quot;^8.5.6&quot;,
    &quot;prettier&quot;: &quot;^3.6.2&quot;,
    &quot;tailwindcss&quot;: &quot;^3.4.18&quot;
  }
}
</code></pre>
<h2>Top-Level Fields</h2>
<ul>
<li><strong><code>name</code></strong>: <code>&quot;fezcodex&quot;</code> - The name of the project. This is often used for npm packages and identifies your project.</li>
<li><strong><code>version</code></strong>: <code>&quot;0.1.0&quot;</code> - The current version of the project. Follows semantic versioning (major.minor.patch).</li>
<li><strong><code>private</code></strong>: <code>true</code> - Indicates that the package is not intended to be published to a public npm registry. This is common for application-level projects.</li>
<li><strong><code>homepage</code></strong>: <code>&quot;https://fezcode.com&quot;</code> - Specifies the homepage URL for the project. For applications deployed to GitHub Pages, this is often the live URL.</li>
</ul>
<h2><code>dependencies</code></h2>
<p>This section lists all the packages required by the application to <em>run in production</em>. These are core libraries that your code directly uses.</p>
<ul>
<li><strong><code>@phosphor-icons/react</code></strong>: Provides a flexible icon library with a focus on consistency and customization.</li>
<li><strong><code>@testing-library/dom</code>, <code>@testing-library/jest-dom</code>, <code>@testing-library/react</code>, <code>@testing-library/user-event</code></strong>: These are testing utilities that facilitate writing user-centric tests for React components. They help ensure the application behaves as expected from a user&#39;s perspective.</li>
<li><strong><code>framer-motion</code></strong>: A powerful and easy-to-use library for creating animations and interactive elements in React applications.</li>
<li><strong><code>front-matter</code></strong>: A utility for parsing front-matter (metadata) from strings, typically used with Markdown files.</li>
<li><strong><code>react</code></strong>: The core React library itself.</li>
<li><strong><code>react-dom</code></strong>: Provides DOM-specific methods that enable React to interact with the web browser&#39;s DOM.</li>
<li><strong><code>react-icons</code></strong>: Another popular library offering a wide range of customizable SVG icons from various icon packs.</li>
<li><strong><code>react-markdown</code></strong>: A React component that securely renders Markdown as React elements, allowing you to display Markdown content in your application.</li>
<li><strong><code>react-router-dom</code></strong>: The standard library for client-side routing in React applications, allowing navigation between different views.</li>
<li><strong><code>react-scripts</code></strong>: A package from Create React App that provides scripts for common development tasks like starting a development server, building for production, and running tests.</li>
<li><strong><code>react-slick</code> / <code>slick-carousel</code></strong>: Libraries used for creating carousels or sliders, likely for displaying image galleries or testimonials.</li>
<li><strong><code>react-syntax-highlighter</code></strong>: A component that enables syntax highlighting for code blocks, often used in conjunction with <code>react-markdown</code> to display code snippets beautifully.</li>
<li><strong><code>web-vitals</code></strong>: A library for measuring and reporting on a set of standardized metrics that reflect the real-world user experience on your website.</li>
</ul>
<h2><code>scripts</code></h2>
<p>This object defines a set of command-line scripts that can be executed using <code>npm run &lt;script-name&gt;</code>. These automate common development and deployment tasks.</p>
<ul>
<li><strong><code>prestart</code></strong>: <code>&quot;node scripts/generateWallpapers.js&quot;</code> - A pre-script hook that runs <em>before</em> the <code>start</code> script. In this case, it executes a Node.js script to generate wallpapers, likely for dynamic backgrounds or assets.</li>
<li><strong><code>start</code></strong>: <code>&quot;craco start&quot;</code> - Starts the development server. <code>craco</code> (Create React App Configuration Override) is used here to allow customizing the underlying Webpack/Babel configuration of <code>react-scripts</code> without ejecting the CRA setup.</li>
<li><strong><code>prebuild</code></strong>: <code>&quot;node scripts/generateWallpapers.js&quot;</code> - Similar to <code>prestart</code>, this runs <em>before</em> the <code>build</code> script, ensuring assets are generated before the production build.</li>
<li><strong><code>build</code></strong>: <code>&quot;craco build&quot;</code> - Creates a production-ready build of the application, optimizing and bundling all assets for deployment.</li>
<li><strong><code>test</code></strong>: <code>&quot;craco test&quot;</code> - Runs the project&#39;s test suite.</li>
<li><strong><code>eject</code></strong>: <code>&quot;react-scripts eject&quot;</code> - This is a one-way operation that removes the single build dependency from your project, giving you full control over the Webpack configuration files and build scripts. It&#39;s rarely used unless deep customization is needed.</li>
<li><strong><code>lint</code></strong>: <code>&quot;eslint \&quot;src/**/*.{js,jsx}\&quot; \&quot;scripts/**/*.js\&quot; --fix&quot;</code> - Runs ESLint, a tool for identifying and reporting on patterns in JavaScript code to maintain code quality and style. The <code>--fix</code> flag attempts to automatically fix some issues.</li>
<li><strong><code>format</code></strong>: <code>&quot;prettier --write \&quot;src/**/*.{js,jsx,css,json}\&quot;&quot;</code> - Runs Prettier, an opinionated code formatter, to ensure consistent code style across the project. The <code>--write</code> flag formats files in place.</li>
<li><strong><code>predeploy</code></strong>: <code>&quot;npm run build&quot;</code> - Runs the <code>build</code> script before the <code>deploy</code> script, ensuring that the latest production build is created before deployment.</li>
<li><strong><code>deploy</code></strong>: <code>&quot;gh-pages -d build -b gh-pages&quot;</code> - Deploys the <code>build</code> directory to the <code>gh-pages</code> branch of the GitHub repository, facilitating hosting on GitHub Pages.</li>
</ul>
<h2><code>eslintConfig</code></h2>
<p>This field configures ESLint. <code>&quot;extends&quot;: [&quot;react-app&quot;, &quot;react-app/jest&quot;]</code> means it&#39;s extending the recommended ESLint configurations provided by Create React App, along with specific rules for Jest testing.</p>
<h2><code>browserslist</code></h2>
<p>This field specifies the target browsers for your client-side code. This is used by tools like Babel and Autoprefixer to ensure your JavaScript and CSS are compatible with the specified browser versions.</p>
<ul>
<li><strong><code>production</code></strong>: Defines the browser targets for the production build (e.g., browsers with more than 0.2% market share, excluding Internet Explorer-era browsers and Opera Mini).</li>
<li><strong><code>development</code></strong>: Defines less strict browser targets for development, usually focusing on the latest versions of common development browsers.</li>
</ul>
<h2><code>devDependencies</code></h2>
<p>These are packages required only for development and building the project, not for the application to run in production. They provide tools, testing utilities, and build-related functionalities.</p>
<ul>
<li><strong><code>@craco/craco</code></strong>: The main Craco package that allows overriding Create React App&#39;s Webpack configuration.</li>
<li><strong><code>@tailwindcss/typography</code></strong>: A Tailwind CSS plugin that provides a set of <code>prose</code> classes to add beautiful typographic defaults to raw HTML or Markdown, improving readability of content.</li>
<li><strong><code>autoprefixer</code></strong>: A PostCSS plugin that adds vendor prefixes to CSS rules, ensuring cross-browser compatibility.</li>
<li><strong><code>cross-env</code></strong>: A utility that provides a universal way to set environment variables across different operating systems, commonly used in npm scripts.</li>
<li><strong><code>gh-pages</code></strong>: A tool specifically for publishing content to the <code>gh-pages</code> branch on GitHub, used for deploying to GitHub Pages.</li>
<li><strong><code>postcss</code></strong>: A tool for transforming CSS with JavaScript plugins. Tailwind CSS relies on PostCSS.</li>
<li><strong><code>prettier</code></strong>: The code formatter used in the <code>format</code> script.</li>
<li><strong><code>tailwindcss</code></strong>: The core Tailwind CSS framework, enabling utility-first styling in the project.</li>
</ul>
<p>This <code>package.json</code> file provides a comprehensive insight into the project&#39;s setup, dependencies, and available scripts for development, testing, and deployment.</p>
<p><a href="https://fezcode.com/blog/package-json-explained">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Project Overview]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/project-overview</link>
            <guid isPermaLink="false">https://fezcode.com/blog/project-overview</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 25 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>001 - Project Overview: Fezcode</h1>
<p>This document provides a high-level overview of the &quot;Fezcode&quot; project, a React-based web application designed to serve as a personal blog or portfolio site.</p>
<h2>Purpose</h2>
<p>The primary purpose of this project is to display blog posts, projects, and other content in a structured and visually appealing manner. It leverages modern web technologies to create a dynamic and responsive user experience.</p>
<h2>Key Technologies</h2>
<p>The project is built using the following core technologies:</p>
<ul>
<li><strong>React:</strong> A JavaScript library for building user interfaces. It allows for the creation of reusable UI components and manages the state of the application efficiently.</li>
<li><strong>Create React App (CRA) with Craco:</strong> The project was likely bootstrapped using Create React App, which provides a solid foundation for React development. Craco (Create React App Configuration Override) is used to customize the Webpack and Babel configurations without ejecting from CRA, enabling features like Tailwind CSS integration.</li>
<li><strong>Tailwind CSS:</strong> A utility-first CSS framework that allows for rapid UI development by composing pre-defined CSS classes directly in the markup.</li>
<li><strong>React Router DOM:</strong> A library for handling client-side routing in React applications, enabling navigation between different pages without full page reloads.</li>
<li><strong>Framer Motion:</strong> A production-ready motion library for React, used for animations and interactive elements.</li>
<li><strong>Phosphor Icons / React Icons:</strong> Libraries providing a collection of customizable SVG icons.</li>
<li><strong>Markdown:</strong> Blog post content is written in Markdown and rendered using <code>react-markdown</code>.</li>
<li><strong>Syntax Highlighting:</strong> Code blocks within Markdown are highlighted using <code>react-syntax-highlighter</code>.</li>
<li><strong>GitHub Pages:</strong> The application is deployed to GitHub Pages, a static site hosting service.</li>
</ul>
<h2>Project Structure Highlights</h2>
<p>The project follows a typical React application structure, with key directories including:</p>
<ul>
<li><strong><code>public/</code></strong>: Contains static assets like <code>index.html</code>, images, and the raw content for blog posts (<code>posts/</code>), logs (<code>logs/</code>), and projects (<code>projects/</code>).</li>
<li><strong><code>src/</code></strong>: Contains the main application source code, organized into:<ul>
<li><strong><code>components/</code></strong>: Reusable UI components (e.g., <code>Navbar</code>, <code>Footer</code>, <code>Toast</code>).</li>
<li><strong><code>pages/</code></strong>: Page-level components that represent different views of the application (e.g., <code>HomePage</code>, <code>BlogPostPage</code>, <code>NotFoundPage</code>).</li>
<li><strong><code>hooks/</code></strong>: Custom React hooks for encapsulating reusable logic (e.g., <code>useToast</code>).</li>
<li><strong><code>utils/</code></strong>: Utility functions and helpers.</li>
<li><strong><code>styles/</code></strong>: Custom CSS files.</li>
<li><strong><code>config/</code></strong>: Configuration files (e.g., colors, fonts).</li>
</ul>
</li>
<li><strong><code>scripts/</code></strong>: Contains utility scripts, such as <code>generateWallpapers.js</code>.</li>
</ul>
<h2>How it Works (High-Level)</h2>
<ol>
<li><strong>Entry Point (<code>src/index.js</code>):</strong> The application starts by rendering the main <code>App</code> component into the <code>index.html</code> file.</li>
<li><strong>Main Application (<code>src/App.js</code>):</strong> The <code>App</code> component sets up client-side routing using <code>HashRouter</code>, defines the overall layout, and manages global contexts like the <code>ToastContext</code>.</li>
<li><strong>Routing (<code>react-router-dom</code>):</strong> <code>AnimatedRoutes</code> (likely a component that uses <code>react-router-dom</code>&#39;s <code>Routes</code> and <code>Route</code> components) handles mapping URLs to specific page components.</li>
<li><strong>Content Fetching:</strong> Blog posts and other dynamic content are fetched from <code>.txt</code> files located in the <code>public/</code> directory. Metadata for these posts is often stored in corresponding <code>.json</code> files (e.g., <code>public/posts/posts.json</code>). The blog page now includes a search functionality to easily find posts by title or slug.</li>
<li><strong>Styling (<code>Tailwind CSS</code>):</strong> The UI is styled primarily using Tailwind CSS utility classes, with some custom CSS if needed.</li>
<li><strong>Deployment:</strong> The application is built into static assets and deployed to GitHub Pages using the <code>gh-pages</code> package.</li>
</ol>
<p>This overview provides a foundational understanding of the Fezcode project. Subsequent documents will delve into more specific details of each component and concept.</p>
<p><a href="https://fezcode.com/blog/project-overview">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[The Necessary D&D Post]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/dnd-content</link>
            <guid isPermaLink="false">https://fezcode.com/blog/dnd-content</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 21 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>RPG and DnD Content</h1>
<blockquote>
<p>When you see <code>d&amp;d</code> tag, it also includes fantasy and rpg content as well.</p>
</blockquote>
<p>Dungeons &amp; Dragons is <em>kinda</em> fun, offering engaging narratives and strategic depth.
It&#39;s a hobby that provides a welcome escape and a platform for creative exploration.</p>
<p><a href="https://www.youtube.com/watch?v=UkgVL05NsWo">Divinity - Original Sin 2 (Main Theme)</a></p>
<h2>Introduction</h2>
<p>Let&#39;s talk about some of the D&amp;D and RPG content I enjoy.
I often find myself immersed in the detailed lore and mechanics of games like <strong>Divinity Original Sin 2</strong>, where decisions carry weight.
I appreciate shows like &quot;Secret Level,&quot; especially for their Warhammer episodes, which offer a glimpse into the creative process behind these expansive universes.
My gaming collection is rich with D&amp;D-inspired titles such as <strong>Baldur&#39;s Gate 3</strong>, <strong>Pathfinder</strong>, <strong>Warhammer 40,000: Dawn of War II</strong>, and <strong>SpellForce 3</strong>.
Beyond these, I also delve into other fantasy epics like the <strong>Warcraft</strong> series, each presenting its own blend of adventure and challenge.</p>
<p>My connection to these worlds also includes a practical side, evident in my Warhammer figurine collection.
The process of building and painting these miniatures is a satisfying aspect of the hobby,
complementing the imaginative battles they represent. It&#39;s an activity that combines creativity,
strategic thinking, and a sense of community, all contributing to a well-rounded pastime.</p>
<p><a href="https://www.youtube.com/watch?v=ncauRK9f75Q">Secret Level - Warhammer Episode</a></p>
<h2>D&amp;D, RPG, and Fantasy: A Quick Dive</h2>
<p>It&#39;s easy to use terms like D&amp;D, RPG, and fantasy interchangeably, and honestly, I often do! While they&#39;re distinct, they share a lot of common ground, especially in the realm of immersive storytelling and world-building.</p>
<p><strong>Dungeons &amp; Dragons (D&amp;D)</strong> is a specific tabletop role-playing game, often considered the progenitor of modern RPGs. It has its own set of rules, lore, and established settings. When I talk about D&amp;D, I&#39;m usually referring to this specific game or experiences directly inspired by its mechanics and themes.</p>
<p><strong>Role-Playing Games (RPGs)</strong> are a broader genre where players assume the roles of characters in a fictional setting. This can be tabletop (like D&amp;D), video games (like Divinity Original Sin 2 or Baldur&#39;s Gate 3), or even live-action. The core is character development, narrative choice, and often, strategic combat.</p>
<p><strong>Fantasy</strong> is a genre of speculative fiction set in a fictional universe, often inspired by real-world myth and folklore. It typically involves magic, mythical creatures, and supernatural elements. Many RPGs and D&amp;D campaigns are set in fantasy worlds, but not all fantasy is an RPG, and not all RPGs are fantasy (think sci-fi RPGs).</p>
<p>Here&#39;s a quick comparison:</p>
<table>
<thead>
<tr>
<th align="left">Feature</th>
<th align="left">Dungeons &amp; Dragons (D&amp;D)</th>
<th align="left">Role-Playing Games (RPGs)</th>
<th align="left">Fantasy Genre</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>Definition</strong></td>
<td align="left">Specific tabletop game</td>
<td align="left">Broad game genre</td>
<td align="left">Broad fiction genre</td>
</tr>
<tr>
<td align="left"><strong>Ruleset</strong></td>
<td align="left">Specific, detailed</td>
<td align="left">Varies widely</td>
<td align="left">N/A</td>
</tr>
<tr>
<td align="left"><strong>Player Role</strong></td>
<td align="left">Character in D&amp;D world</td>
<td align="left">Character in fictional setting</td>
<td align="left">Reader/Viewer</td>
</tr>
<tr>
<td align="left"><strong>Interaction</strong></td>
<td align="left">High (tabletop)</td>
<td align="left">High (tabletop/video game)</td>
<td align="left">Low (passive)</td>
</tr>
<tr>
<td align="left"><strong>Setting</strong></td>
<td align="left">Typically fantasy</td>
<td align="left">Any (fantasy, sci-fi, etc.)</td>
<td align="left">Typically magical/mythical</td>
</tr>
</tbody></table>
<p>Despite their differences, the lines blur constantly. Many fantasy stories lend themselves perfectly to RPGs, and D&amp;D is a prime example of a fantasy RPG. For me, when I say &quot;DND,&quot; I&#39;m often using it as a shorthand to encompass all these wonderful aspects: the specific game, the broader genre of role-playing, and the rich, imaginative fantasy worlds they inhabit. It&#39;s all part of the same grand adventure!</p>
<p><a href="https://fezcode.com/blog/dnd-content">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Gemini Explains How Hooks Work with Toast Component]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/gemini-explains-how-hooks-work-with-toast-component</link>
            <guid isPermaLink="false">https://fezcode.com/blog/gemini-explains-how-hooks-work-with-toast-component</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Sat, 18 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Gemini Explains How Hooks Work with Toast Component</h1>
<p>There are two version of this, first one is explanation it extracted from the system, and the second is the one I prompted it to create explanation.</p>
<h1>First One: Gemini Explains How Hooks Work with Toast Component</h1>
<p>This post explains the changes made to implement a toast notification system, focusing on React concepts like Hooks, Context, and Providers.</p>
<h2>Core Concepts</h2>
<h3>Components</h3>
<p>React applications are built using <strong>components</strong>. A component is a reusable piece of code that defines a part of the user interface.</p>
<h3>State</h3>
<p>Components can have <strong>state</strong>. State is data that is managed by the component. When the state of a component changes, React automatically re-renders the component to reflect the new state.</p>
<h3>Hooks</h3>
<p><strong>Hooks</strong> are special functions that let you &quot;hook into&quot; React features. They were introduced in React to allow the use of state and other React features in functional components. Hooks are functions whose names start with <code>use</code>.</p>
<ul>
<li><code>useState</code>: This hook lets you add state to a functional component.</li>
<li><code>useEffect</code>: This hook lets you perform side effects in a functional component.</li>
<li><code>useContext</code>: This hook lets you subscribe to React context without introducing nesting.</li>
</ul>
<h3>Context and Providers</h3>
<p><strong>Context</strong> provides a way to share data between components without having to pass props down manually at every level. The <strong>Provider</strong> is a component that makes the data available to all the components in the tree below it. Any component in the tree can then access the data using the <code>useContext</code> hook.</p>
<h3>Custom Hooks</h3>
<p>A <strong>custom hook</strong> is a JavaScript function whose name starts with <code>use</code> and that calls other hooks. Custom hooks are a way to reuse stateful logic between components.</p>
<h2>File-by-File Explanation of Changes</h2>
<p>Now, let&#39;s look at the changes made to each file.</p>
<h3><code>src/hooks/useToast.js</code></h3>
<p>This file defines a custom hook called <code>useToast</code>. It uses <code>useContext</code> to access the <code>ToastContext</code> and provides a simple way for any component to access the <code>addToast</code> and <code>removeToast</code> functions.</p>
<pre><code class="language-javascript">import { useContext } from &#39;react&#39;;
import { ToastContext } from &#39;../components/ToastContext&#39;;

export const useToast = () =&gt; {
  return useContext(ToastContext);
};
</code></pre>
<h3><code>src/components/ToastContext.js</code></h3>
<p>This file defines the <code>ToastContext</code> component, which manages the state of the toasts. It uses <code>useState</code> to hold the <code>toasts</code> array, and provides <code>addToast</code> and <code>removeToast</code> functions. It also renders the <code>Toast</code> components.</p>
<pre><code class="language-javascript">import React, { createContext, useState, useCallback } from &#39;react&#39;;
import Toast from &#39;./Toast&#39;;

export const ToastContext = createContext();

let id = 0;

export const ToastContext = ({ children }) =&gt; {
  const [toasts, setToasts] = useState([]);

  const addToast = useCallback((toast) =&gt; {
    const newToast = { ...toast, id: id++ };
    setToasts((prevToasts) =&gt; {
      if (prevToasts.length &gt;= 5) {
        const updatedToasts = prevToasts.slice(0, prevToasts.length - 1);
        return [newToast, ...updatedToasts];
      }
      return [newToast, ...prevToasts];
    });
  }, []);

  const removeToast = useCallback((id) =&gt; {
    setToasts((prevToasts) =&gt; prevToasts.filter((toast) =&gt; toast.id !== id));
  }, []);

  return (
    &lt;ToastContext.Provider value={{ addToast, removeToast }}&gt;
      {children}
      &lt;div className=&quot;fixed top-28 right-10 z-50&quot;&gt;
          {toasts.map((toast) =&gt; (
            &lt;Toast
              key={toast.id}
              id={toast.id}
              title={toast.title}
              message={toast.message}
              duration={toast.duration}
              removeToast={removeToast}
            /&gt;
          ))}
      &lt;/div&gt;
    &lt;/ToastContext.Provider&gt;
  );
};
</code></pre>
<h3><code>src/components/Toast.js</code></h3>
<p>This file defines the <code>Toast</code> component, which is the UI for a single toast notification. It uses <code>useEffect</code> to set a timer for automatic removal and <code>framer-motion</code> for animations.</p>
<pre><code class="language-javascript">import React, { useEffect } from &#39;react&#39;;
import { motion } from &#39;framer-motion&#39;;
import { X } from &#39;@phosphor-icons/react&#39;;

const Toast = ({ id, title, message, duration, removeToast }) =&gt; {
  useEffect(() =&gt; {
    const timer = setTimeout(() =&gt; {
      removeToast(id);
    }, duration);

    return () =&gt; {
      clearTimeout(timer);
    };
  }, [id, duration, removeToast]);

  return (
    &lt;motion.div
      initial={{ x: &#39;100%&#39;, opacity: 0 }}
      animate={{ x: 0, opacity: 1 }}
      exit={{ opacity: 0 }}
      transition={{ type: &#39;spring&#39;, stiffness: 120, damping: 20 }}
      className=&quot;text-gray-300 py-4 px-10 rounded-lg shadow-lg border backdrop-blur-sm flex items-center justify-between w-96 mb-4&quot;
      style={{ backgroundColor: &#39;rgba(68, 64, 59, 0.8)&#39;, borderColor: &#39;#5a5e64&#39; }}
    &gt;
      &lt;div className=&quot;flex flex-col text-sm&quot;&gt;
        &lt;span&gt;{title}&lt;/span&gt;
        &lt;span&gt;{message}&lt;/span&gt;
      &lt;/div&gt;
      &lt;button onClick={() =&gt; removeToast(id)} className=&quot;pr-2&quot;&gt;
        &lt;X size={24} weight=&quot;bold&quot; /&gt;
      &lt;/button&gt;
    &lt;/motion.div&gt;
  );
};

export default Toast;
</code></pre>
<h3><code>src/App.js</code></h3>
<p>The main component of the application. The <code>ToastContext</code> wraps the entire application to make the toast functionality available everywhere.</p>
<pre><code class="language-javascript">import React from &#39;react&#39;;
import { HashRouter as Router } from &#39;react-router-dom&#39;;
import Layout from &#39;./components/Layout&#39;;
import AnimatedRoutes from &#39;./components/AnimatedRoutes&#39;;
import { ToastContext } from &#39;./components/ToastContext&#39;;

function App() {
  return (
    &lt;Router&gt;
      &lt;ToastContext&gt;
        &lt;Layout&gt;
          &lt;AnimatedRoutes /&gt;
        &lt;/Layout&gt;
      &lt;/ToastContext&gt;
    &lt;/Router&gt;
  );
}

export default App;
</code></pre>
<h3><code>src/pages/BlogPostPage.js</code></h3>
<p>This component uses the <code>useToast</code> hook to show a toast notification when a code block is copied.</p>
<pre><code class="language-javascript">// ... imports
import { useToast } from &#39;../hooks/useToast&#39;;

// ...

const CodeBlock = ({ node, inline, className, children, openModal, ...props }) =&gt; {
  const match = /language-(\w+)/.exec(className || &#39;&#39;);
  const { addToast } = useToast();
  const handleCopy = () =&gt; {
    const textToCopy = String(children);
    if (navigator.clipboard &amp;&amp; navigator.clipboard.writeText) {
      navigator.clipboard.writeText(textToCopy).then(() =&gt; {
        addToast({ title: &#39;Success&#39;, message: &#39;Copied to clipboard!&#39;, duration: 3000 });
      }, () =&gt; {
        addToast({ title: &#39;Error&#39;, message: &#39;Failed to copy!&#39;, duration: 3000 });
      });
    } else {
      const textArea = document.createElement(&#39;textarea&#39;);
      textArea.value = textToCopy;
      textArea.style.position = &#39;fixed&#39;;
      textArea.style.left = &#39;-9999px&#39;;
      document.body.appendChild(textArea);
      textArea.focus();
      textArea.select();
      try {
        document.execCommand(&#39;copy&#39;);
        addToast({ title: &#39;Success&#39;, message: &#39;Copied to clipboard!&#39;, duration: 3000 });
      } catch (err) {
        addToast({ title: &#39;Error&#39;, message: &#39;Failed to copy!&#39;, duration: 3000 });
      }
      document.body.removeChild(textArea);
    }
  };

  // ... rest of the component
};

// ... rest of the file
</code></pre>
<hr>
<h1>Second One: Gemini Explains How Hooks Work with Toast Component</h1>
<p>I gave Gemini the following prompt:</p>
<blockquote>
<p>Pretend like I have no idea what react.js is. I want you to
explain everything done in the changed files. What is useHook
what are hooks and all that. The changes made to App.js,
Blogpostpage.js, toast.js, toastprovider.js (and what provider
is), useToast.js (what is useXXXX). Explain everything step by
step to me. Write everything to <code>explanation.md</code> file. (not
exists)</p>
</blockquote>
<h1>Explanation of the Toast Notification System</h1>
<p>This document explains the changes made to the website to implement a toast notification system. The explanation is intended for someone with no prior knowledge of React.js.</p>
<h2>Core Concepts</h2>
<p>Before diving into the code changes, let&#39;s understand some fundamental concepts of React.</p>
<h3>Components</h3>
<p>React applications are built using <strong>components</strong>. A component is a reusable piece of code that defines a part of the user interface. For example, a button, a form, or a navigation bar can all be components. Components can be nested inside other components to create complex user interfaces.</p>
<h3>State</h3>
<p>Components can have <strong>state</strong>. State is data that is managed by the component. When the state of a component changes, React automatically re-renders the component to reflect the new state. For example, a component that displays a counter might have a <code>count</code> state. When the <code>count</code> is incremented, the component re-renders to display the new value.</p>
<h3>Hooks</h3>
<p><strong>Hooks</strong> are special functions that let you &quot;hook into&quot; React features. They were introduced in React to allow the use of state and other React features in functional components (which are simpler than class-based components). Hooks are functions whose names start with <code>use</code>. Here are some common hooks:</p>
<ul>
<li><code>useState</code>: This hook lets you add state to a functional component.</li>
<li><code>useEffect</code>: This hook lets you perform side effects in a functional component. Side effects are operations that are not directly related to rendering the UI, such as fetching data from a server or setting up a timer.</li>
<li><code>useContext</code>: This hook lets you subscribe to React context without introducing nesting.</li>
</ul>
<h3>Context and Providers</h3>
<p>In a React application, data is typically passed from parent components to child components via props. However, this can become cumbersome for data that needs to be accessed by many components at different nesting levels.</p>
<p><strong>Context</strong> provides a way to share data between components without having to pass props down manually at every level. The <strong>Provider</strong> is a component that makes the data available to all the components in the tree below it. Any component in the tree can then access the data using the <code>useContext</code> hook.</p>
<h3>Custom Hooks</h3>
<p>A <strong>custom hook</strong> is a JavaScript function whose name starts with <code>use</code> and that calls other hooks. Custom hooks are a way to reuse stateful logic between components. For example, you could create a custom hook that fetches data from a server and returns the data and a loading state. This hook could then be used in any component that needs to fetch data.</p>
<h2>File-by-File Explanation of Changes</h2>
<p>Now, let&#39;s look at the changes made to each file.</p>
<h3><code>src/hooks/useToast.js</code></h3>
<p>This file defines a custom hook called <code>useToast</code>.</p>
<pre><code class="language-javascript">import { useContext } from &#39;react&#39;;
import { ToastContext } from &#39;../components/ToastContext&#39;;

export const useToast = () =&gt; {
  return useContext(ToastContext);
};
</code></pre>
<ul>
<li><code>useContext(ToastContext)</code>: This line uses the <code>useContext</code> hook to access the data provided by the <code>ToastContext</code>. The <code>ToastContext</code> is created in <code>ToastContext.js</code>.</li>
<li><code>export const useToast</code>: This line exports the <code>useToast</code> hook so that it can be used in other components.</li>
</ul>
<p>This custom hook provides a simple way for any component to access the <code>addToast</code> and <code>removeToast</code> functions without having to pass them down as props.</p>
<h3><code>src/components/ToastContext.js</code></h3>
<p>This file defines the <code>ToastContext</code> component, which is responsible for managing the state of the toasts.</p>
<pre><code class="language-javascript">import React, { createContext, useState, useCallback } from &#39;react&#39;;
import Toast from &#39;./Toast&#39;;

export const ToastContext = createContext();

let id = 0;

export const ToastContext = ({ children }) =&gt; {
  const [toasts, setToasts] = useState([]);

  const addToast = useCallback((toast) =&gt; {
    const newToast = { ...toast, id: id++ };
    setToasts((prevToasts) =&gt; {
      if (prevToasts.length &gt;= 5) {
        const updatedToasts = prevToasts.slice(0, prevToasts.length - 1);
        return [newToast, ...updatedToasts];
      }
      return [newToast, ...prevToasts];
    });
  }, []);

  const removeToast = useCallback((id) =&gt; {
    setToasts((prevToasts) =&gt; prevToasts.filter((toast) =&gt; toast.id !== id));
  }, []);

  return (
    &lt;ToastContext.Provider value={{ addToast, removeToast }}&gt;
      {children}
      &lt;div className=&quot;fixed top-28 right-10 z-50&quot;&gt;
          {toasts.map((toast) =&gt; (
            &lt;Toast
              key={toast.id}
              id={toast.id}
              title={toast.title}
              message={toast.message}
              duration={toast.duration}
              removeToast={removeToast}
            /&gt;
          ))}
      &lt;/div&gt;
    &lt;/ToastContext.Provider&gt;
  );
};
</code></pre>
<ul>
<li><code>export const ToastContext = createContext();</code>: This line creates a new context object.</li>
<li><code>const [toasts, setToasts] = useState([]);</code>: This line uses the <code>useState</code> hook to create a <code>toasts</code> state variable, which is an array that will hold all the toast objects.</li>
<li><code>addToast</code>: This function adds a new toast to the <code>toasts</code> array. It also ensures that there are at most 5 toasts at any given time.</li>
<li><code>removeToast</code>: This function removes a toast from the <code>toasts</code> array.</li>
<li><code>&lt;ToastContext.Provider value={{ addToast, removeToast }}&gt;</code>: This is the provider component. It makes the <code>addToast</code> and <code>removeToast</code> functions available to all the components in the tree below it.</li>
<li><code>{toasts.map((toast) =&gt; ...)}</code>: This code maps over the <code>toasts</code> array and renders a <code>Toast</code> component for each toast object.</li>
</ul>
<h3><code>src/components/Toast.js</code></h3>
<p>This file defines the <code>Toast</code> component, which is the UI for a single toast notification.</p>
<pre><code class="language-javascript">import React, { useEffect } from &#39;react&#39;;
import { motion } from &#39;framer-motion&#39;;
import { X } from &#39;@phosphor-icons/react&#39;;

const Toast = ({ id, title, message, duration, removeToast }) =&gt; {
  useEffect(() =&gt; {
    const timer = setTimeout(() =&gt; {
      removeToast(id);
    }, duration);

    return () =&gt; {
      clearTimeout(timer);
    };
  }, [id, duration, removeToast]);

  return (
    &lt;motion.div
      initial={{ x: &#39;100%&#39;, opacity: 0 }}
      animate={{ x: 0, opacity: 1 }}
      exit={{ opacity: 0 }}
      transition={{ type: &#39;spring&#39;, stiffness: 120, damping: 20 }}
      className=&quot;text-gray-300 py-4 px-10 rounded-lg shadow-lg border backdrop-blur-sm flex items-center justify-between w-96 mb-4&quot;
      style={{ backgroundColor: &#39;rgba(68, 64, 59, 0.8)&#39;, borderColor: &#39;#5a5e64&#39; }}
    &gt;
      &lt;div className=&quot;flex flex-col text-sm&quot;&gt;
        &lt;span&gt;{title}&lt;/span&gt;
        &lt;span&gt;{message}&lt;/span&gt;
      &lt;/div&gt;
      &lt;button onClick={() =&gt; removeToast(id)} className=&quot;pr-2&quot;&gt;
        &lt;X size={24} weight=&quot;bold&quot; /&gt;
      &lt;/button&gt;
    &lt;/motion.div&gt;
  );
};

export default Toast;
</code></pre>
<ul>
<li><code>useEffect(() =&gt; { ... }, [id, duration, removeToast]);</code>: This hook sets a timer to automatically remove the toast after a certain duration. The dependency array <code>[id, duration, removeToast]</code> ensures that the timer is only set once when the component is first rendered.</li>
<li><code>&lt;motion.div ...&gt;</code>: This is a component from the <code>framer-motion</code> library that provides animations. The <code>initial</code>, <code>animate</code>, and <code>exit</code> props define the animations for when the toast appears and disappears.</li>
</ul>
<h3><code>src/App.js</code></h3>
<p>This is the main component of the application. The only change here was to wrap the entire application with the <code>ToastContext</code>.</p>
<pre><code class="language-javascript">import React from &#39;react&#39;;
import { HashRouter as Router } from &#39;react-router-dom&#39;;
import Layout from &#39;./components/Layout&#39;;
import AnimatedRoutes from &#39;./components/AnimatedRoutes&#39;;
import { ToastContext } from &#39;./components/ToastContext&#39;;

function App() {
  return (
    &lt;Router&gt;
      &lt;ToastContext&gt;
        &lt;Layout&gt;
          &lt;AnimatedRoutes /&gt;
        &lt;/Layout&gt;
      &lt;/ToastContext&gt;
    &lt;/Router&gt;
  );
}

export default App;
</code></pre>
<ul>
<li><code>&lt;ToastContext&gt;</code>: By wrapping the <code>Layout</code> component with the <code>ToastContext</code>, we make the <code>addToast</code> and <code>removeToast</code> functions available to all the components in the application.</li>
</ul>
<h3><code>src/pages/BlogPostPage.js</code></h3>
<p>This component displays a single blog post. The change here was to use the <code>useToast</code> hook to show a toast notification when the user copies a code block.</p>
<pre><code class="language-javascript">// ... imports
import { useToast } from &#39;../hooks/useToast&#39;;

// ...

const CodeBlock = ({ node, inline, className, children, openModal, ...props }) =&gt; {
  const match = /language-(\w+)/.exec(className || &#39;&#39;);
  const { addToast } = useToast();
  const handleCopy = () =&gt; {
    const textToCopy = String(children);
    if (navigator.clipboard &amp;&amp; navigator.clipboard.writeText) {
      navigator.clipboard.writeText(textToCopy).then(() =&gt; {
        addToast({ title: &#39;Success&#39;, message: &#39;Copied to clipboard!&#39;, duration: 3000 });
      }, () =&gt; {
        addToast({ title: &#39;Error&#39;, message: &#39;Failed to copy!&#39;, duration: 3000 });
      });
    } else {
      const textArea = document.createElement(&#39;textarea&#39;);
      textArea.value = textToCopy;
      textArea.style.position = &#39;fixed&#39;;
      textArea.style.left = &#39;-9999px&#39;;
      document.body.appendChild(textArea);
      textArea.focus();
      textArea.select();
      try {
        document.execCommand(&#39;copy&#39;);
        addToast({ title: &#39;Success&#39;, message: &#39;Copied to clipboard!&#39;, duration: 3000 });
      } catch (err) {
        addToast({ title: &#39;Error&#39;, message: &#39;Failed to copy!&#39;, duration: 3000 });
      }
      document.body.removeChild(textArea);
    }
  };

  // ... rest of the component
};

// ... rest of the file
</code></pre>
<ul>
<li><code>const { addToast } = useToast();</code>: This line uses the <code>useToast</code> hook to get the <code>addToast</code> function.</li>
<li><code>addToast({ title: &#39;Success&#39;, message: &#39;Copied to clipboard!&#39;, duration: 3000 });</code>: This line calls the <code>addToast</code> function to show a toast notification.</li>
</ul>
<p>This change demonstrates how the <code>useToast</code> hook can be used in any component to easily show toast notifications without having to worry about the implementation details.</p>
<p><a href="https://fezcode.com/blog/gemini-explains-how-hooks-work-with-toast-component">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Do I Need to Create a Lib For That?]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/do-i-need-to-create-a-lib-for-that</link>
            <guid isPermaLink="false">https://fezcode.com/blog/do-i-need-to-create-a-lib-for-that</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Thu, 16 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h2>My Journey into Go Libraries</h2>
<p>Creating my first Go library, <a href="https://github.com/fezcode/go-tournament-brackets">go-tournament-brackets</a>, has been a, <em>experience</em>...
It&#39;s a project that allowed me to dive deep into Go&#39;s capabilities for building reusable and efficient code.
The process of designing the data structures, handling edge cases like automatic bye calculations,
and then building an interactive command-line interface on top of it was both challenging and immensely satisfying.
There&#39;s a unique sense of accomplishment in seeing your code not just work, but also be easily consumable by others.</p>
<h2>About <code>go-tournament-brackets</code></h2>
<p><code>go-tournament-brackets</code> is a versatile Go library designed for generating and managing single-elimination tournament brackets. It offers two primary components:</p>
<ul>
<li><strong>A Robust Go Library:</strong> This provides a set of data models and functions that can be integrated into any Go application.
It intelligently handles tournament logic, including the correct calculation of rounds, match-ups, and automatic byes for varying numbers of participants.</li>
<li><strong>An Interactive Command-Line Interface (CLI):</strong> Built on top of the library, this CLI allows users to run a tournament from start to finish.
You can input participant names, visualize the bracket in ASCII art, and interactively determine match winners until a champion is crowned.</li>
</ul>
<p>This library aims to simplify the process of setting up and managing tournaments, whether you&#39;re integrating it into a larger application or running a quick tournament from your terminal.</p>
<h2>Ok, but why?</h2>
<p>The inspiration for <code>go-tournament-brackets</code> struck during a casual phone call with a friend, Mustafa, (<em>he personally requested to be named directly</em>). I was unwinding, listening to <a href="https://www.youtube.com/watch?v=27lPAUdE1ys">Morcheeba&#39;s &quot;Easier Said than Done,&quot;</a>
when he posed a fun challenge: rank our favorite rappers. His idea was to create a bracket, share it, and play through it together. Simple enough, right?</p>
<p>Not quite. As he started looking for online bracket makers, we quickly hit a wall. Most platforms demanded sign-ups,
locked away certain tournament types behind paywalls, and generally overcomplicated what should have been a straightforward,
enjoyable activity. For something so simple, the hoops we had to jump through felt entirely unnecessary.
That&#39;s when the idea sparked: why not build my own? A bracket maker that was free, flexible, and didn&#39;t force you into a convoluted user experience.
And so, the seed for <code>go-tournament-brackets</code> was planted.</p>
<h2>How did I do that?</h2>
<p>The journey from idea to a working library began with a deep dive into the mechanics of tournament brackets.
I found myself poring over Wikipedia articles, unraveling the intricacies of single-elimination formats, byes, and seeding.
Once I had a solid grasp of the theoretical underpinnings, I turned to my trusty collaborator, Gemini 2.5-Pro.</p>
<p>My first request to Gemini was simple: &quot;Generate the necessary Go files for a tournament bracket library.&quot;
It quickly scaffolded the basic project structure, providing the initial Go files. From there, I started defining the core data structures,
translating the concepts from my research into Go structs. The <code>models.go</code> file, which you can see <a href="https://raw.githubusercontent.com/fezcode/go-tournament-brackets/refs/heads/main/models.go">here</a>, was born out of this phase.</p>
<pre><code class="language-go">// Tournament is the root object that contains all data for a tournament event.
type Tournament struct {
	Name           string
	Rounds         []Round
	TournamentType TournamentType
	Options        *Options
}
</code></pre>
<p>With the foundational structs in place, Gemini and I embarked on implementing the core logic.
This was where the real challenge and fun began. Handling the &quot;bye&quot; mechanism – <strong>ensuring that teams or competitors who automatically advance in the first round are correctly placed</strong> – proved to be particularly tricky.
It&#39;s not as straightforward as it might seem, but with Gemini&#39;s assistance, we iterated through various approaches, and it did its best to help navigate those complexities. It was a true collaborative effort, pushing both my understanding and Gemini&#39;s capabilities to deliver a robust solution.</p>
<h2>Getting Your Go Module Out There!</h2>
<p>So, you&#39;ve built your awesome Go package, and now you want to share it with the world (or at least your fellow developers). Here&#39;s the lowdown on how to get your module published and discoverable:</p>
<ol>
<li><p><strong>Your <code>go.mod</code> File is Key:</strong>
First things first, make sure your <code>go.mod</code> file has the right module path. This should usually point directly to your GitHub repo, like <code>module github.com/fezcode/go-tournament-brackets</code>. This is how Go knows where to find your cool new code!</p>
</li>
<li><p><strong>Tag It, You&#39;re It! (Creating a Release):</strong>
Go modules love Git tags for versioning. Think of a tag as a snapshot of a specific, stable version of your code.</p>
<ul>
<li><strong>Tag your commit:</strong> Use <code>git tag vX.Y.Z</code> (e.g., <code>git tag v0.1.0</code>). We highly recommend following Semantic Versioning (SemVer) – it makes life easier for everyone!</li>
<li><strong>Push that tag:</strong> Don&#39;t forget to push your shiny new tag to GitHub: <code>git push origin vX.Y.Z</code>. This is what tells the Go module proxies that a new version exists.</li>
</ul>
</li>
<li><p><strong>Letting Go Proxies Know (No, You Don&#39;t &quot;Publish&quot; It Directly):</strong>
Here&#39;s a cool part: Go module proxies (like <code>proxy.golang.org</code>) are pretty smart. They&#39;ll usually find your new module version automatically once you push that tag. You don&#39;t typically run a &quot;publish&quot; command.</p>
<ul>
<li><strong>Want to nudge it?</strong> If you&#39;re impatient (we get it!), you can explicitly ask a proxy to fetch your new version. For example, running <code>go get github.com/fezcode/go-tournament-brackets@v0.1.0</code> (or <code>go list -m ...</code>) from <em>any</em> Go project will make the proxy grab it if it hasn&#39;t already. This is more about <em>verifying</em> discovery than publishing.</li>
</ul>
</li>
<li><p><strong>Patience, Young Padawan (Waiting for <code>pkg.go.dev</code>):</strong>
After your module is tagged and the proxies know about it, <code>pkg.go.dev</code> (Go&#39;s official package discovery site) will eventually list it. Just a heads-up: this isn&#39;t instant. It can take anywhere from a few minutes to a few hours for it to show up. So, grab a coffee, and it&#39;ll be there!</p>
</li>
</ol>
<p><a href="https://fezcode.com/blog/do-i-need-to-create-a-lib-for-that">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[HashRouter vs. BrowserRouter]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/hashrouter-vs-browserrouter</link>
            <guid isPermaLink="false">https://fezcode.com/blog/hashrouter-vs-browserrouter</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Wed, 15 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>HashRouter vs. BrowserRouter</h1>
<p>When building Single-Page Applications (SPAs) with React, <code>react-router-dom</code> is a go-to library for handling client-side routing. It offers two primary router components: <code>HashRouter</code> and <code>BrowserRouter</code>.
While both achieve the goal of navigating between different views in your application without full page reloads,
they do so in fundamentally different ways, and these differences become crucial when deploying to static hosting environments like GitHub Pages.</p>
<h2>The Core Difference: How They Handle URLs</h2>
<p>The key to understanding <code>HashRouter</code> and <code>BrowserRouter</code> lies in how they interact with the browser&#39;s URL structure, specifically the <code>path</code> and <code>fragment</code> (or hash) parts of a URL.</p>
<p>A typical URL looks something like this: <code>protocol://hostname:port/path?query#fragment</code></p>
<ul>
<li><strong><code>/path</code></strong>: This part of the URL is sent to the server. The server uses it to identify and serve a specific resource (e.g., an HTML file, an image, etc.).</li>
<li><strong><code>#fragment</code></strong>: This part, also known as the hash, is <em>never</em> sent to the server. It&#39;s entirely handled by the client-side (the web browser).</li>
</ul>
<h3><code>HashRouter</code>: The Static Hosting Friend</h3>
<ul>
<li><strong>URL Structure:</strong> <code>https://yourdomain.com/#/blog/my-post</code></li>
<li><strong>How it Works:</strong><ol>
<li>When a user navigates to a URL with a hash (e.g., <code>#/blog/my-post</code>), the server (in this case, GitHub Pages) only sees the part of the URL <em>before</em> the hash: <code>https://yourdomain.com/</code>.</li>
<li>GitHub Pages, being a static file server, simply looks for and serves the <code>index.html</code> file located at the root of your deployment.</li>
<li>Once <code>index.html</code> loads, your React application starts. <code>HashRouter</code> then inspects the full URL in the browser&#39;s address bar, extracts the route information from after the hash (<code>/blog/my-post</code>), and renders the corresponding component.</li>
</ol>
</li>
<li><strong>Why it Works on GitHub Pages:</strong> Because the server never sees the route information (e.g., <code>/blog/my-post</code>), it never tries to find a physical file at that path. It always serves <code>index.html</code>, and your client-side JavaScript handles all the routing. This makes <code>HashRouter</code> a very robust and straightforward choice for static hosting where you don&#39;t have server-side routing capabilities.</li>
</ul>
<h3><code>BrowserRouter</code>: The Clean URL Enthusiast (with a Catch)</h3>
<ul>
<li><strong>URL Structure:</strong> <code>https://yourdomain.com/blog/my-post</code></li>
<li><strong>How it Works:</strong><ol>
<li>When a user navigates to a URL without a hash (e.g., <code>/blog/my-post</code>), the server receives the <em>entire path</em>: <code>https://yourdomain.com/blog/my-post</code>.</li>
<li>GitHub Pages, as a static server, will then try to find a physical file at <code>/blog/my-post</code>.</li>
</ol>
</li>
<li><strong>Why it&#39;s Tricky on GitHub Pages:</strong> Since your React application is a Single-Page Application, there isn&#39;t a physical file named <code>my-post</code> (or <code>blog/my-post</code>) on the server. All your application&#39;s code is bundled into <code>index.html</code> and its associated JavaScript files. Consequently, GitHub Pages returns a 404 &quot;Not Found&quot; error because it can&#39;t find a file at the requested path. Your <code>index.html</code> is never served, and your React app never gets a chance to load and handle the routing.</li>
</ul>
<h2>The <code>404.html</code> Workaround for <code>BrowserRouter</code> (and Why It&#39;s Often More Trouble)</h2>
<p>To make <code>BrowserRouter</code> work on GitHub Pages, a common workaround involves creating a custom <code>404.html</code> file. This file contains a JavaScript script that, when served by GitHub Pages (because a 404 occurred), attempts to:</p>
<ol>
<li>Rewrite the URL in the browser&#39;s history to the original requested path (e.g., <code>/blog/my-post</code>).</li>
<li>Redirect the browser to your <code>index.html</code>.</li>
</ol>
<p>The idea is that once <code>index.html</code> loads, <code>BrowserRouter</code> will see the rewritten URL and render the correct component. However, this approach is often fraught with issues:</p>
<ul>
<li><strong>Timing and Browser Behavior:</strong> The script&#39;s execution, <code>history.replaceState</code>, and the subsequent redirect can be sensitive to browser behavior, caching, and network timing.</li>
<li><strong>Flickering/Double Redirects:</strong> Users might experience a brief flicker of the 404 page or multiple redirects before the correct content loads.</li>
<li><strong>Debugging Complexity:</strong> Debugging issues in this setup can be challenging due to the asynchronous nature of the redirects and the interaction between the server&#39;s 404 handling and client-side JavaScript.</li>
</ul>
<h2>Conclusion: Choose Wisely for Static Hosting</h2>
<p>For projects deployed on static hosting services like GitHub Pages, <code>HashRouter</code> offers a simpler, more reliable, and less problematic solution. While <code>BrowserRouter</code> provides aesthetically cleaner URLs, the effort required to make it work consistently on static hosts often outweighs the benefits, especially for personal projects or portfolios where server-side configuration is not an option. If clean URLs are an absolute requirement, a more robust hosting solution with server-side routing capabilities (like Netlify, Vercel, or a custom server) would be a better fit.</p>
<p><a href="https://fezcode.com/blog/hashrouter-vs-browserrouter">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[About Fezcodex]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/about-fezcodex</link>
            <guid isPermaLink="false">https://fezcode.com/blog/about-fezcodex</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Tue, 14 Oct 2025 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>My Warning Post (and what is this site about)</h1>
<p><strong>Welcome to my little corner of the internet!</strong> This isn&#39;t just a website; it&#39;s my personal playground, a digital diary where I ramble about pretty much anything that catches my fancy.
I&#39;m <em>A. Samil Bulbul</em>, a senior software engineer by trade, but here, <strong>I&#39;m just me</strong> - sharing thoughts, creations, and obsessions.</p>
<p>You&#39;ll find a <strong>mixed bag</strong> here, from deep dives into the fascinating world of computer science and engineering (think React, Go, JavaScript, and Linux wizardry)
to my unfiltered rants about <strong>life</strong> (<em>not really</em>), the universe, and everything in between. But it&#39;s not all serious code and existential crises! I&#39;m also a huge fan of escaping into other worlds,
so expect plenty of reviews and musings on the latest (or classic) video games that have stolen my hours (Vampire: The Masquerade - Bloodlines 2, anyone?).
And when I&#39;m not gaming, I&#39;m probably lost in a good book, binging a captivating series like <em>&quot;The Bear&quot;</em>,  or grooving to some timeless tunes (<em>De La Soul always on rotation</em>).</p>
<p>This space is entirely my own, a place where my ideas, opinions, and sometimes questionable tastes run wild.
If you&#39;re looking for polished, perfectly curated content,
you might find some of that, but mostly, you&#39;ll find genuine, unadulterated me. Dive in, explore, and enjoy the ride!</p>
<p><a href="https://fezcode.com/blog/about-fezcodex">Read more...</a></p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Gemini 2.5 Flash Explains me How Image Modal Works]]></title>
            <description><![CDATA[[object Object]]]></description>
            <link>https://fezcode.com/blog/gemini-explains-how-image-modal-works</link>
            <guid isPermaLink="false">https://fezcode.com/blog/gemini-explains-how-image-modal-works</guid>
            <dc:creator><![CDATA[Ahmed Samil Bulbul]]></dc:creator>
            <pubDate>Fri, 05 Jan 2024 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<h1>Gemini Explains How Image Modal Works</h1>
<p>Ok, this is a first for me. I asked, as a <em>backend developer</em> who knows nothing about <strong>React</strong>, Gemini to explain the <code>ImageModal.js</code> component.
I gave the following prompt:</p>
<blockquote>
<p>can you explain to me everything done. Explain to me every word if necessary.
I don&#39;t know anything about react so write it to text file called <code>explanation.md</code>.
Write everything step by step to that file. As I said I don&#39;t know what <code>useEffect</code> is or what
<code>[src]</code> does so explain everything in ImageModal.js and LogDetailPage.js files.</p>
</blockquote>
<p>And I believe it gave me a great starting point. Where is the result. Enjoy.</p>
<hr>
<h1>Explanation of Changes: Adding an Image Modal</h1>
<p>This document explains the changes made to implement an image modal feature, allowing users to click on images within log entries to view them in full size with animations. This explanation is tailored for someone unfamiliar with React.js.</p>
<hr>
<h2>Core Concepts Explained</h2>
<p>Before diving into the file-specific changes, let&#39;s understand some fundamental React concepts used:</p>
<ul>
<li><strong>Components:</strong> In React, a &quot;component&quot; is a self-contained, reusable piece of code that represents a part of your user interface. Think of it like a building block. For example, a button, a navigation bar, or an entire page can be a component. We created a new component called <code>ImageModal</code>.</li>
<li><strong>Props (Properties):</strong> Props are how you pass data from a parent component to a child component. They are like arguments to a function. For example, our <code>ImageModal</code> component receives <code>src</code> (the image source) and <code>onClose</code> (a function to close the modal) as props.</li>
<li><strong>State:</strong> State is data that a component manages internally and can change over time. When a component&#39;s state changes, React automatically re-renders that component to reflect the new data. We use <code>useState</code> to manage state.</li>
<li><strong>Hooks:</strong> Hooks are special functions in React that let you &quot;hook into&quot; React features like state and lifecycle methods from functional components (components written as JavaScript functions).<ul>
<li><strong><code>useState</code>:</strong> This hook lets you add React state to functional components. It returns a pair: the current state value and a function that lets you update it. For example, <code>const [modalImageSrc, setModalImageSrc] = useState(null);</code> means <code>modalImageSrc</code> holds the current image source for the modal (initially <code>null</code>, meaning no image is open), and <code>setModalImageSrc</code> is the function you call to change it.</li>
<li><strong><code>useEffect</code>:</strong> This hook lets you perform &quot;side effects&quot; in functional components. Side effects are things like data fetching, subscriptions, or manually changing the DOM (Document Object Model, which is the structure of your web page).<ul>
<li><code>useEffect(() =&gt; { /* code */ }, [dependencies])</code>: The first argument is a function containing your effect logic. The second argument, <code>[dependencies]</code>, is an array of values that the effect depends on. If any value in this array changes between renders, the effect function will run again. If the array is empty (<code>[]</code>), the effect runs only once after the initial render (like <code>componentDidMount</code> in class components). If you omit the array entirely, the effect runs after every render.</li>
<li><strong><code>[src]</code> in <code>useEffect</code>:</strong> In our <code>ImageModal.js</code>, <code>useEffect(() =&gt; { ... }, [src]);</code> means that the code inside this <code>useEffect</code> will run whenever the <code>src</code> prop changes. This is crucial for controlling the body&#39;s scroll behavior: when <code>src</code> becomes available (modal opens), we hide the scrollbar; when <code>src</code> becomes <code>null</code> (modal closes), we restore it. The <code>return () =&gt; { ... };</code> part is a &quot;cleanup&quot; function that runs when the component unmounts or before the effect runs again, ensuring we always restore the scrollbar.</li>
</ul>
</li>
</ul>
</li>
<li><strong><code>framer-motion</code>:</strong> This is a popular animation library for React. It provides components like <code>motion.div</code> and <code>AnimatePresence</code> to easily add animations.<ul>
<li><strong><code>motion.div</code>:</strong> A special <code>div</code> component from <code>framer-motion</code> that accepts animation props like <code>initial</code> (starting state), <code>animate</code> (ending state), <code>exit</code> (state when component is removed), and <code>transition</code> (how long the animation takes).</li>
<li><strong><code>AnimatePresence</code>:</strong> A component from <code>framer-motion</code> that enables components to animate when they are removed from the React tree (e.g., when <code>src</code> becomes <code>null</code> and the modal disappears). Without <code>AnimatePresence</code>, <code>exit</code> animations wouldn&#39;t work.</li>
</ul>
</li>
<li><strong><code>ReactMarkdown</code>:</strong> A library used to render Markdown text (like the content of your log files) as HTML in a React application.<ul>
<li><strong><code>components</code> prop:</strong> This prop allows you to override how <code>ReactMarkdown</code> renders specific HTML elements. For example, we tell it to use our custom <code>ImageRenderer</code> component whenever it encounters an <code>&lt;img&gt;</code> tag.</li>
</ul>
</li>
</ul>
<hr>
<h2><code>src/components/ImageModal.js</code> Explained</h2>
<p>This file defines the <code>ImageModal</code> component, which is responsible for displaying a full-size image in an overlay and handling its opening/closing animations.</p>
<pre><code class="language-javascript">import React, { useEffect } from &#39;react&#39;; // Import React and the useEffect hook
import { X } from &#39;@phosphor-icons/react&#39;; // Import the &#39;X&#39; icon for the close button
import { motion, AnimatePresence } from &#39;framer-motion&#39;; // Import motion and AnimatePresence for animations

const ImageModal = ({ src, alt, onClose }) =&gt; { // Define the ImageModal component, receiving src, alt, and onClose as props
  useEffect(() =&gt; { // This effect runs when src changes
    if (src) { // If an image source is provided (modal is open)
      document.body.style.overflow = &#39;hidden&#39;; // Prevent scrolling on the main page
    } else { // If no image source (modal is closed)
      document.body.style.overflow = &#39;unset&#39;; // Restore scrolling
    }
    return () =&gt; { // This cleanup function runs when the component unmounts or before the effect re-runs
      document.body.style.overflow = &#39;unset&#39;; // Ensure scrolling is restored
    };
  }, [src]); // The effect re-runs whenever the &#39;src&#39; prop changes

  // The modal is only rendered if &#39;src&#39; has a value (i.e., an image is to be displayed)
  return (
    &lt;AnimatePresence&gt; {/* AnimatePresence enables exit animations for components */}
      {src &amp;&amp; ( // Conditionally render the modal only if &#39;src&#39; exists
        &lt;motion.div // This is the main modal overlay, animated by framer-motion
          className=&quot;fixed inset-0 bg-black bg-opacity-75 flex justify-center items-center z-50 p-4&quot;
          onClick={onClose} // Clicking the overlay closes the modal
          initial={{ opacity: 0 }} // Initial animation state (fully transparent)
          animate={{ opacity: 1 }} // Animation to (fully opaque)
          exit={{ opacity: 0 }} // Animation when component is removed (fade out)
        &gt;
          &lt;motion.div // This is the container for the image and close button, also animated
            className=&quot;relative&quot;
            onClick={e =&gt; e.stopPropagation()} // Prevent clicks on the image/button from closing the modal
            initial={{ scale: 0.8, opacity: 0 }} // Initial state (smaller and transparent)
            animate={{ scale: 1, opacity: 1 }} // Animation to (normal size, opaque)
            exit={{ scale: 0.8, opacity: 0 }} // Animation when removed (shrink and fade out)
            transition={{ duration: 0.2 }} // Animation duration
          &gt;
            &lt;button // The close button
              onClick={onClose} // Closes the modal when clicked
              className=&quot;absolute top-2 right-2 text-white text-2xl bg-gray-800 rounded-full p-2 hover:bg-gray-700 focus:outline-none&quot;
            &gt;
              &lt;X size={24} weight=&quot;bold&quot; /&gt; {/* The &#39;X&#39; icon */}
            &lt;/button&gt;
            &lt;img src={src} alt={alt} className=&quot;max-w-full max-h-[90vh] object-contain&quot; /&gt; {/* The actual image */}
          &lt;/motion.div&gt;
        &lt;/motion.div&gt;
      )}
    &lt;/AnimatePresence&gt;
  );
};

export default ImageModal; // Export the component so it can be used in other files
</code></pre>
<p><strong>Key Changes in <code>ImageModal.js</code>:</strong></p>
<ol>
<li><strong>Imports:</strong> Added <code>motion</code> and <code>AnimatePresence</code> from <code>framer-motion</code>.</li>
<li><strong>Conditional Rendering with <code>AnimatePresence</code>:</strong> The entire modal content is now wrapped in <code>&lt;AnimatePresence&gt;</code> and conditionally rendered using <code>{src &amp;&amp; (...) }</code>. This tells <code>framer-motion</code> to watch for when the <code>src</code> prop becomes <code>null</code> and the modal is about to disappear, allowing the <code>exit</code> animations to play.</li>
<li><strong><code>motion.div</code> for Animations:</strong><ul>
<li>The outer <code>div</code> (the dark overlay) is now a <code>motion.div</code> with <code>initial={{ opacity: 0 }}</code>, <code>animate={{ opacity: 1 }}</code>, and <code>exit={{ opacity: 0 }}</code> for a fade-in/fade-out effect.</li>
<li>The inner <code>div</code> (containing the image and close button) is also a <code>motion.div</code> with <code>initial={{ scale: 0.8, opacity: 0 }}</code>, <code>animate={{ scale: 1, opacity: 1 }}</code>, and <code>exit={{ scale: 0.8, opacity: 0 }}</code>. This creates a subtle &quot;pop&quot; effect where the image scales up slightly as it appears and scales down as it disappears.</li>
<li><code>transition={{ duration: 0.2 }}</code> sets the animation speed to 0.2 seconds.</li>
</ul>
</li>
</ol>
<hr>
<h2><code>src/pages/LogDetailPage.js</code> Explained</h2>
<p>This file is responsible for displaying the detailed content of a single log entry. The changes here involve integrating the <code>ImageModal</code> and making images clickable.</p>
<pre><code class="language-javascript">// ... (existing imports)
import ImageModal from &#39;../components/ImageModal&#39;; // NEW: Import the ImageModal component

// ... (LinkRenderer component)

const LogDetailPage = () =&gt; {
  // ... (existing useParams, useState, useRef)

  // NEW: State to manage the image modal.
  // modalImageSrc will hold the URL of the image to display in the modal, or null if no modal is open.
  // setModalImageSrc is the function to update this state.
  const [modalImageSrc, setModalImageSrc] = useState(null);

  // ... (useEffect for fetching log data)

  // ... (loading and not found states)

  // NEW: Custom component to render &lt;img&gt; tags within ReactMarkdown.
  // This allows us to add custom behavior (like opening a modal) to images.
  const ImageRenderer = ({ src, alt }) =&gt; (
    &lt;img
      src={src} // The source URL of the image
      alt={alt} // The alt text for accessibility
      className=&quot;cursor-pointer max-w-full h-auto&quot; // Styling for the image, including making it look clickable
      onClick={() =&gt; setModalImageSrc(src)} // When the image is clicked, set its source to modalImageSrc, which opens the modal
    /&gt;
  );

  return (
    &lt;div className=&quot;bg-gray-900 py-16 sm:py-24&quot;&gt;
      &lt;div className=&quot;mx-auto max-w-7xl px-6 lg:px-8&quot;&gt;
        &lt;div className=&quot;lg:grid lg:grid-cols-4 lg:gap-8&quot;&gt;
          &lt;div className=&quot;lg:col-span-3&quot;&gt;
            &lt;Link to=&quot;/logs&quot; className=&quot;text-primary-400 hover:underline flex items-center justify-center gap-2 text-lg mb-4&quot;&gt;
              &lt;ArrowLeftIcon size={24} /&gt; Back to Logs
            &lt;/Link&gt;
            &lt;div ref={contentRef} className=&quot;prose prose-xl prose-dark max-w-none&quot;&gt;
              {/* MODIFIED: ReactMarkdown now uses our custom ImageRenderer for &lt;img&gt; tags */}
              &lt;ReactMarkdown components={{ a: LinkRenderer, img: ImageRenderer }}&gt;{log.body}&lt;/ReactMarkdown&gt;
            &lt;/div&gt;
          &lt;/div&gt;
          &lt;div className=&quot;hidden lg:block&quot;&gt;
            &lt;LogMetadata metadata={log.attributes} /&gt;
          &lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;
      {/* NEW: The ImageModal component is rendered here. */}
      {/* It receives the image source from modalImageSrc and a function to close itself. */}
      &lt;ImageModal src={modalImageSrc} alt=&quot;Full size image&quot; onClose={() =&gt; setModalImageSrc(null)} /&gt;
    &lt;/div&gt;
  );
};

export default LogDetailPage;
</code></pre>
<p><strong>Key Changes in <code>LogDetailPage.js</code>:</strong></p>
<ol>
<li><strong>Import <code>ImageModal</code>:</strong> We added <code>import ImageModal from &#39;../components/ImageModal&#39;;</code> at the top of the file so we can use our new modal component.</li>
<li><strong><code>useState</code> for <code>modalImageSrc</code>:</strong><ul>
<li><code>const [modalImageSrc, setModalImageSrc] = useState(null);</code> was added. This creates a piece of state called <code>modalImageSrc</code>. Its initial value is <code>null</code>, meaning no image is currently open in the modal.</li>
<li><code>setModalImageSrc</code> is the function we&#39;ll use to change <code>modalImageSrc</code>. When we call <code>setModalImageSrc(&#39;path/to/image.jpg&#39;)</code>, the modal will open with that image. When we call <code>setModalImageSrc(null)</code>, the modal will close.</li>
</ul>
</li>
<li><strong><code>ImageRenderer</code> Component:</strong><ul>
<li>This is a small, new component defined within <code>LogDetailPage</code>. Its job is to tell <code>ReactMarkdown</code> how to render <code>&lt;img&gt;</code> tags.</li>
<li>Instead of just rendering a plain <code>&lt;img&gt;</code> tag, our <code>ImageRenderer</code> adds an <code>onClick</code> event handler.</li>
<li><code>onClick={() =&gt; setModalImageSrc(src)}</code>: When a user clicks on an image, this function is called. It updates the <code>modalImageSrc</code> state with the <code>src</code> (source URL) of the clicked image. This change in state then triggers the <code>ImageModal</code> to appear.</li>
</ul>
</li>
<li><strong><code>ReactMarkdown</code> <code>components</code> Prop Modification:</strong><ul>
<li>The line <code>&lt;ReactMarkdown components={{ a: LinkRenderer }}&gt;{log.body}&lt;/ReactMarkdown&gt;</code> was changed to <code>&lt;ReactMarkdown components={{ a: LinkRenderer, img: ImageRenderer }}&gt;{log.body}&lt;/ReactMarkdown&gt;</code>.</li>
<li>This tells <code>ReactMarkdown</code> to use our <code>LinkRenderer</code> for <code>&lt;a&gt;</code> (link) tags and our new <code>ImageRenderer</code> for <code>&lt;img&gt;</code> (image) tags. Any other HTML tags will be rendered by <code>ReactMarkdown</code>&#39;s default behavior.</li>
</ul>
</li>
<li><strong>Rendering <code>ImageModal</code>:</strong><ul>
<li><code>&lt;ImageModal src={modalImageSrc} alt=&quot;Full size image&quot; onClose={() =&gt; setModalImageSrc(null)} /&gt;</code> was added at the end of the <code>LogDetailPage</code>&#39;s return statement.</li>
<li><code>src={modalImageSrc}</code>: This passes the current value of our <code>modalImageSrc</code> state to the <code>ImageModal</code>. If <code>modalImageSrc</code> is <code>null</code>, the <code>ImageModal</code> won&#39;t render (or will animate out if it was previously open). If it has an image URL, the modal will display that image.</li>
<li><code>onClose={() =&gt; setModalImageSrc(null)}</code>: This passes a function to the <code>ImageModal</code>. When the <code>ImageModal</code>&#39;s close button is clicked, or the overlay is clicked, it calls this <code>onClose</code> function, which in turn sets <code>modalImageSrc</code> back to <code>null</code>, causing the modal to close.</li>
</ul>
</li>
</ol>
<hr>
<h2>Step-by-Step Summary of Actions</h2>
<ol>
<li><p><strong>Created <code>src/components/ImageModal.js</code>:</strong></p>
<ul>
<li>A new file was created to house the <code>ImageModal</code> component.</li>
<li>This component handles displaying the full-size image, the close button, and the overlay.</li>
<li>It uses <code>useEffect</code> to control body scrolling when the modal is open/closed.</li>
<li>It uses <code>framer-motion</code>&#39;s <code>motion.div</code> and <code>AnimatePresence</code> for animated transitions (fade and scale) when opening and closing.</li>
</ul>
</li>
<li><p><strong>Modified <code>src/pages/LogDetailPage.js</code> (First <code>replace</code> call):</strong></p>
<ul>
<li>Added <code>import ImageModal from &#39;../components/ImageModal&#39;;</code> to bring the new modal component into <code>LogDetailPage</code>.</li>
<li>Added <code>const [modalImageSrc, setModalImageSrc] = useState(null);</code> to manage the state of the modal (whether it&#39;s open and which image it should display).</li>
</ul>
</li>
<li><p><strong>Modified <code>src/pages/LogDetailPage.js</code> (Second <code>replace</code> call):</strong></p>
<ul>
<li>Defined a new functional component <code>ImageRenderer</code> within <code>LogDetailPage</code>. This component is a custom renderer for <code>&lt;img&gt;</code> tags. It takes the <code>src</code> and <code>alt</code> of an image and renders it, but also adds an <code>onClick</code> handler that calls <code>setModalImageSrc(src)</code> to open the modal with the clicked image.</li>
<li>Updated the <code>ReactMarkdown</code> component&#39;s <code>components</code> prop to include <code>img: ImageRenderer</code>. This tells <code>ReactMarkdown</code> to use our custom <code>ImageRenderer</code> whenever it encounters an image in the Markdown content.</li>
<li>Added the <code>&lt;ImageModal&gt;</code> component to the <code>LogDetailPage</code>&#39;s render output. It receives <code>modalImageSrc</code> as its <code>src</code> prop and a function to close itself (<code>onClose={() =&gt; setModalImageSrc(null)}</code>).</li>
</ul>
</li>
</ol>
<p>These changes collectively enable the interactive image modal feature with smooth animations.</p>
<p><a href="https://fezcode.com/blog/gemini-explains-how-image-modal-works">Read more...</a></p>]]></content:encoded>
        </item>
    </channel>
</rss>